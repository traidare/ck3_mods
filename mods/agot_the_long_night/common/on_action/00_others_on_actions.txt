on_vassal_gained = {
	effect = {
		if = {
			limit = {
				root = { has_trait = other_trait }
			}
			root = { save_scope_as = thegreatother }
			scope:vassal = {
				mutate_into_other_effect = yes
				every_courtier_or_guest = {
					mutate_into_other_effect = yes
				}
			}
		}
	}
}
on_title_granted = {
	effect = {
		if = {
			limit = {
				root = { has_trait = other_trait }
			}
			root = { save_scope_as = thegreatother }
			scope:recipient = {
				mutate_into_other_effect = yes
				every_courtier_or_guest = {
					mutate_into_other_effect = yes
				}
			}
		}
	}
}
on_title_gain = {
	effect = {
		if = {
			limit = { root = { has_trait = other_trait } }
			if = {
				limit = { NOT = { has_character_flag = others_heartbeat_active } }
				trigger_event = { id = others_heartbeat.1 }
			}
		}
		if = {
			limit = {
				exists = root.liege
				root.liege = { has_trait = other_trait }
			}
			root.liege = {
				save_scope_as = thegreatother
				if = {
					limit = { NOT = { has_character_flag = others_heartbeat_active } }
					trigger_event = { id = others_heartbeat.1 }
				}
			}
			root = { mutate_into_other_effect = yes every_courtier_or_guest = { mutate_into_other_effect = yes } }
		}
	}
}
on_county_auto_granted_to_local_culture = {
	effect = {
		if = {
			limit = {
				scope:actor = { has_trait = other_trait }
			}
			scope:actor = { save_scope_as = thegreatother }
			scope:recipient = {
				mutate_into_other_effect = yes
				every_courtier_or_guest = {
					mutate_into_other_effect = yes
				}
			}
		}
	}
}
on_county_auto_granted_to_liege_culture = {
	effect = {
		if = {
			limit = {
				scope:actor = { has_trait = other_trait }
			}
			scope:actor = { save_scope_as = thegreatother }
			scope:recipient = {
				mutate_into_other_effect = yes
				every_courtier_or_guest = {
					mutate_into_other_effect = yes
				}
			}
		}
	}
}