# Grand Remembrance 1.8.1 / CK3 1.19 runtime repair.
# The GUI override checks GetPlayer.IsValid before constructing the root scope.

gr_chronicle_window = {
	scope = character

	is_shown = {
		is_alive = yes
		has_variable = gr_chronicle_open
		var:gr_chronicle_open > 0
	}
}
