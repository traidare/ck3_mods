#Setup
#asoiaf_destroy_crownlands_title_effect = {
#	destroy_title = title:e_the_crownlands
#}

#asoiaf_test_coa_effect = {
#	add_character_flag = dynn_Baratheon_flag
#	primary_title = { generate_coa = agot_faction_coa }
#	remove_character_flag = dynn_Baratheon_flag
#}

asoiaf_destroy_ww_ok_effect = {
	root = {
		every_equipped_character_artifact = { #ALL Valyrian steel swords, but not Ice (one I've got in my inventory)
			limit = {
				has_variable = valyrian_steel
				NOT = { has_variable = asoiaf_is_ice }
			}
			save_scope_as = this_artifact
		}
		destroy_artifact = scope:this_artifact
	}
}

asoiaf_religion_doctrines_effect = {
	if = {
		limit = { has_game_rule = default_gender_equality }
		faith:old_gods_wnw = {
			remove_doctrine = doctrine_gender_equal
			add_doctrine = doctrine_gender_male_dominated #male dominated in the Seven Kingdoms
		}
		faith:valyrian_pan_freehold = {
			remove_doctrine = doctrine_gender_equal
			add_doctrine = doctrine_gender_male_dominated #evidence points to male dominance
		}
		faith:fots_dragon = {
			remove_doctrine = doctrine_gender_equal
			add_doctrine = doctrine_gender_male_dominated #same as regular Valyrian faith
		}
	}
	faith:old_gods_wnw = {
		remove_doctrine = doctrine_consanguinity_cousins
		add_doctrine = doctrine_consanguinity_aunt_nephew_and_uncle_niece #examples exist of northmen marrying nieces
	}
	faith:rhllor_fots = { #Red God is based on the faith followed by Stannis and his supporters, which is 100% fundamentalist
		remove_doctrine = doctrine_pluralism_righteous
		add_doctrine = doctrine_pluralism_fundamentalist
	}
}

asoiaf_setup_houses_effect = {
	character:Baelish_2 ?= { #Petyr Baelish's House
		house = { save_scope_as = asoiaf_founders_old_house }
		set_house = house:house_BaelishHH
		dynasty = {
			every_dynasty_member = {
				limit = {
					any_ancestor = {
						this = character:Baelish_2
					}
					house = scope:asoiaf_founders_old_house
				}
				set_house = house:house_BaelishHH
				trigger_event = { id = agot_coa_events.1 }
			}
		}
		add_to_global_variable_list = {
			name = asoiaf_baelish_hh_unavailable
			target = flag:asoiaf_baelish_cadet_hh
		}
	}
	character:Ryswell_3 ?= { #Rodrik Ryswell (son of Torrhen)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Ryswell_3 COA_KEY = rodrick_ryswell_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Ryswell_4 ?= { #Barbrey Ryswell (daughter of Rodrik)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
				OR = {
					primary_spouse.dynasty ?= dynasty:dynn_Dustin
					current_date >= 8283.8.1 #she inherits Barrowton after her husband dies
				}
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Ryswell_4 COA_KEY = barbery_dustin_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Frey_79 ?= { #Benfrey Frey (son of Walder)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Frey_79 COA_KEY = benfrey_frey_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Crossing_3 ?= { #Aemon of the Crossing (son of Walder)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Crossing_3 COA_KEY = aemon_rivers_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Frey_54 ?= { #Little Walder Frey
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Frey_54 COA_KEY = little_walder_frey_personal_coa }
		}
	}
	character:Frey_73 ?= { #Big Walder Frey
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Frey_73 COA_KEY = big_walder_frey_personal_coa }
		}
	}
	character:Tyrell_12 ?= { #Garlan Tyrell (son of Mace)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Tyrell_12 COA_KEY = garlan_tyrell_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Tyrell_13 ?= { #Loras Tyrell (son of Mace)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Tyrell_13 COA_KEY = loras_tyrell_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Tully_9 ?= { #Brynden Blackfish
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Tully_9 COA_KEY = bryden_blackfish_tully_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Frey_16 ?= { #Cleos Frey (son of Emmon)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Frey_16 COA_KEY = cleos_frey_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Greyjoy_6 ?= { #Euron Greyjoy (son of Quellon)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Greyjoy_6 COA_KEY = euron_greyjoy_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Harlaw_13 ?= { #Hotho Humpback
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Harlaw_13 COA_KEY = hotho_harlaw_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Harlaw_12 ?= { #Harras the Knight
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Harlaw_12 COA_KEY = harras_harlaw_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Harlaw_14 ?= { #Boremund the Blue
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Harlaw_14 COA_KEY = boremund_harlaw_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Harlaw_2 ?= { #Sigfryd Silverhair
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Harlaw_2 COA_KEY = sigfryd_harlaw_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_63 ?= { #Rhaenyra Targaryen (daughter of Viserys I)
		if = {
			limit = {
				is_alive = yes
				is_married = yes #looks odd to have a quartered sigil with a husband you haven't married yet
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_63 COA_KEY = rhaenrya_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_65 ?= { #Aegon II Targaryen
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_65 COA_KEY = aegon_ii_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_61 ?= { #Daemon Targaryen (son of Baelon)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_61 COA_KEY = daemon_targaryen_personal_coa }
		}
	}
	character:Targaryen_95 ?= { #Aegor Bittersteel
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
				NOT = { house ?= house:house_Bittersteel }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_95 COA_KEY = Bittersteel_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_96 ?= { #Brynden Bloodraven
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_96 COA_KEY = bloodraven_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Blackfyre_11 ?= { #Daemon III Blackfyre
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Blackfyre_11 COA_KEY = daemon_iii_blackfyre_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_111 ?= { #Valarr Targaryen (son of Baelor)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_111 COA_KEY = valarr_targaryen_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_110 ?= { #Maekar I Targaryen
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_110 COA_KEY = maekar_targaryen_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_118 ?= { #Daeron Targaryen (son of Maekar I)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_118 COA_KEY = daeron_targaryen_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_119 ?= { #Aerion Targaryen (son of Maekar I)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_119 COA_KEY = aerion_targaryen_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Blackwood_84 ?= { #Robert Blackwood (son of Edmund)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Blackwood_84 COA_KEY = robert_blackwood_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Blackwood_85 ?= { #Roland Blackwood (son of Edmund)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Blackwood_85 COA_KEY = roland_blackwood_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Blackwood_86 ?= { #Roger Blackwood (son of Edmund)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Blackwood_86 COA_KEY = roger_blackwood_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	#character:Stark_5 ?= { #Lyanna Stark (daughter of Rickard)
	#	if = {
	#		limit = {
	#			is_alive = yes
	#			NOT = { has_character_flag = has_personal_coa }
	#		}
	#		agot_create_personal_coa_for_historical_ai = { ACTOR = character:Stark_5 COA_KEY = laughing_tree_personal_coa }
	#	}
	#}
	character:Stark_6 ?= { #Robb Stark (son of Eddard)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Stark_6 COA_KEY = robb_stark_personal_coa }
		}
	}
	character:Hardyng_5 ?= { #Harrold Hardyng (son of Ronnel)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Hardyng_5 COA_KEY = harrold_hardyng_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Arryn_hh_4 ?= { #Hugh of the Vale
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Arryn_hh_4 COA_KEY = hugh_of_the_vale_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Targaryen_60 ?= { #Viserys I Targaryen
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Targaryen_60 COA_KEY = viserys_I_targaryen_personal_coa }
		}
	}
	character:Lannister_9 ?= { #Lancel Lannister (son of Kevan)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
				has_title ?= title:c_darry
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Lannister_9 COA_KEY = lancel_lannister_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Baratheon_4 ?= { #Renly Baratheon (son of Steffon)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
				primary_spouse.dynasty ?= dynasty:dynn_Tyrell
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Baratheon_4 COA_KEY = house_BaratheonSE }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Hightower_99 ?= { #Gwayne Hightower (son of Otto)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Hightower_99 COA_KEY = gwayne_hightower_personal_arms }
		}
	}
	character:Hightower_92 ?= { #Ormund Hightower (son of Hobert)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Hightower_92 COA_KEY = ormund_hightower_personal_arms }
		}
	}
	character:Bracken_56 ?= { #Amos Bracken
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Bracken_56 COA_KEY = amos_bracken_personal_coa }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Strong_31 ?= { #Larys Strong (son of Lyonel)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Strong_31 COA_KEY = larys_strong_personal_coa }
		}
	}
	character:Baelish_2 ?= { #Petyr Baelish (Littlefinger)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Baelish_2 COA_KEY = petyr_baelish_personal_coa }
		}
	}
	character:Beesbury_46 ?= { #Humfrey Beesbury (son of Arros)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Beesbury_46 COA_KEY = humfrey_beesbury_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Baratheon_71 ?= { #Lyonel Baratheon (son of Symeon)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Baratheon_71 COA_KEY = lyonel_baratheon_personal_arms }
		}
		if = {
			limit = {
				is_alive = yes
				is_landed = yes
				has_character_flag = has_personal_coa
			}
			every_held_title = {
				limit = {
					agot_is_valid_dyn_coa_title = yes
				}
				set_coa = holder.var:my_personal_coa.house
			}
			add_character_flag = landed_coa_personalized
		}
	}
	character:Clegane_3 ?= { #Gregor Clegane (the Mountain)
		if = {
			limit = {
				is_alive = yes
				NOT = { has_character_flag = has_personal_coa }
			}
			agot_create_personal_coa_for_historical_ai = { ACTOR = character:Clegane_3 COA_KEY = gregor_clegane_personal_coa }
		}
	}
	
	#HISTORICAL FIGURES
	#exists = character:Targaryen_61 #Daemon Targaryen (son of Baelon)
	#character:Targaryen_61 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if he is already dead
	#		}
	#		set_house = house:house_Targaryen_Daemon
	#	}
	#}
	#exists = character:Targaryen_63 #Rhaenyra Targaryen (daughter of Viserys I)
	#character:Targaryen_63 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if she is already dead
	#		}
	#		set_house = house:house_Targaryen_Rhaenyra
	#	}
	#}
	#exists = character:Targaryen_65 #Aegon II Targaryen
	#character:Targaryen_65 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if he is already dead
	#		}
	#		set_house = house:house_Targaryen_Aegon_II
	#	}
	#}
	#character:Targaryen_95 ?= { #Aegor Bittersteel
	#	set_house = house:house_Bittersteel
	#}
	#character:Targaryen_96 ?= { #Brynden Bloodraven
	#	set_house = house:house_Bloodraven
	#}
	#exists = character:Targaryen_110 #Maekar I Targaryen
	#character:Targaryen_110 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if he is already dead
	#		}
	#		set_house = house:house_Targaryen_Maekar_I
	#	}
	#}
	#exists = character:Targaryen_111 #Valarr Targaryen (son of Baelor)
	#character:Targaryen_111 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if he is already dead
	#		}
	#		set_house = house:house_Targaryen_Valarr
	#	}
	#}
	#exists = character:Targaryen_118 #Daeron Targaryen (son of Maekar I)
	#character:Targaryen_118 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if he is already dead
	#		}
	#		set_house = house:house_Targaryen_Daeron
	#	}
	#}
	#exists = character:Targaryen_119 #Aerion Targaryen (son of Maekar I)
	#character:Targaryen_119 = {
	#	if = {
	#		limit = {
	#			is_alive = no #ONLY set House if he is already dead
	#		}
	#		set_house = house:house_Brightflame
	#	}
	#}
	
	#MISC
	dynasty:dynn_Essos ?= { #Young Griff's temp House during his reconquest
		add_dynasty_modifier = valyrian_heritage_dynasty_modifier
		set_variable = agot_allow_adelphogamous_incest
	}
	#dynasty:dynn_Velaryon.dynasty_founder.house ?= { set_variable = agot_allow_adelphogamous_incest } #so it doesn't apply to House Longwaters
	#dynasty:dynn_Aranys ?= { set_variable = agot_allow_adelphogamous_incest }
	dynasty:dynn_Targaryen ?= { set_variable = agot_allow_adelphogamous_incest add_dynasty_prestige = 250 } #if base AGOT mod doesn't set it for some reason
	if = {
		limit = { exists = house:house_Brightflame }
		character:Ruin_Empress ?= { trigger_event = { id = asoiaf_setup_events.8 days = 1 } } #House Brightflame dragonrider modifier removal event (delayed since the modifier is ALSO given on game start)
		house:house_Brightflame = { remove_variable = agot_allow_adelphogamous_incest }
	}
}

asoiaf_setup_characters_effect = {
	#every_living_character = { #Red Priest amulet startup effect
	#	limit = {
	#		AND = { #Red priest conditions; replace with the red priest trait once it's added
	#			has_trait = devoted
	#			religion = religion:the_rhllor_religion
	#		}
	#		#add exclusion for Thoros of Myr when he's got a custom ID
	#	}
	#	create_artifact_asoiaf_red_priest_necklace_effect = { OWNER = this }
	#}
	title:c_the_bloody_gate.holder ?= {
		if = {
			limit = {
				NOT = { title:c_the_bloody_gate.holder = title:e_the_vale.holder }
				NOR = { #designating your parent, grandparent, or great-grandparent your heir causes crashes!
					is_child_of = title:e_the_vale.holder
					is_grandchild_of = title:e_the_vale.holder
					is_great_grandchild_of = title:e_the_vale.holder
				}
			}
			title:c_the_bloody_gate.holder = { set_designated_heir_unsafe = title:e_the_vale.holder }
		}
	}
	character:Targaryen_1 ?= { #Aerys II Targaryen crown inheritance
		if = {
			limit = {
				current_date >= 8262.1.1 #Aerys II ascends the throne and commissions the crown
				current_date < 8283.6.1 #Aerys is killed
			}
			create_artifact_asoiaf_aerys_II_crown_effect_historical = { OWNER = this }
			trigger_event = { id = asoiaf_equip_crown_events.8 days = 1 } #equip Aerys II's crown with delay because of overlapping effects
		}
	}
	character:Baratheon_2 ?= { #Robert I Baratheon; Aerys II crown inheritance
		if = {
			limit = {
				current_date = 8283.6.1 #Robert inherits the crown via taking the iron throne (gives it to Stannis the next day)
			}
			create_artifact_asoiaf_aerys_II_crown_effect_historical = { OWNER = this }
		}
	}
	character:Baratheon_3 ?= { #Stannis Baratheon; Aerys II crown inheritance
		if = {
			limit = {
				current_date >= 8283.6.2 #Stannis is given the crown and wears it later as king
			}
			create_artifact_asoiaf_aerys_II_crown_effect_historical = { OWNER = this }
		}
		if = {
			limit = {
				current_date >= 8299.1.4 #Stannis declares himself king
			}
			asoiaf_equip_aerys_II_crown_artefact_effect = yes #equip Aerys II's crown
		}
	}
	character:Targaryen_3 ?= { #Rhaegar's sword inheritance
		if = {
			limit = {
				current_date >= 8275.1.1 #Rhaegar begins combat training; technically made in 278, but we want it available during the Defiance of Duskendale bookmark
				current_date < 8283.8.4 #Rhaegar dies on the Trident
				has_game_rule = asoiaf_new_clothes_valyrian_rule_on
			}
			asoiaf_create_artifact_targaryen_falchion_effect_historical = { OWNER = this }
			#Rhaegar's other sword is destroyed in a delayed startup event
		}
	}
	character:Lonmouth_2 ?= {
		if = {
			limit = {
				current_date >= 8283.8.4 #Richard picks it up from the battlefield
				current_date < 8283.10.28 #Richard gives it to Viserys on Dragonstone
				has_game_rule = asoiaf_new_clothes_valyrian_rule_on
			}
			asoiaf_create_artifact_targaryen_falchion_effect_historical = { OWNER = this }
		}
	}
	character:Targaryen_8 ?= {
		if = {
			limit = {
				current_date >= 8283.10.28 #Richard gives the sword to Viserys on Dragonstone
				current_date < 8298.8.1 #Viserys is killed by Drogo
				has_game_rule = asoiaf_new_clothes_valyrian_rule_on
			}
			asoiaf_create_artifact_targaryen_falchion_effect_historical = { OWNER = this }
		}
	}
	character:Targaryen_9 ?= {
		if = {
			limit = {
				current_date >= 8298.8.1 #Viserys is killed by Drogo and Daenerys takes the sword
				has_game_rule = asoiaf_new_clothes_valyrian_rule_on
			}
			asoiaf_create_artifact_targaryen_falchion_effect_historical = { OWNER = this }
		}
	}
	character:Baratheon_2 ?= {
		if = {
			limit = {
				current_date >= 8289.7.1 #sword is forged for Robert shortly after Greyjoy Rebellion
				is_alive = yes
			}
			asoiaf_create_artifact_krakenfall_effect_historical = { OWNER = this }
			set_global_variable = asoiaf_krakenfall_is_forged #so the on_action doesn't trigger
		}
	}
	character:Baratheon_5 ?= {
		if = {
			limit = {
				current_date >= 8298.10.28 #Joffrey inherits the sword form Robert
				is_alive = yes
			}
			asoiaf_create_artifact_krakenfall_effect_historical = { OWNER = this }
			set_global_variable = asoiaf_krakenfall_is_forged #so the on_action doesn't trigger
		}
	}
	character:Baratheon_7 ?= {
		if = {
			limit = {
				current_date >= 8300.1.1 #Tommen inherits the sword form Joffrey
				is_alive = yes
			}
			asoiaf_create_artifact_krakenfall_effect_historical = { OWNER = this }
			set_global_variable = asoiaf_krakenfall_is_forged #so the on_action doesn't trigger
		}
	}
	character:Baratheon_5 ?= { #Joffrey I gets Hearteater
		if = {
			limit = {
				current_date >= 8298.10.28 #Joffrey inherits the sword form Robert
				is_alive = yes
				has_game_rule = asoiaf_new_clothes_westerlands_rule_on
			}
			asoiaf_create_artifact_hearteater_effect_historical = { OWNER = this }
			every_character_artifact = {
				limit = { has_variable = asoiaf_is_hearteater }
				save_scope_as = this_artifact
			}
			scope:this_artifact = {
				equip_artifact_to_owner_replace = yes
			}
		}
	}
	character:Baratheon_7 ?= { #Tommen I inherits Hearteater
		if = {
			limit = {
				current_date >= 8300.1.1 #Tommen inherits the sword form Joffrey
				is_alive = yes
				has_game_rule = asoiaf_new_clothes_westerlands_rule_on
			}
			asoiaf_create_artifact_hearteater_effect_historical = { OWNER = this }
		}
	}
	character:Tyrell_13 ?= { #Loras Tyrell (son of Mace)
		if = {
			limit = {
				current_date >= 8299.6.12 #Loras retrieves the crown from Renly's corpse
				is_alive = yes
				character:Baratheon_4 ?= { is_alive = no } #Renly is dead
			}
			agot_create_artifact_renly_crown_historical_effect = { OWNER = this }
		}
	}
	character:Lannister_7 ?= { #when Jaime dies in the books, this needs to be made into a dynamic system using dates
		if = {
			limit = { has_game_rule = asoiaf_new_clothes_westerlands_rule_on }
			create_artifact_asoiaf_jaimes_sword_effect = { OWNER = this }
		}
		trigger_event = { id = asoiaf_setup_events.9 days = 1 } #destroy vanilla AGOT's Jaime sword and equip AGOT+'s Jaime's Sword with delay because of overlapping effects
		#every_character_artifact = { #not necessary anymore, but keeping it for future reference
		#	limit = { has_variable = asoiaf_is_jaimes_sword }
		#	save_scope_as = this_artifact
		#}
		#scope:this_artifact = {
		#	equip_artifact_to_owner_replace = yes
		#}
	}
	#title:c_winterfell.holder ?= {
	#	create_artifact_stark_throne_effect = { OWNER = this }
	#}
	character:Clegane_4 ?= { #Sandor Clegane (the Hound)
		if = {
			limit = {
				age >= 10 #so its available relatively early
				current_date < 8300.1.31 #the day Rorge steals the Hound's helm
				has_game_rule = asoiaf_new_clothes_westerlands_rule_on
			}
			asoiaf_create_artifact_hound_armour_effect_historical = { OWNER = this }
		}
	}
	character:Brave_Companions_10 ?= { #Rorge
		if = {
			limit = {
				current_date >= 8300.1.31 #the day Rorge steals the Hound's helm
				has_game_rule = asoiaf_new_clothes_westerlands_rule_on
			}
			asoiaf_create_artifact_hound_armour_effect_historical = { OWNER = this }
		}
	}
	character:Stark_7 ?= { #Jon Snow makes Needle
		if = {
			limit = {
				current_date = 8298.2.28 #the DAY it was forged; he gifts it to Arya the next day
				has_game_rule = asoiaf_new_clothes_north_rule_on
			}
			asoiaf_create_artifact_needle_effect_historical = { OWNER = this }
		}
	}
	character:Stark_9 ?= { #Arya Stark gets Needle
		if = {
			limit = {
				current_date >= 8298.3.1 #when Jon gifts it to Arya
				current_date < 8298.10.24 #when Arya becomes Arry
				has_game_rule = asoiaf_new_clothes_north_rule_on
			}
			asoiaf_create_artifact_needle_effect_historical = { OWNER = this }
		}
	}
	character:Stark_Dummy_9 ?= { #Arry "inherits" Needle
		if = {
			limit = {
				current_date >= 8298.10.24 #when Jon gifts it to Arya
				has_game_rule = asoiaf_new_clothes_north_rule_on
			}
			asoiaf_create_artifact_needle_effect_historical = { OWNER = this }
		}
	}
	character:Martell_8 ?= { #Oberyn Martell gets his spear
		if = {
			limit = {
				is_alive = yes
				age >= 16
				has_game_rule = asoiaf_new_clothes_dorne_rule_on
			}
			asoiaf_create_artifact_red_vipers_spear_effect_historical = { OWNER = this }
		}
	}
	character:Uller_6 ?= { #Ellaria takes Oberyn's spear after his death
		if = {
			limit = {
				character:Martell_8 ?= { is_alive = no }
				current_date >= 8300.1.15
				current_date < 8300.2.1
				has_game_rule = asoiaf_new_clothes_dorne_rule_on
			}
			asoiaf_create_artifact_red_vipers_spear_effect_historical = { OWNER = this }
		}
	}
	character:Martell_12 ?= { #Obara Sand gets Oberyn's spear from Ellaria
		if = {
			limit = {
				character:Martell_8 ?= { is_alive = no }
				current_date >= 8300.2.1
				has_game_rule = asoiaf_new_clothes_dorne_rule_on
			}
			asoiaf_create_artifact_red_vipers_spear_effect_historical = { OWNER = this }
		}
	}
	character:Targaryen_13_B ?= {
		set_house = house:house_Belerys #sets the wife of the Targ dynasty to a new House to prevent duplication in the dynasty tree window
	}
	character:Brightflame_7 ?= { #set mother of Aerion Brightflame's bastards, and fix religion/culture
		set_mother = character:Brightflame_dummy_mother
		set_culture = culture:lyseni
		#set_character_faith = faith:fc_pan_lys
		remove_claim = title:h_the_iron_throne
		remove_claim = title:e_the_crownlands
		remove_claim = title:k_the_blackwater
		remove_claim = title:d_kings_landing
		remove_claim = title:c_kings_landing
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		add_trait = bastard
		set_house = character:Brightflame_dummy_mother.house
		remove_trait = bastard_founder
	}
	character:Brightflame_2 ?= { #set mother of Aerion Brightflame's bastards, and fix religion/culture
		set_mother = character:Brightflame_dummy_mother
		set_culture = culture:lyseni
		#set_character_faith = faith:fc_pan_lys
		remove_claim = title:h_the_iron_throne
		remove_claim = title:e_the_crownlands
		remove_claim = title:k_the_blackwater
		remove_claim = title:d_kings_landing
		remove_claim = title:c_kings_landing
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		add_trait = bastard
		set_house = character:Brightflame_dummy_mother.house
		remove_trait = bastard_founder
	}
	character:Brightflame_8 ?= { #set mother of Aerion Brightflame's bastards, and fix religion/culture
		set_mother = character:Brightflame_dummy_mother
		set_culture = culture:lyseni
		#set_character_faith = faith:fc_pan_lys
		remove_claim = title:h_the_iron_throne
		remove_claim = title:e_the_crownlands
		remove_claim = title:k_the_blackwater
		remove_claim = title:d_kings_landing
		remove_claim = title:c_kings_landing
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		add_trait = bastard
		set_house = character:Brightflame_dummy_mother.house
		remove_trait = bastard_founder
	}
	character:Brightflame_6 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_1 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_11 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_9 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_10 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_13 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_14 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_15 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Brightflame_16 ?= { #remove claims from Aerion Brightflame's bastard descendants
		remove_claim = title:k_dragonstone
		remove_claim = title:d_dragonstone
		remove_claim = title:c_dragonstone
		remove_claim = title:c_summerfield
		set_house = character:Brightflame_dummy_mother.house
	}
	character:Targaryen_63 ?= { #Rhaenyra Targaryen (daughter of Viserys I)
		if = { #AGOT team didn't set her court language to Common Tongue
			limit = {
				is_landed = yes
				highest_held_title_tier >= tier_kingdom
			}
			set_court_language = language_agot_westeros
		}
	}
	if = {
		limit = { exists = character:Strong_30 }
		character:Targaryen_75 ?= { set_real_father = character:Strong_30 } #for some reason this is not already the case in the base game
		character:Targaryen_76 ?= { set_real_father = character:Strong_30 }
		character:Targaryen_77 ?= { set_real_father = character:Strong_30 }
	}
	character:Targaryen_3 ?= {
		add_martial_skill = 5
		#add_prowess_skill = -2 #otherwise he's higher even than Robert
		if = {
			limit = { 
				NOT = { has_perk = pedagogy_perk }
			}
			if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = open_minded_perk }
			}
			if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = apostate_perk }
			}
			if = { limit = { is_alive = yes } add_perk = apostate_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = scientific_perk }
			}
			if = { limit = { is_alive = yes } add_perk = scientific_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = planned_cultivation_perk }
			}
			if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = scholarly_circles_perk }
			}
			if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = learn_on_the_job_perk }
			}
			if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = sanctioned_loopholes_perk }
			}
			if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = scholar_perk }
			}
			if = { limit = { is_alive = yes } add_perk = scholar_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = stalwart_leader_perk }
				current_date >= 8278.1.1
			}
			if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = chivalric_dominance_perk }
				current_date >= 8278.1.1
			}
			if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = never_back_down_perk }
				current_date >= 8278.1.1
			}
			if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		}
		if = {
			limit = {
				current_date >= 8282.6.15 #one month before Rhaegar and lyanna run away together
				exists = character:Stark_5
			}
			set_relation_soulmate = character:Stark_5 #Lyanna; should be lover, but it glitches out and deletes when you unpause the game
		}
		if = { #AGOT team didn't set his court language to Common Tongue
			limit = {
				is_landed = yes
				highest_held_title_tier >= tier_kingdom
			}
			set_court_language = language_agot_westeros
		}
	}
	character:Targaryen_127 ?= { #Jaehaerys II Targaryen
		if = { #AGOT team didn't set his court language to Common Tongue
			limit = {
				is_landed = yes
				highest_held_title_tier >= tier_kingdom
			}
			set_court_language = language_agot_westeros
		}
	}
	character:Bolton_1 ?= { #Roose Bolton (son of Donnel)
		if = {
			limit = { 
				NOT = { has_perk = dark_insights_perk }
			}
			if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = divine_retribution_perk }
			}
			if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = dreadful_perk }
			}
			if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = thriving_in_chaos_perk }
			}
			if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = malice_implicit_perk }
			}
			if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = fear_tax_perk }
			}
			if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = forever_infamous_perk }
			}
			if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = prison_feudal_complex_perk }
			}
			if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
		}
	}
	character:Bolton_5 ?= { #Ramsay Bolton (bastard son of Roose)
		if = {
			limit = { 
				NOT = { has_perk = dark_insights_perk }
			}
			if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = divine_retribution_perk }
			}
			if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = dreadful_perk }
			}
			if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = thriving_in_chaos_perk }
			}
			if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = malice_implicit_perk }
			}
			if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = fear_tax_perk }
			}
			if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = forever_infamous_perk }
			}
			if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = prison_feudal_complex_perk }
			}
			if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
		}
		if = {
			limit = { 
				NOT = { has_perk = torturer_perk }
			}
			if = { limit = { is_alive = yes } add_perk = torturer_perk }
		}
	}
	character:asoiaf_Kat_1 ?= { #Kat's father in Dorne
		if = {
			limit = {
				is_alive = yes
				current_date >= 8062.1.1 #born, lived, and died in Dorne
			}
			title:b_bloodbridge.holder = { add_courtier = character:asoiaf_Kat_1 }
		}
	}
	character:asoiaf_Kat_1_rs ?= { #Kat's mother in Dorne
		if = {
			limit = {
				is_alive = yes
				current_date >= 8063.1.1 #born, lived, and died in Dorne
			}
			title:b_bloodbridge.holder = { add_courtier = character:asoiaf_Kat_1_rs }
		}
	}
	character:Hammer_2 ?= { #Hendry (father of Hugh)
		if = {
			limit = { is_alive = yes }
			title:b_hookmarket.holder = { add_courtier = character:Hammer_2 }
		}
	}
	character:Hammer_1 ?= { #Hugh the Hammer
		if = {
			limit = {
				is_alive = yes
				current_date >= 8095.1.1 #lived in King's Landing
				current_date < 8129.12.5
			}
			title:b_hookmarket.holder = { add_courtier = character:Hammer_1 }
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8129.12.5 #went to Dragonstone for the sowing
			}
			title:b_dragonstone.holder = { add_courtier = character:Hammer_1 }
		}
	}
	character:asoiaf_Kat_2 ?= { #Kat (wife of Hugh the Hamer)
		if = {
			limit = {
				is_alive = yes
				current_date >= 8097.1.1 #born where her father lives in Dorne
				current_date < 8118.1.1
			}
			title:b_bloodbridge.holder = { add_courtier = character:asoiaf_Kat_2 }
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8118.1.1 #she moves away to King's Landing
			}
			title:b_hookmarket.holder = { add_courtier = character:asoiaf_Kat_2 }
		}
	}
	character:asoiaf_Kat_3 ?= { #Kat's brother in Tumbleton
		if = {
			limit = {
				is_alive = yes
				current_date >= 8100.1.1 #born where his father lives in Dorne
				current_date < 8118.1.1
			}
			title:b_bloodbridge.holder = { add_courtier = character:asoiaf_Kat_3 }
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8118.1.1 #he moves away to Tumbleton
			}
			title:b_tumbleton.holder = { add_courtier = character:asoiaf_Kat_3 }
		}
	}
	character:asoiaf_Hammer_2 ?= { #Kat/Hugh's daughter
		if = {
			limit = {
				is_alive = yes
				current_date >= 8119.1.1 #born and raised in King's Landing
			}
			title:b_hookmarket.holder = { add_courtier = character:asoiaf_Hammer_2 }
		}
	}
	character:White_1 ?= { #Ulf the White
		if = {
			limit = {
				is_alive = yes
				current_date >= 8091.1.1 #lived in King's Landing
				current_date < 8129.12.5
			}
			title:b_hookmarket.holder = { add_courtier = character:White_1 }
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8129.12.5 #went to Dragonstone for the sowing
			}
			title:b_dragonstone.holder = { add_courtier = character:White_1 }
		}
	}
	character:asoiaf_Leipa_12 ?= { #Hanush of Leipa easter egg
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
	}
	character:asoiaf_Kobyla_1 ?= { #Radzig Kobyla easter egg
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
	}
	character:asoiaf_Kobyla_2 ?= { #Henry of Skalitz easter egg
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = roaming_perk }
		if = { limit = { is_alive = yes } add_perk = far_and_wide_perk }
		if = { limit = { is_alive = yes } add_perk = of_the_people_perk }
		if = { limit = { is_alive = yes } add_perk = local_hero_perk }
		if = {
			limit = { current_date >= 8299.1.1 } #his parents are dead, killed in the raid led by Istvan
			create_landless_adventurer_title_effect = {
				REASON = flag:voluntary
				FLAVOR_CHAR = character:asoiaf_Kobyla_2
			}
			add_gold = 50
			add_realm_law = camp_purpose_explorers
			add_courtier = character:asoiaf_Capon_2 #Hans always a follower
		}
		random_list = {
			10 = {
				asoiaf_sexuality_remove_flags_effect = yes
				add_character_flag = sexuality_ensure_bisexual
				set_sexuality = bisexual #Henry x Hans relationship handled in Hans' effect
			}
			90 = {
				asoiaf_sexuality_remove_flags_effect = yes
				add_character_flag = sexuality_ensure_heterosexual
				set_sexuality = heterosexual
			}
		}
		if = {
			limit = {
				current_date = 8299.1.1 #EXACTLY the day of the raid
			}
			create_artifact_asoiaf_radzig_sword_effect_historical = { OWNER = this CREATOR = character:asoiaf_Martin RECIPIENT = character:asoiaf_Kobyla_1 HENRY = character:asoiaf_Kobyla_2 } #given to Henry the same day of the raid
		}
		if = {
			limit = {
				current_date > 8299.1.1
				character:asoiaf_Toth_2 ?= { is_alive = no } #Istvan is dead
			}
			create_artifact_asoiaf_radzig_sword_effect_historical = { OWNER = this CREATOR = character:asoiaf_Martin RECIPIENT = character:asoiaf_Kobyla_1 HENRY = character:asoiaf_Kobyla_2 } #reclaimed by Henry
		}
	}
	character:asoiaf_Capon_2 ?= { #Hans Capon easter egg
		add_trait_xp = {
			trait = lifestyle_hunter
			track = hunter
			value = 100
		}
		add_trait_xp = {
			trait = lifestyle_reveler
			value = 50
		}
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 30
		}
		add_trait_xp = {
			trait = tourney_participant
			track = bow
			value = 100
		}
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		if = { limit = { is_alive = yes } add_perk = mortal_adoration_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = smooth_operator_perk }
		if = { limit = { is_alive = yes } add_perk = seducer_perk }
		random_list = {
			10 = {
				asoiaf_sexuality_remove_flags_effect = yes
				add_character_flag = sexuality_ensure_bisexual
				set_sexuality = bisexual
			}
			90 = {
				asoiaf_sexuality_remove_flags_effect = yes
				add_character_flag = sexuality_ensure_heterosexual
				set_sexuality = heterosexual
			}
		}
		if = { #if Henry and Hans both rolled bisexual, make them lovers
			limit = {
				AND = {
					current_date >= 8299.1.1 #when the friendship is set in the character history file
					has_sexuality = bisexual
					character:asoiaf_Kobyla_2 ?= { has_sexuality = bisexual }
				}
			}
			character:asoiaf_Kobyla_2 ?= { #Henry of Skalitz
				set_relation_lover = { reason = asoiaf_henry_hans_lover_desc target = character:asoiaf_Capon_2 }
			}
		}
	}
	character:asoiaf_Toth_2 ?= { #Istvan Toth easter egg
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = {
			limit = {
				current_date >= 8299.1.2 #stolen the day after the raid
				is_alive = yes
			}
			create_artifact_asoiaf_radzig_sword_effect_historical = { OWNER = this CREATOR = character:asoiaf_Martin RECIPIENT = character:asoiaf_Kobyla_1 HENRY = character:asoiaf_Kobyla_2 }
		}
	}
	character:asoiaf_Erik ?= { #Erik (lover of Istvan) easter egg
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk } #challenged Henry to single combat
		if = {
			limit = {
				character:asoiaf_Toth_2 ?= { is_alive = no } #Istvan is killed by Henry
			}
			character:asoiaf_Kobyla_2 ?= { #scope Henry of Skalitz's sword after he reclaimed it from Istvan
				every_character_artifact = {
					limit = { has_variable = asoiaf_is_radzig_sword }
					save_scope_as = this_artifact
				}
			}
			add_personal_artifact_claim = scope:this_artifact
		}
	}
	character:Stark_5 ?= {
		set_relation_friend = character:Stark_4 #Benjen was friend of Lyanna and helped her during Tourney of Harrenhal
	}
	character:Hightower_8 ?= { #Malora Hightower (the Mad Maid)
		add_trait = refusing_marriage #not exactly a maid if she marries
	}
	character:Slynt_1 ?= {
		if = {
			limit = {
				is_alive = yes
				current_date >= 8283.6.1 #Janos appointed Commander of the Gold Cloaks
				character:Deem_1 ?= { is_alive = yes }
			}
			set_relation_friend = character:Deem_1 #Allar Deem
		}
	}
	character:Stark_hh_2 ?= { #Hodor
		remove_nickname = yes
		if = {
			limit = {
				current_date >= 8269.1.1
			}
			change_first_name = Hodor
		}
	}
	#Ancestry fix for Daena Blackfyre and Saera Jayn
	#character:Blackfyre_16 ?= {
	#	change_first_name = Danza
		#set_father = character:asoiaf_Blackfyre_16_father #these are only set if Young Griff invades and the game tries to make him a Blackfyre
		#set_mother = character:asoiaf_Blackfyre_16_mother
		#set_house = character:asoiaf_Blackfyre_16_father.house
	#	set_culture = character:asoiaf_Blackfyre_16_father.culture
	#	set_character_faith = character:asoiaf_Blackfyre_16_father.faith
	#}
	character:Jayn_53 ?= {
		change_first_name = Alara
		set_culture = character:Jayn_52.culture
		set_character_faith = character:Jayn_52.faith
	}
	character:asoiaf_Melisandre_1 ?= {
		if = {
			limit = { has_game_rule = asoiaf_alternative_ages_rule_off } #Melisandre is NOT meant to be old with the game rule off
			death = { death_reason = death_vanished } #kill the historical version
		}
	}
	character:Melisandre_1 ?= {
		learn_language = language_agot_westeros
	}
	character:Florent_10 ?= { #Selyse Florent (daughter of Ryam)
		if = { #if Selyse and Stannis are married but he doesn't have the alliance with Florent House head yet for some reason
			limit = {
				is_alive = yes
				character:Baratheon_3 ?= { is_alive = yes } #Stannis
				is_spouse_of = character:Baratheon_3
			}
			character:Baratheon_3 = {
				create_alliance = { #alliance with the Florents via marriage
					target = dynasty:dynn_Florent.dynast #whomever is the head of House Florent
					allied_through_owner = character:Baratheon_3 #Stannis
					allied_through_target = character:Florent_10 #Selyse
				}
			}
		}
		if = {
			limit = {
				is_alive = yes
				age < 15
				character:Baratheon_3 ?= { #Stannis
					is_alive = yes
					age < 15
				}
				NOT = { is_spouse_of = character:Baratheon_3 }
			}
			create_betrothal = character:Baratheon_3 #Stannis married Selyse before Robert's Rebellion in AGOT+'s canon
		}
		if = {
			limit = {
				is_alive = yes
				age >= 15
				character:Baratheon_3 ?= { #Stannis
					is_alive = yes
					age >= 15
				}
				NOT = { is_spouse_of = character:Baratheon_3 }
			}
			marry = character:Baratheon_3 #Stannis married Selyse before Robert's Rebellion in AGOT+'s canon
			character:Baratheon_3 = {
				create_alliance = { #alliance with the Florents via marriage
					target = dynasty:dynn_Florent.dynast #whomever is the head of House Florent
					allied_through_owner = character:Baratheon_3 #Stannis
					allied_through_target = character:Florent_10 #Selyse
				}
			}
			if = {
				limit = {
					character:Florent_3 ?= { is_alive = yes } #Axell Florent (son of Imry)
					character:Baratheon_3 ?= { is_landed = yes } #Stannis is landed somewhere
				}
				character:Baratheon_3 = { add_courtier = character:Florent_3 }
			}
			if = {
				limit = {
					character:Florent_16 ?= { is_alive = yes } #Jon Flowers (bastard of Axell)
					character:Baratheon_3 ?= { is_landed = yes } #Stannis is landed somewhere
				}
				character:Baratheon_3 = { add_courtier = character:Florent_16 }
			}
		}
	}
	character:Targaryen_60 ?= { #Viserys I Targaryen
		if = {
			limit = {
				is_landed = yes
				current_date >= 8105.9.3 #the day Viserys names Rhaenyra his heir over Daemon
				exists = character:Targaryen_63 #Rhaenyra Targaryen (daughter of Viserys I)
			}
			set_designated_heir_unsafe = character:Targaryen_63 #NECESSARY for Canon Children trigger to work for Aegon/Aemond/Daeron, which checks if Viserys' heir is Rhaenyra
		}														#She MUST be the DESIGNATED heir otherwise the heir switches to the boys for a split second on their birth and fucks up the trigger
	}
	character:Baelish_2 ?= { #Petyr Baelish
		if = {
			limit = {
				has_trait = education_stewardship_4
			}
			remove_trait = education_stewardship_4
			add_trait = education_intrigue_4
			add_stewardship_skill = 8
		}
		if = {
			limit = {
				NOT = { has_perk = golden_obligations_perk }
			}
			if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = heregeld_perk }
			}
			if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = detailed_ledgers_perk }
			}
			if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = war_profiteer_perk }
			}
			if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = fearful_troops_perk }
			}
			if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = at_any_cost_perk }
			}
			if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = it_is_my_domain_perk }
			}
			if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = golden_aplomb_perk }
			}
			if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = avaricious_perk }
			}
			if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = truth_is_relative_perk }
			}
			if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = digging_for_dirt_perk }
			}
			if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = kidnapper_perk }
			}
			if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = court_of_shadows_perk }
			}
			if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = prepared_for_anything_perk }
			}
			if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = swift_execution_perk }
			}
			if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = a_job_done_right_perk }
			}
			if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = twice_schemed_perk }
			}
			if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = schemer_perk }
			}
			if = { limit = { is_alive = yes } add_perk = schemer_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = master_of_masses_perk }
			}
			if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = instructed_spies_perk }
			}
			if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = convincing_perk }
			}
			if = { limit = { is_alive = yes } add_perk = convincing_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = charismatic_perk }
			}
			if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = little_birds_perk }
			}
			if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = innocent_children_perk }
			}
			if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = omerta_perk }
			}
			if = { limit = { is_alive = yes } add_perk = omerta_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = sleeper_cells_perk }
			}
			if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = spider_perk }
			}
			if = { limit = { is_alive = yes } add_perk = spider_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = dreadful_perk }
			}
			if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = thriving_in_chaos_perk }
			}
			if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		}
	}
	character:Manderly_6 ?= { #Wyman Manderly (Lamprey Lord)
		if = {
			limit = {
				NOT = { has_trait = whole_of_body }
			}
			add_trait = whole_of_body
		}
	}
	character:Lannister_1 ?= { #Tywin Lannister (son of Tytos)
		add_intrigue_skill = 5
		add_trait = shrewd
		if = {
			limit = {
				NOT = { has_perk = firm_hand_perk }
			}
			if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = serve_the_crown_perk }
			}
			if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = strict_organization_perk }
			}
			if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = hard_rule_perk }
				current_date >= 8261.11.1 #puts down the Reyne-Tarbeck Rebellion
			}
			if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = dreadful_perk }
			}
			if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = meritocracy_perk }
			}
			if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = chains_of_loyalty_perk }
			}
			if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = likable_perk }
			}
			if = { limit = { is_alive = yes } add_perk = likable_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = positions_of_power_perk }
			}
			if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = large_levies_perk }
			}
			if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = soon_forgiven_perk }
			}
			if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = toe_the_line_perk }
			}
			if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = sleeper_cells_perk }
			}
			if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = administrator_perk }
			}
			if = { limit = { is_alive = yes } add_perk = administrator_perk }
		}
		if = { limit = { is_alive = yes } add_perk = mustering_the_troops_perk }
	}
	character:Lannister_8 ?= { #Tyrion Lannister (son of Tywin)
		add_martial_skill = -3 #so it's not too high
	}
	character:Targaryen_64 ?= { #Baelon Targaryen (son of Viserys I)
		give_nickname = nick_the_heir_for_a_day
		add_trait = sickly
	}
	character:Strickland_1 ?= { #Harry Strickland (Homeless)
		give_nickname = nick_homeless_prefix
	}
	character:Frey_1 ?= { #THE Walder Frey
		add_character_modifier = asoiaf_health_boost
	}
	character:Frey_52 ?= { #Fat Walda Frey (daughter of Merrett)
		give_nickname = nick_fat_prefix
	}
	character:Oakheart_24 ?= { #Joselyn Oakheart (wife of Robert Redwyne)
		add_character_modifier = asoiaf_health_boost
	}
	character:Most_Devout_20 ?= { #High Septon: The Fat One
		add_character_modifier = asoiaf_health_boost
	}
	character:Most_Devout_21 ?= { #High Septon (The One After the Fat One)
		add_character_modifier = asoiaf_health_boost
	}
	character:Ironmaker_1 ?= { #Erik Ironbreaker (Anvil-Breaker)
		add_character_modifier = asoiaf_health_boost
	}
	character:Crossing_1 ?= { #Walder Rivers (bastard son of Walder)
		give_nickname = nick_bastard_prefix
		if = {
			limit = {
				house ?= house:house_Crossing #if he has founded his cadet branch
			}
			remove_trait = bastard
			add_trait = bastard_founder
		}
	}
	character:Baratheon_14 ?= { #Orys Baratheon (bastard son of Aerion)
		if = {
			limit = {
				dynasty ?= dynasty:dynn_Baratheon #if he has founded his new dynasty
			}
			remove_trait = bastard
			add_trait = bastard_founder
		}
	}
	character:Targaryen_120 ?= { #Aemon Targaryen (son of Maekar I)
		add_character_modifier = asoiaf_health_boost
	}
	character:Harlaw_2 ?= { #Sigfryd Harlaw (Silverhair)
		add_character_modifier = asoiaf_health_boost
	}
	character:Redwyne_2 ?= { #Olenna Redwyne (the Queen of Thorns)
		add_character_modifier = asoiaf_health_boost
		if = {
			limit = {
				NOT = { has_perk = truth_is_relative_perk }
			}
			if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = digging_for_dirt_perk }
			}
			if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = kidnapper_perk }
			}
			if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = court_of_shadows_perk }
			}
			if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = prepared_for_anything_perk }
			}
			if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = swift_execution_perk }
			}
			if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = a_job_done_right_perk }
			}
			if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = twice_schemed_perk }
			}
			if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = schemer_perk }
			}
			if = { limit = { is_alive = yes } add_perk = schemer_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = like_weed_in_a_garden_perk }
			}
			if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = unshackled_lust_perk }
			}
			if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = subtle_desire_perk }
			}
			if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = enticing_opportunity_perk }
			}
			if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = home_advantage_perk }
			}
			if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		}

	}
	character:Pycelle_1 ?= {
		add_character_modifier = asoiaf_health_boost
	}
	character:Stark_hh_23 ?= { #Old Nan
		add_character_modifier = asoiaf_health_boost
	}
	character:Mormont_2 ?= { #Jeor Mormont (the Old Bear)
		add_character_modifier = asoiaf_health_boost
	}
	character:Hightower_5 ?= { #Leyton Hightower (the Old Man)
		add_trait = whole_of_body
		add_trait = diligent
		remove_trait = eccentric
		if = {
			limit = {
				current_date >= 8284.1.1
			}
			give_nickname = nick_the_old_man
		}
	}
	character:Tully_7 ?= { #Edmure Tully (son of Hoster)
		if = {
			limit = {
				has_trait = zealous
			}
			remove_trait = zealous
		}
	}
	character:Baratheon_3 ?= {
		if = {
			limit = {
				current_date >= 8283.6.1 #Robert inherits the throne
			}
			add_pressed_claim = title:h_the_iron_throne
		}
		if = {
			limit = {
				current_date >= 8283.1.1 #Davos saves Storm's End
				exists = character:Seaworth_1
			}
			set_relation_friend = character:Seaworth_1
		}
		if = {
			limit = {
				current_date >= 8300.1.1 #Melisande arrived some years prior, and by now has Stannis around her finger
				exists = character:Melisandre_1
			}
			set_relation_lover = character:Melisandre_1
		}
		if = {
			limit = {
				exists = character:Melisandre_1
				is_landed = yes
			}
			add_courtier = character:Melisandre_1
		}
		if = {
			limit = {
				exists = character:Baratheon_hh_3
				character:Baratheon_hh_3 = { is_alive = yes } #Maester Cressen
			}
			add_hook = {
				type = loyalty_hook
				target = character:Baratheon_hh_3
			}
		}
	}
	character:Baratheon_13 ?= { #Shireen Baratheon (daughter of Stannis)
		if = {
			limit = {
				character:Baratheon_3 ?= {
					house = house:house_BaratheonDS
				}
			}
			set_house = house:house_BaratheonDS
		}
	}
	dynasty:dynn_Florent.dynast ?= { #Remove this random claim from the Florents; they trace their lineage and are proud of it, but that's far different from a CLAIM
		if = {
			limit = {
				is_alive = yes
				has_claim_on = title:e_the_reach
				has_claim_on = title:k_the_mander
			}
			remove_claim = title:e_the_reach
			remove_claim = title:k_the_mander
		}
	}
	character:Melisandre_1 ?= {
		create_artifact_asoiaf_red_priest_necklace_effect = { OWNER = this }
	}
	character:Tyrell_14 ?= { #Margaery Tyrell (daughter of Mace)
		set_relation_friend = character:Redwyne_2 #Olenna
		if = {
			limit = { exists = character:Tyrell_13 }
			set_relation_friend = character:Tyrell_13 #Loras
		}
	}
	character:Tyrell_13 ?= {
		if = {
			limit = { exists = character:Baratheon_4 }
			set_relation_best_friend = character:Baratheon_4 #Renly
		}
	}
	character:Eunuch_1 ?= { #Varys
		add_intrigue_skill = 6
		if = {
			limit = {
				NOT = { has_perk = truth_is_relative_perk }
			}
			if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = digging_for_dirt_perk }
			}
			if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = kidnapper_perk }
			}
			if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = court_of_shadows_perk }
			}
			if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = prepared_for_anything_perk }
			}
			if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = swift_execution_perk }
			}
			if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = a_job_done_right_perk }
			}
			if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = twice_schemed_perk }
			}
			if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = schemer_perk }
			}
			if = { limit = { is_alive = yes } add_perk = schemer_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = master_of_masses_perk }
			}
			if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = instructed_spies_perk }
			}
			if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = convincing_perk }
			}
			if = { limit = { is_alive = yes } add_perk = convincing_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = charismatic_perk }
			}
			if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = little_birds_perk }
			}
			if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = innocent_children_perk }
			}
			if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = omerta_perk }
			}
			if = { limit = { is_alive = yes } add_perk = omerta_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = sleeper_cells_perk }
			}
			if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		}
		if = {
			limit = {
				NOT = { has_perk = spider_perk }
			}
			if = { limit = { is_alive = yes } add_perk = spider_perk }
		}
	}
	character:Baratheon_4 ?= { #Renly
		if = {
			limit = {
				current_date >= 8283.6.1 #Robert inherits the throne
			}
			add_pressed_claim = title:h_the_iron_throne
		}
	}
	character:Targaryen_65 ?= { #Aegon II Targaryen
		add_character_flag = { flag = no_drunkard_visual }
	}
	character:Clegane_4 ?= { #Sando Clegane (the Hound)
		add_character_flag = { flag = no_drunkard_visual }
	}
	character:Stokeworth_5 ?= { #Falyse Stokeworth (daughter of Tanda)
		if = {
			limit = {
				has_trait = vengeful
			}
			remove_trait = vengeful
		}
	}
	character:Lannister_135 ?= { #Orson Lannister (son of Daven)
		if = {
			limit = {
				has_trait = intellect_bad_2
			}
			remove_trait = intellect_bad_2 #was dropped on his head as a baby; was not born stupid
			add_trait = dull
		}
	}
	character:Brotherhood_8 ?= { #Thoros of Myr
		add_character_flag = { flag = no_drunkard_visual }
		learn_language = language_agot_westeros
	}
}

asoiaf_got_names_effect = {
	character:Dormand_10 ?= { #Artys Dormand
		change_first_name = Artys
	}
	character:Greyjoy_13 ?= { #Yara Greyjoy (daughter of Balon)
		change_first_name = Yara
	}
	character:Arryn_8 ?= { #Robin Arryn (son of Jon)
		change_first_name = Robin
	}
	character:Ashford_27 ?= { #Gwin Ashford (daughter of Alyn)
		change_first_name = Gwin
	}
	character:Florent_72 ?= { #Alyrie Florent (daughter of Armen)
		change_first_name = Alyrie
	}
	character:Fossoway_6 ?= { #Symun Fossoway (son of Bennis)
		change_first_name = Symun
	}
}

asoiaf_alternative_ages_cleanup_effect = {
	#clear childhood education focus
	if = {
		limit = {
			OR = {
				has_focus = education_diplomacy
				has_focus = education_martial
				has_focus = education_stewardship
				has_focus = education_learning
				has_focus = education_intrigue
			}
		}
		set_focus = no
	}
	#assign sexuality if blank
	if = {
		limit = {
			has_sexuality = none
		}
		set_sexuality = heterosexual
	}
	#clear all childhood traits
	asoiaf_clear_childhood_traits_effect = yes
	# Hostage reaches adulthood
	if = {
		limit = { is_hostage = yes }
		save_scope_as = hostage
		warden ?= {
			set_variable = {
				name = hook_chance
				value = scope:hostage.bp2_hostage_loyalty_hook_chance_value
			}
			trigger_event = {
				id = bp2_hostage_system.0200
				days = 5
			}
		}
	}
	#replace uncrowned child modifier with regular version
	if = {
		limit = {
			has_inactive_trait = not_had_coronation
			has_character_modifier = uncoronated_child_modifier
		}
		remove_character_modifier = uncoronated_child_modifier
		add_character_modifier = uncoronated_modifier
	}
	#remove stray guardians
	if = {
		limit = {
			any_relation = {
				type = guardian
				count > 0
			}
		}
		random_relation = {
			type = guardian
			save_scope_as = educator
		}
		wrap_up_education_effect = yes
	}
	#victim transforms into opinion modifier
	if = {
		limit = {
			any_relation = { type = victim count >= 1 }
		}
		random_relation = { type = victim save_scope_as = victim }
		if = {
			limit = { is_ai = no }
			set_variable = {
				name = had_childhood_victim_variable
				value = scope:victim
			}
		}
		add_opinion = {
			modifier = childhood_victim
			target = scope:victim
		}
		if = {
			limit = {
				NOT = { has_relation_potential_rival = scope:victim }
			}
			hidden_effect = {
				set_relation_potential_rival = scope:victim
			}
		}
		if = {
			limit = {
				any_relation = { type = victim count >= 1 }
			}
			remove_relation_victim = scope:victim
		}
	}
	#bully transforms into opinion modifier
	if = {
		limit = {
			any_relation = { type = bully count >= 1 }
		}
		random_relation = { type = bully save_scope_as = bully }
		if = {
			limit = { is_ai = no }
			set_variable = {
				name = had_childhood_bully_variable
				value = scope:bully
			}
		}
		add_opinion = {
			modifier = childhood_bully
			target = scope:bully
		}
		if = {
			limit = {
				NOT = { has_relation_potential_rival = scope:bully }
			}
			hidden_effect = {
				set_relation_potential_rival = scope:bully
			}
		}
		if = {
			limit = {
				any_relation = { type = bully count >= 1 }
			}
			remove_relation_bully = scope:bully
		}
	}
	#crush transforms into opinion modifier
	if = {
		limit = {
			OR = {
				any_relation = { type = crush count >= 1 }
				exists = var:unrequited_crush
			}
		}
		if = {
			limit = {
				exists = var:unrequited_crush
			}
			var:unrequited_crush = {
				save_scope_as = crushed_crush
			}
			scope:crushed_crush = {
				if = {
					limit = {
						is_ai = no
					}
					set_variable = {
						name = had_childhood_crush_variable
						value = root
					}
				}
			}
		}
		if = {
			limit = {
				any_relation = { type = crush count >= 1 }
			}
			random_relation = {
				type = crush
				save_scope_as = crush
			}
			if = {
				limit = { is_ai = no }
				set_variable = {
					name = had_childhood_crush_variable
					value = scope:crush
				}
			}
		}
		if = {
			limit = {
				exists = scope:crush
			}
			save_scope_as = root_crush
			scope:crush = {
				if = {
					limit = {
						has_relation_crush = root
					}
					add_character_flag = crush_over
					trigger_event = {
						id = childhood.0999
					}
				}
			}
		}
		if = {
			limit = {
				exists = var:unrequited_crush
			}
			save_scope_as = root_crush
			scope:crushed_crush = {
				if = {
					limit = {
						has_relation_crush = root
					}
					add_character_flag = crush_over
					add_character_flag = unrequited_crush
					trigger_event = {
						id = childhood.0999
					}
				}
			}
		}
		if = {
			limit = {
				exists = scope:crush
			}
			add_opinion = {
				modifier = childhood_crush
				target = scope:crush
			}
		}
		every_relation = {
			type = crush
			reverse_add_opinion = {
				modifier = childhood_crush
				target = root
			}
			root = {
				remove_relation_crush = prev
			}
		}
	}
}

asoiaf_alternative_ages_effect = {
	character:Most_Devout_20 ?= { #High Septon (The Fat One)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -14 #AGOT+ birthday: 244.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Most_Devout_21 ?= { #High Septon (The One After the Fat One)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -7 #AGOT+ birthday: 230.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Most_Devout_22 ?= { #High Sparrow
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 238.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Melisandre_1 ?= { #Melisandre
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 375 #7898.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Palehair_hh_1 ?= { #Dyanna (mother of Gaemon Palehair)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -12 #AGOT+ birthday: 80.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Palehair_hh_2 ?= { #Sylvenna Sand (lover of Dyanna)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -12 #AGOT+ birthday: 81.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tall_1 ?= { #Duncan the Tall
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Pycelle_1 ?= { #Pycelle
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -15 #AGOT+ birthday: 231.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hollard_10 ?= { #Dontos Hollard (the Red)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 265.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lowborn_Kings_Landing_1 ?= { #Shae (lover of Tyrion)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 10 #AGOT+ birthday: 270.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hotah_1_B ?= { #Areo Hotah
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -5 #AGOT+ birthday: 256.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Arryn_hh_1 ?= { #Marillion
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 7 #AGOT+ birthday: 273.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Santagar_6 ?= { #Aron Santagar (son of Sylva)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 264.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Uller_6 ?= { #Ellaria Sand
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 6 #AGOT+ birthday: 261.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Martell_4 ?= { #Doran Martell (son of Loreza)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -1 #AGOT+ birthday: 249.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Martell_11 ?= { #Trystane Martell (son of Doran)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 285.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Martell_12 ?= { #Obara Sand
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -3 #AGOT+ birthday: 275.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Martell_13 ?= { #Nymeria Sand
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -3 #AGOT+ birthday: 278.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Martell_14 ?= { #Tyene Sand
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -3 #AGOT+ birthday: 280.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_50 ?= { #Cassandra Baratheon (daughter of Borros)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 109.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_51 ?= { #Maris Baratheon (daughter of Borros)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 111.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_52 ?= { #Floris Baratheon (daughter of Borros)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 114.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_53 ?= { #Ellyn Baratheon (daughter of Borros)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 112.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_4 ?= { #Renly Baratheon (son of Steffon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 274.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_5 ?= { #Joffrey I Baratheon
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 285.5.5
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_6 ?= { #Myrcella Baratheon (daughter of Robert I)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 287.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baratheon_7 ?= { #Tommen I Baratheon
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 288.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_hh_1 ?= { #Maester Luwin
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 10 #AGOT+ birthday: 237.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_84 ?= { #Rickon Stark (father of Cregan)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 262.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_2 ?= { #Brandon Stark (son of Rickard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 258.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_3 ?= { #Eddard Stark (son of Rickard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 259.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_5 ?= { #Lyanna Stark (daughter of Rickard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 262.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_4 ?= { #Benjen Stark (son of Rickard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 264.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_8 ?= { #Sansa Stark (daughter of Eddard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 285.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_Dummy_8 ?= { #Sansa dummy
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 285.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_9 ?= { #Arya Stark (daughter of Eddard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 287.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_Dummy_9 ?= { #Arya dummy
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 287.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_10 ?= { #Brandon Stark (son of Eddard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 288.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_Dummy_10 ?= { #Brandon dummy
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 288.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_11 ?= { #Rickon Stark (son of Eddard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 292.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stark_Dummy_11 ?= { #Rickon dummy
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 292.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Poole_3 ?= { #Jeyne Poole (daughter of Vayon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 285.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Poole_Dummy_3 ?= { #Jeyne dummy
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 285.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Umber_11 ?= { #Jon Umber (Smalljon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 8 #AGOT+ birthday: 271.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tully_4 ?= { #Catelyn Tully (daughter of Hoster)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 263.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tully_5 ?= { #Lady Stoneheart (Catelyn dummy)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 263.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tully_6 ?= { #Lysa Tully (daughter of Hoster)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 265.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tully_7 ?= { #Edmure Tully (son of Hoster)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 6 #AGOT+ birthday: 267.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Strong_28 ?= { #Simon Strong (son of Bywin)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -10 #AGOT+ birthday: 64.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Strong_56 ?= { #Maynard Strong (son of Bywin)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -9 #AGOT+ birthday: 66.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Frey_52 ?= { #Walda Frey (Fat Walda)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 283.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Frey_82 ?= { #Roslin Frey (daughter of Walder)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 282.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Erenford_3 ?= { #Joyeause Erenford (daughter of Raymond)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -1 #AGOT+ birthday: 283.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Arryn_80 ?= { #Aemma Arryn (daughter of Rodrik)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 81.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Arryn_8 ?= { #Robin Arryn (son of Jon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 289.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Royce_1 ?= { #Yohn Royce (Bronze)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 242.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Egen_3 ?= { #Vardis Egen
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -8 #AGOT+ birthday: 259.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Moore_2 ?= { #Mandon Moore (son of Jon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 262.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Trant_2 ?= { #Meryn Trant (son of Donnel)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -5 #AGOT+ birthday: 258.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Baelish_2 ?= { #Petyr Baelish (Littlefinger)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 265.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hightower_89 ?= { #Hobert Hightower
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -1 #AGOT+ birthday: 68.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tarly_6 ?= { #Dickon Tarly (son of Randyll)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 287.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_1 ?= { #Tywin Lannister (son of Tytos)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 237.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_6 ?= { #Cersei Lannister (daughter of Tywin)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 261.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_7 ?= { #Jaime Lannister (son of Tywin)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 261.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_8 ?= { #Tyrion Lannister (son of Tywin)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 268.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_9 ?= { #Lancel Lannister (son of Kevan)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 280.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_10 ?= { #Willem Lannister (son of Kevan)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 284.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_11 ?= { #Martyn Lannister (son of Kevan)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 284.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Lannister_12 ?= { #Janei Lannister (daughter of Kevan)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 11 #AGOT+ birthday: 286.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Stokeworth_6 ?= { #Lollys Stokeworth (daughter of Tanda)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -6 #AGOT+ birthday: 271.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Ashford_27 ?= { #Gwin Ashford (daughter of Alyn)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 194.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tyrell_13 ?= { #Loras Tyrell (son of Mace)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 279.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Tyrell_14 ?= { #Margaery Tyrell (daughter of Mace)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 280.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hightower_97 ?= { #Alicent Hightower (daughter of Otto)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -3 #AGOT+ birthday: 85.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hightower_98 ?= { #Myles Hightower (son of Otto)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 92.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hightower_99 ?= { #Gwayne Hightower (son of Otto)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -6 #AGOT+ birthday: 99.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Beesbury_21 ?= { #Lyman Beesbury (father of Braxton)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -1 #AGOT+ birthday: 49.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Florent_10 ?= { #Selyse Florent (daughter of Ryam)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 263.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Celtigar_24 ?= { #Bartimos Celtigar (son of Harrold)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 3 #AGOT+ birthday: 82.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Velaryon_31 ?= { #Corlys Velaryon (the Sea Snake)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 54.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Targaryen_61 ?= { #Daemon Targaryen (son of Baelon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -3 #AGOT+ birthday: 84.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Targaryen_63 ?= { #Rhaenyra Targaryen (daughter of Viserys I)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 95.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Targaryen_68 ?= { #Daeron Targaryen (son of Viserys I)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 112.12.27; 16 a week or so (max) after the Battle of Rook's Rest
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Targaryen_1 ?= { #Aerys II Targaryen
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 241.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Targaryen_2 ?= { #Rhaella Targaryen (daughter of Jaehaerys II)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 243.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Reyne_63 ?= { #Martyn Reyne (friend of Aegon II)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 105.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Estermont_23 ?= { #Lundram Estermont (elder brother of Leon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 1 #AGOT+ birthday: 100.1.1; needs to be older than Leon, who had his age increased by 4
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Estermont_44 ?= { #Leon Estermont (friend of Aegon II)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 101.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Darklyn_18 ?= { #Steffon Darklyn (son of Harrold)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -3 #AGOT+ birthday: 78.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Cargyll_11 ?= { #Arryk Cargyll (Kingsguard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 82.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Cargyll_12 ?= { #Erryk Cargyll (Kingsguard)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -2 #AGOT+ birthday: 82.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Cole_11 ?= { #Criston Cole (the Kingmaker)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -6 #AGOT+ birthday: 88.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Thorne_17 ?= { #Rickard Thorne (son of Ellera)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 26 #AGOT+ birthday: 77.1.1
		set_mother = character:Thorne_64
		set_father = character:Appleton_70
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Hammer_1 ?= { #Hugh the Hammer
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 7 #AGOT+ birthday: 88.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:White_1 ?= { #Ulf the White
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 5 #AGOT+ birthday: 86.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Greyjoy_13 ?= { #Yara Greyjoy (daughter of Balon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 2 #AGOT+ birthday: 274.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Royce_93 ?= { #Gerold Royce (son of Gunthor)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -20 #AGOT+ birthday: 85.1.1
		set_father = character:Royce_36 #Gunthor Royce (son of Raymar)
		set_mother = character:Upcliff_22 #Arwen Upcliff (daughter of Estin)
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Strickland_1 ?= { #Harry Strickland (Homeless)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -15 #AGOT+ birthday: 262.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Marbrand_4 ?= { #Addam Marbrand (son of Damon)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 258.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Broome_35 ?= { #Alfred Broome (son of Morrec)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -14 #AGOT+ birthday: 81.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Rayder_1 ?= { #Mance Rayder (King Beyond the Wall)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = 4 #AGOT+ birthday: 252.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Giantsbane_1 ?= { #Tormund Giantsbane
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -14 #AGOT+ birthday: 266.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Giantsbane_2 ?= { #Toregg the Tall (son of Tormund)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -10 #AGOT+ birthday: 282.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Giantsbane_3 ?= { #Torwynd the Tame (son of Tormund)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -10 #AGOT+ birthday: 284.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Giantsbane_4 ?= { #Dormund (son of Tormund)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -10 #AGOT+ birthday: 287.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
	character:Giantsbane_5 ?= { #Munda (daughter of Tormund)
		if = {
			limit = { age < 16 }
			save_scope_as = asoiaf_underaged
		}
		change_age = -10 #AGOT+ birthday: 288.1.1
		if = {
			limit = {
				scope:asoiaf_underaged ?= this
				age >= 16
			}
			asoiaf_alternative_ages_cleanup_effect = yes
		}
	}
}

asoiaf_got_traits_effect = {
	character:Rayder_1 ?= { #Mance Rayder (King Beyond the Wall)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_5
		add_trait = intellect_good_2
		add_trait = authoritative
		add_trait = ambitious
		add_trait = brave
		add_trait = stubborn
		add_trait = strong
		add_trait = shrewd
		add_trait = lifestyle_poet
		add_trait = education_martial_prowess_4
		add_trait = august
		add_trait = diplomat
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = praetorian_guard_perk }
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = inspiring_rule_perk }
		if = { limit = { is_alive = yes } add_perk = true_ruler_perk }
		if = { limit = { is_alive = yes } add_perk = writing_history_perk }
		if = { limit = { is_alive = yes } add_perk = a_life_of_glory_perk }
		if = { limit = { is_alive = yes } add_perk = dignitas_perk }
		if = { limit = { is_alive = yes } add_perk = august_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = enduring_hardships_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = ducal_conquest_perk }
		if = { limit = { is_alive = yes } add_perk = forced_vassalage_perk }
		if = { limit = { is_alive = yes } add_perk = adaptive_traditions_perk }
		if = { limit = { is_alive = yes } add_perk = flexible_truces_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_negotiations_perk }
		if = { limit = { is_alive = yes } add_perk = embassies_perk }
		if = { limit = { is_alive = yes } add_perk = accomplished_forger_perk }
		if = { limit = { is_alive = yes } add_perk = diplomat_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				OR = {
					has_title = title:e_beyond_the_wall
					is_alive = no #he's been executed already
				}
			}
			add_trait = conqueror
		}
	}
	character:Giantsbane_1 ?= { #Tormund Giantsbane
		add_character_flag = no_drunkard_visual
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		remove_trait = drunkard
		add_trait = physique_good_2
		add_trait = education_martial_2
		add_trait = gregarious
		add_trait = brave
		add_trait = honest
		add_trait = lustful
		add_trait = lifestyle_reveler
		add_trait = berserker
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = roaming_perk }
		learn_language = language_agot_westeros
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Giantsbane_2 ?= { #Toregg the Tall (son of Tormund)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Giantsbane_3 ?= { #Torwynd the Tame (son of Tormund)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = education_martial_prowess_1
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Giantsbane_4 ?= { #Dormund (son of Tormund)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Giantsbane_5 ?= { #Munda (daughter of Tormund)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Lowborn_Wildling_1 ?= { #Ygritte
		learn_language = language_agot_westeros
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = bossy
		}
	}
	character:asoiaf_Melisandre_1 ?= {
		add_character_modifier = contraceptives_modifier #to guarantee infertility, in case the necklace is not enough
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_5
		add_trait = beauty_good_3
		add_trait = authoritative
		add_trait = zealous
		add_trait = callous
		add_trait = stubborn
		#add_trait = ambitious #removed for now to keep traits at 4
		add_trait = freedman
		add_trait = witch
		add_trait = immortal
		set_immortal_age = 38
		add_trait = lifestyle_mystic
		add_trait_xp = {
			trait = lifestyle_mystic
			value = 100
		}
		add_trait = red_priest
		#stats are pre-increased to AGOT+ levels in the history file
		add_trait = theologian
		if = { limit = { is_alive = yes } add_perk = faithful_perk }
		if = { limit = { is_alive = yes } add_perk = zealous_proselytizer_perk }
		if = { limit = { is_alive = yes } add_perk = religious_icon_perk }
		if = { limit = { is_alive = yes } add_perk = prophet_perk }
		if = { limit = { is_alive = yes } add_perk = clerical_justifications_perk }
		if = { limit = { is_alive = yes } add_perk = church_and_state_perk }
		if = { limit = { is_alive = yes } add_perk = radiant_perk }
		if = { limit = { is_alive = yes } add_perk = defender_of_the_faith_perk }
		if = { limit = { is_alive = yes } add_perk = theologian_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Melisandre_1 ?= {
		add_character_modifier = contraceptives_modifier #to guarantee infertility, in case the necklace is not enough
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_5
		add_trait = beauty_good_3
		add_trait = authoritative
		add_trait = zealous
		add_trait = callous
		add_trait = stubborn
		#add_trait = ambitious #removed for now to keep traits at 4
		add_trait = freedman
		add_trait = witch
		add_trait = immortal
		set_immortal_age = 38
		add_trait = lifestyle_mystic
		add_trait_xp = {
			trait = lifestyle_mystic
			value = 100
		}
		add_trait = red_priest
		add_diplomacy_skill = 10
		add_intrigue_skill = 5
		add_learning_skill = 10
		add_stewardship_skill = 10
		add_trait = theologian
		if = { limit = { is_alive = yes } add_perk = faithful_perk }
		if = { limit = { is_alive = yes } add_perk = zealous_proselytizer_perk }
		if = { limit = { is_alive = yes } add_perk = religious_icon_perk }
		if = { limit = { is_alive = yes } add_perk = prophet_perk }
		if = { limit = { is_alive = yes } add_perk = clerical_justifications_perk }
		if = { limit = { is_alive = yes } add_perk = church_and_state_perk }
		if = { limit = { is_alive = yes } add_perk = radiant_perk }
		if = { limit = { is_alive = yes } add_perk = defender_of_the_faith_perk }
		if = { limit = { is_alive = yes } add_perk = theologian_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Hightower_89 ?= { #Hobert Hightower
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = stubborn
		add_trait = ambitious
		add_trait = diligent
		add_trait = authoritative
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Hightower_90 ?= { #Otto Hightower (brother of Hobert)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_3
		remove_trait = scholar
		add_trait = intellect_good_2
		add_trait = diligent
		add_trait = ambitious
		add_trait = deceitful
		add_trait = patient
		add_trait = shrewd
		add_trait = education_martial_prowess_2
		add_trait = schemer
		add_trait = administrator
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		if = {
			limit = {
				is_alive = yes
				character:Florent_72 ?= { is_alive = yes } #Alyrie Florent (daughter of Armen)
				NOT = { is_spouse_of = character:Florent_72 }
			}
			create_betrothal = character:Florent_72
		}
		if = {
			limit = {
				current_date >= 8125.1.1 #his wife dies
			}
			add_trait = refusing_marriage
		}
	}
	character:Hightower_92 ?= { #Ormund Hightower (son of Hobert)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = wrathful
		add_trait = zealous
		add_trait = just
		add_trait = ambitious
		add_trait = beauty_good_2
		add_trait = shrewd
		add_trait = lifestyle_poet
		add_trait = fashionable
		add_trait = education_martial_prowess_3
		add_trait = organizer
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = wit
			value = 75
		}
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = faithful_perk }
		if = { limit = { is_alive = yes } add_perk = zealous_proselytizer_perk }
		if = { limit = { is_alive = yes } add_perk = clerical_justifications_perk }
		if = { limit = { is_alive = yes } add_perk = church_and_state_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Hightower_97 ?= { #Alicent Hightower (daughter of Otto)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_3
		add_stewardship_skill = 6
		add_diplomacy_skill = 5 #she was not incompetent in any of these areas
		add_intrigue_skill = 10
		add_trait = honest
		add_trait = diligent
		add_trait = authoritative
		add_trait = zealous #alt: just
		add_trait = shrewd
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
		if = {
			limit = {
				NOT = { exists = character:Targaryen_65 } #Aegon II Targaryen hasn't been born
			}
			set_relation_best_friend = { reason = asoiaf_alicent_rhaenyra_friend_desc target = character:Targaryen_63 } #childhood friend of Rhaenyra
		}
		if = {
			limit = {
				exists = character:Targaryen_65 #Aegon II Targaryen has been born
			}
			set_relation_nemesis = { reason = asoiaf_alicent_rhaenyra_rival_desc target = character:Targaryen_63 } #rivalry with Rhaenyra over succession
		}
		if = {
			limit = {
				character:Cole_11 ?= { is_alive = yes }
				current_date >= 8111.1.1 #day of the tournament where Alicent wears her green dress and Rhaenyra her black
			}
			character:Cole_11 ?= {
				add_secret = { #Criston's dalliance with Rhaenyra
					type = secret_lover
					target = character:Targaryen_63 #Rhaenyra
				}
				every_secret = { reveal_to = character:Hightower_97 }
			}
			set_relation_friend = { reason = asoiaf_alicent_criston_friend_desc target = character:Cole_11 } #friends with Criston Cole after he confesses his crime to her
		}
		if = {
			limit = {
				character:Cole_11 ?= { is_alive = yes }
				current_date >= 8129.4.8 #9 days after Viserys I dies in 8129.3.30
			}
			set_relation_lover = { reason = asoiaf_alicent_criston_lover_desc target = character:Cole_11 } #takes Criston as a lover now that Viserys is dead
		}
	}
	character:Hightower_99 ?= { #Gwayne Hightower (son of Otto)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = arrogant
		add_trait = honest
		add_trait = diligent
		add_trait = gregarious
		add_trait = education_martial_prowess_3
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Cole_11 ?= { #Criston Cole (the Kingmaker)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = education_martial_prowess_5
		remove_trait = gallant
		add_trait = just #would've been honour: core to his self-image, and his constant acting against it causes him great stress
		add_trait = brave
		add_trait = wrathful
		add_trait = diligent #ruthless
		add_trait = loyal
		add_trait = military_engineer
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 40
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 40
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				character:Targaryen_63 ?= { is_alive = yes } #Rhaenyra
				current_date >= 8111.1.1 #day of the tournament where Alicent wears her green dress and Rhaenyra her black
			}
			set_relation_grudge = { reason = asoiaf_criston_rhaenyra_grudge_desc target = character:Targaryen_63 }
		}
		if = {
			limit = {
				character:Targaryen_65 ?= { is_alive = yes } #Aegon II
				current_date >= 8111.1.1 #day of the tournament where Alicent wears her green dress and Rhaenyra her black
			}
			set_relation_friend = { reason = asoiaf_criston_aegon_friend_desc target = character:Targaryen_65 } #Cole takes on a father figure to Alicent's children
		}
		if = {
			limit = {
				character:Targaryen_66 ?= { is_alive = yes } #Helaena
				current_date >= 8111.1.1 #day of the tournament where Alicent wears her green dress and Rhaenyra her black
			}
			set_relation_friend = { reason = asoiaf_criston_helaena_friend_desc target = character:Targaryen_66 } #Cole takes on a father figure to Alicent's children
		}
		if = {
			limit = {
				character:Targaryen_67 ?= { is_alive = yes } #Aemond
				current_date >= 8111.1.1 #day of the tournament where Alicent wears her green dress and Rhaenyra her black
			}
			set_relation_friend = { reason = asoiaf_criston_aemond_friend_desc target = character:Targaryen_67 } #Cole takes on a father figure to Alicent's children
		}
		if = {
			limit = {
				current_date >= 8129.4.2 #the day Lyman Beesbury is murdered
				character:Beesbury_21 ?= { is_alive = no } #Lyman is dead
			}
			add_secret = {
				type = secret_murder
				target = character:Beesbury_21
			}
		}
	}
	character:Beesbury_21 ?= { #Lyman Beesbury (father of Braxton)
		add_character_modifier = asoiaf_health_boost
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_4
		add_trait = diligent
		add_trait = just
		add_trait = stubborn
		add_trait = honest
		add_trait = loyal
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Oakheart_4 ?= { #Arys Oakheart (son of Arwyn)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_martial_2
		add_trait = compassionate
		add_trait = humble
		add_trait = brave
		add_trait = trusting
		add_trait = education_martial_prowess_2
		add_trait = gallant
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = kingsguard_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = promising_prospects_perk }
		if = { limit = { is_alive = yes } add_perk = loyalty_and_respect_perk }
		if = { limit = { is_alive = yes } add_perk = peacemaker_perk }
		if = { limit = { is_alive = yes } add_perk = gallant_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Velaryon_31 ?= { #Corlys Velaryon (the Sea Snake)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_4
		add_trait = education_martial_prowess_3
		add_trait = ambitious
		add_trait = diligent
		add_trait = arrogant
		add_trait = brave
		add_trait = shrewd
		add_trait = avaricious
		add_trait = architect
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		if = { limit = { is_alive = yes } add_perk = tax_man_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_measures_perk }
		if = { limit = { is_alive = yes } add_perk = organized_muster_rolls_perk }
		if = { limit = { is_alive = yes } add_perk = cutting_corners_perk }
		if = { limit = { is_alive = yes } add_perk = professional_workforce_perk }
		if = { limit = { is_alive = yes } add_perk = centralization_perk }
		if = { limit = { is_alive = yes } add_perk = popular_figurehead_perk }
		if = { limit = { is_alive = yes } add_perk = divided_attention_perk }
		if = { limit = { is_alive = yes } add_perk = architect_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		#if = { #DISABLED because Paradox removed ability to designate anyone your heir; only works on children now
		#	limit = {
		#		character:Velaryon_35 ?= { is_alive = no }
		#		character:Targaryen_76 ?= { is_alive = yes } #Corlys delcares Lucerys heir after Laenor dies
		#	}
		#	set_designated_heir = character:Targaryen_76 #Lucerys Velaryon (son of Rhaenyra)
		#}
	}
	character:Velaryon_34 ?= { #Laena Velaryon (daughter of Corlys)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = intellect_good_1
		add_trait = compassionate
		add_trait = brave
		add_trait = stubborn
		add_trait = humble
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_bond
				value = 30
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = { is_alive = no }
			set_death_reason = {
				death_reason = death_dragon_fire
				killer = character:dragon_vhagar
			}
		}
	}
	character:Velaryon_35 ?= { #Laenor Velaryon (son of Corlys)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_3
		add_trait = education_martial_2
		add_trait = education_martial_prowess_3
		add_trait = compassionate
		add_trait = brave
		add_trait = gregarious
		add_trait = humble
		add_trait = fashionable
		add_trait = lifestyle_blademaster
		add_martial_skill = 5 #it's far too low by default
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_training
				value = 30
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			set_relation_crush = character:Lonmouth_16
		}
		if = {
			limit = {
				is_alive = yes
				character:Velaryon_34 ?= { is_alive = yes }
			}
			set_relation_best_friend = character:Velaryon_34 #Laena Velaryon (daughter of Corlys)
		}
		if = {
			limit = {
				is_alive = yes
				character:Lonmouth_16 ?= { is_alive = yes }
			}
			set_relation_friend = character:Lonmouth_16
		}
		if = {
			limit = {
				is_alive = yes
				character:Lonmouth_16 ?= { is_alive = no } #Joffrey was killed by Criston
			}
			set_relation_grudge = { reason = asoiaf_laenor_criston_grudge_desc target = character:Cole_11 } #Criston Cole
		}
	}
	character:Lonmouth_16 ?= { #Joffrey Lonmouth (lover of Laenor)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_martial_2
		add_trait = education_martial_prowess_3
		add_trait = gregarious
		add_trait = brave
		add_trait = diligent
		add_trait = trusting
		add_trait = loyal
		add_trait = shrewd
		add_trait = refusing_marriage
		add_martial_skill = 5 #it's far too low by default
		add_intrigue_skill = 10
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk } #VERY quickly found out about Rhaenyra/Criston's relationship
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			set_relation_crush = character:Velaryon_35
		}
		character:Velaryon_31 ?= {
			if = {
				limit = {
					is_alive = yes
					is_ruler = yes
					character:Lonmouth_16 ?= { is_alive = yes }
				}
			}
			add_courtier = character:Lonmouth_16 #Joffrey is Laenor's companion at court
		}
	}
	character:Correy_1 ?= { #Qarl Correy (lover of Laenor)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_3
		add_trait = education_martial_prowess_3
		add_trait = gregarious
		add_trait = brave
		add_trait = ambitious
		add_trait = greedy
		add_trait = fashionable
		add_trait = disloyal
		add_trait = lifestyle_traveler
		add_trait_xp = {
			trait = lifestyle_traveler
			track = danger
			value = 50
		}
		add_trait = refusing_marriage
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Lefford_5 ?= { #Alysanne Lefford (daughter of Leo)
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
	}
	character:Westerling_23 ?= { #Harrold Westerling (Lord Commander)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = education_martial_prowess_3
		add_trait = just
		add_trait = brave
		add_trait = stubborn
		add_trait = diligent
		add_trait = strong
		add_trait = gallant
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = kingsguard_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = promising_prospects_perk }
		if = { limit = { is_alive = yes } add_perk = loyalty_and_respect_perk }
		if = { limit = { is_alive = yes } add_perk = peacemaker_perk }
		if = { limit = { is_alive = yes } add_perk = gallant_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Marbrand_4 ?= { #Addam Marbrand (son of Damon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = education_martial_prowess_4
		add_trait = impatient
		add_trait = brave
		add_trait = diligent
		add_trait = gregarious
		add_trait = loyal
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Broome_35 ?= { #Alfred Broome (son of Morrec)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_2
		add_trait = education_martial_prowess_2
		add_trait = honest
		add_trait = arrogant
		add_trait = callous
		add_trait = rude
		add_trait = shrewd
		add_trait = disloyal
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		set_father = character:Broome_54 #Morrec broome (son of Briony)
		set_mother = character:Brax_67
	}
	character:Swyft_1 ?= { #Harys Swyft (son of Sheldon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = intellect_bad_1
		add_trait = education_stewardship_2
		add_trait = education_martial_prowess_1
		add_trait = calm
		add_trait = rude
		add_trait = temperate
		add_trait = content
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		add_diplomacy_skill = 5 #memes aside, we have no reason to believe he is braindead to the point of having 0 in every stat
		add_martial_skill = 5
		add_stewardship_skill = 4
		add_intrigue_skill = 5
		add_learning_skill = 5
	}
	character:Velaryon_43 ?= { #Addam Velaryon (bastard of Corlys)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = intellect_good_1
		add_trait = education_diplomacy_3
		add_trait = brave
		add_trait = diligent
		add_trait = just #honorable
		add_trait = stubborn
		add_trait = loyal
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		character:Ruin_Empress ?= { #sets correct parents
			trigger_event = { id = asoiaf_setup_events.0003 days = 1 }
		}
	}
	character:Velaryon_44 ?= { #Alyn Velaryon (bastard of Corlys)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_3
		add_trait = education_martial_4
		add_trait = education_martial_prowess_3
		add_trait = brave
		add_trait = diligent
		add_trait = arrogant
		add_trait = lustful
		add_trait = aggressive_attacker
		add_trait = desert_warrior
		add_trait = strategist
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = parthian_tactics_perk }
		if = { limit = { is_alive = yes } add_perk = envelopment_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = engineered_for_destruction_perk }
		if = { limit = { is_alive = yes } add_perk = living_off_the_land_perk }
		if = { limit = { is_alive = yes } add_perk = sappers_perk }
		if = { limit = { is_alive = yes } add_perk = strategist_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		character:Ruin_Empress ?= { #sets correct parents
			trigger_event = { id = asoiaf_setup_events.0003 days = 1 }
		}
	}
	character:Arryn_80 ?= { #Aemma Arryn (daughter of Rodrik)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_2
		add_trait = physique_bad_1
		add_trait = diligent
		add_trait = compassionate
		add_trait = generous
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_27 ?= { #Aegon I Targaryen
		add_trait = lifestyle_surveyor
		if = { limit = { is_alive = yes } add_perk = mustering_the_troops_perk }
		if = { limit = { is_alive = yes } add_perk = local_inspection_perk }
		if = { limit = { is_alive = yes } add_perk = know_your_land_know_your_people_perk }
		if = { limit = { is_alive = yes } add_perk = no_stone_unturned_perk }
		if = { limit = { is_alive = yes } add_perk = travel_logs_perk }
		if = { limit = { is_alive = yes } add_perk = local_arbitration_perk }
		if = { limit = { is_alive = yes } add_perk = realm_charts_perk }
		if = { limit = { is_alive = yes } add_perk = personal_touch_perk }
		if = { limit = { is_alive = yes } add_perk = surveyor_perk }
		if = {
			limit = {
				NOT = { has_trait = dragon_dreams }
			}
			add_trait = dragon_dreams #Aegon's dream about the white walkers
		}
		if = {
			limit = {
				OR = {
					has_title = title:h_the_iron_throne #he's begun his conquest
					is_alive = no #or he's dead
				}
			}
			add_trait = conqueror
		}
	}
	character:Targaryen_35 ?= { #Jaehaerys I Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_stewardship_5
		add_trait = intellect_good_2
		add_trait = fecund
		add_trait = diligent
		add_trait = just
		add_trait = stubborn
		add_trait = temperate
		add_trait = shrewd
		add_trait = education_martial_prowess_4
		add_trait = diplomat
		add_trait = architect
		add_trait = lifestyle_surveyor
		if = { limit = { is_alive = yes } add_perk = tax_man_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_measures_perk }
		if = { limit = { is_alive = yes } add_perk = organized_muster_rolls_perk }
		if = { limit = { is_alive = yes } add_perk = cutting_corners_perk }
		if = { limit = { is_alive = yes } add_perk = professional_workforce_perk }
		if = { limit = { is_alive = yes } add_perk = centralization_perk }
		if = { limit = { is_alive = yes } add_perk = popular_figurehead_perk }
		if = { limit = { is_alive = yes } add_perk = divided_attention_perk }
		if = { limit = { is_alive = yes } add_perk = architect_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = ducal_conquest_perk }
		if = { limit = { is_alive = yes } add_perk = forced_vassalage_perk }
		if = { limit = { is_alive = yes } add_perk = adaptive_traditions_perk }
		if = { limit = { is_alive = yes } add_perk = flexible_truces_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_negotiations_perk }
		if = { limit = { is_alive = yes } add_perk = embassies_perk }
		if = { limit = { is_alive = yes } add_perk = accomplished_forger_perk }
		if = { limit = { is_alive = yes } add_perk = diplomat_perk }
		if = { limit = { is_alive = yes } add_perk = mustering_the_troops_perk }
		if = { limit = { is_alive = yes } add_perk = local_inspection_perk }
		if = { limit = { is_alive = yes } add_perk = know_your_land_know_your_people_perk }
		if = { limit = { is_alive = yes } add_perk = no_stone_unturned_perk }
		if = { limit = { is_alive = yes } add_perk = travel_logs_perk }
		if = { limit = { is_alive = yes } add_perk = local_arbitration_perk }
		if = { limit = { is_alive = yes } add_perk = realm_charts_perk }
		if = { limit = { is_alive = yes } add_perk = personal_touch_perk }
		if = { limit = { is_alive = yes } add_perk = surveyor_perk }

		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
			add_trait = bossy
		}
	}
	character:Targaryen_45 ?= { #Aemon Targaryen (son of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_stewardship_3
		add_trait = compassionate
		add_trait = patient
		add_trait = temperate #honorable
		add_trait = just
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Targaryen_46 ?= { #Baelon Targaryen (son of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_stewardship_3
		add_trait = brave
		add_trait = gregarious
		add_trait = just #honorable
		add_trait = diligent
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Targaryen_47 ?= { #Alyssa Targaryen (daughter of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = diligent
		add_trait = gregarious
		add_trait = brave
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_48 ?= { #Maegelle Targaryen (daughter of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = intellect_good_1
		add_trait = compassionate
		add_trait = patient
		add_trait = just #honorable
		add_trait = zealous
		add_trait = lifestyle_physician
		add_trait = diplomat
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = ducal_conquest_perk }
		if = { limit = { is_alive = yes } add_perk = forced_vassalage_perk }
		if = { limit = { is_alive = yes } add_perk = adaptive_traditions_perk }
		if = { limit = { is_alive = yes } add_perk = flexible_truces_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_negotiations_perk }
		if = { limit = { is_alive = yes } add_perk = embassies_perk }
		if = { limit = { is_alive = yes } add_perk = accomplished_forger_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		add_character_modifier = agot_dragon_disinterest
	}
	character:Targaryen_49 ?= { #Vaegon Targaryen (son of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = patient
		add_trait = rude
		add_trait = chaste
		add_trait = content
		add_trait = lifestyle_mystic
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
		add_character_modifier = agot_dragon_disinterest
	}
	character:Targaryen_50 ?= { #Daella Targaryen (daughter of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_bad_2
		add_trait = intellect_bad_1
		add_trait = fickle
		add_trait = zealous
		add_trait = shy
		add_trait = trusting
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_51 ?= { #Saera Targaryen (daughter of Jaehaerys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = intellect_good_1
		add_trait = stubborn
		add_trait = arrogant
		add_trait = deceitful
		add_trait = lustful
		add_trait = seducer
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		if = { limit = { is_alive = yes } add_perk = mortal_adoration_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = smooth_operator_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		if = {
			limit = {
				any_child = { has_trait = bastard }
			}
			add_trait = fornicator
		}
	}
	character:Targaryen_59 ?= { #Rhaenys Targaryen (daughter of Aemon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = brave
		add_trait = arrogant
		add_trait = temperate
		add_trait = stubborn
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_training
				value = 30
			}
			add_trait_xp = {
				trait = dragonrider
				track = dragon_bond
				value = 65
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_60 ?= { #Viserys I Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		remove_trait = lifestyle_reveler
		remove_trait = improvident
		add_trait = inquisitive
		add_trait = stubborn
		add_trait = forgiving
		add_trait = content
		add_trait = lifestyle_hunter
		add_trait = dragon_dreams
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				has_trait = ill
			}
			remove_trait = ill
		}
		if = {
			limit = {
				has_trait = gout_ridden
			}
			remove_trait = gout_ridden
		}
		if = {
			limit = {
				AND = {
					is_alive = yes
					is_spouse_of = character:Arryn_80 #Aemma Arryn (daughter of Rodrik)
					character:Arryn_80 ?= { is_alive = yes }
				}
			}
			set_relation_soulmate = character:Arryn_80
		}
		if = {
			limit = {
				current_date >= 8126.3.3 #face becomes horribly disfigured 3 years before his death in 129 AC (estimation)
				exists = global_var:Lotd_enabled #Does the player have Legacy of the Dragon mod enabled?
				is_alive = yes #character flags can't be given to dead people, so his mask will just be assigned manually for when he's dead
			}
			add_trait = disfigured
			add_character_flag = wears_valyrian_mask_01 #from Legacy of the Dragon
		}
		if = {
			limit = {
				current_date >= 8105.9.3 #Baelon Targaryen (son of Viserys I) dies, and the same day the Maesters discover the rot
			}
			add_trait = cancer
			add_trait = depressed_1 #Aemma died the prior day on 8105.9.2
			add_character_modifier = stress_frozen_grief
			if = {
				limit = {
					NOT = { has_trait = disfigured } #if he's not at the final stage of his sickness
				}
				add_character_modifier = {
					modifier = asoiaf_health_boost #then give him a survivability boost so he doesn't insta-die
					years = 10
				}
			}
			if = { #after Baelon's death, their relationship became strained to the point of breakage
				limit = { has_relation_friend = character:Targaryen_61 }
				remove_relation_friend = character:Targaryen_61
			}
		}
	}
	character:Targaryen_61 ?= { #Daemon Targaryen (son of Baelon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		remove_trait = open_terrain_expert
		remove_trait = organizer
		add_trait = brave
		add_trait = wrathful
		add_trait = ambitious
		add_trait = callous #ruthless
		add_trait = lifestyle_blademaster
		add_trait = aggressive_attacker
		add_trait = reckless
		add_trait = shrewd
		add_trait = education_martial_prowess_3
		add_trait = tourney_participant
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 35
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 40
		}
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_bond
				value = 100
			}
			add_trait_xp = {
				trait = dragonrider
				track = dragon_training
				value = 65
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Targaryen_70_B ?= { #Aerion Targaryen (son of Daemon)
		change_first_name = Aerion
		remove_trait = spindly
		remove_trait = hunchbacked
		if = {
			limit = { is_alive = no }
			set_death_reason = { death_reason = death_dragon_fire }
		}
	}
	character:Targaryen_63 ?= { #Rhaenyra Targaryen (daughter of Viserys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		remove_trait = comfort_eater
		add_trait = beauty_good_2
		if = { #childhood traits
			limit = { current_date < 8114.11.29 }
			add_trait = brave
			add_trait = arrogant
			add_trait = rude
			add_trait = authoritative
		}
		if = { #adulthood traits
			limit = { current_date >= 8114.11.29 } #Jacaerys is born and Rhaenyra becomes a mother, changing her personality
			add_trait = stubborn
			add_trait = arbitrary
			add_trait = ambitious
			add_trait = patient
		}
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_bond
				value = 65
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = { has_claim_on = title:c_godsroost }
			remove_claim = title:c_godsroost
		}
	}
	character:Targaryen_65 ?= { #Aegon II Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_2
		add_trait = beauty_good_2
		add_trait = arbitrary
		add_trait = gregarious
		add_trait = impatient
		add_trait = lustful
		add_trait = drunkard
		add_trait = rakish
		add_trait = lifestyle_reveler
		add_trait = reckless
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_bond
				value = 65
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		if = {
			limit = { has_relation_friend = character:Targaryen_67 } #Aemond
			remove_relation_friend = character:Targaryen_67 #Aemond
		}
		if = {
			limit = { has_relation_friend = character:Targaryen_68 } #Daeron
			remove_relation_friend = character:Targaryen_68 #Daeron
		}
		if = {
			limit = {
				OR = { #Battle of Rook's Rest has happened
					current_date >= 8129.11.21
					has_trait = crippled
					has_trait = wounded_3
				}
			}
			set_relation_grudge = { reason = asoiaf_aegon_aemond_grudge_desc target = character:Targaryen_67 } #Aemond
		}
	}
	character:Reyne_63 ?= { #Martyn Reyne
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = gregarious
		add_trait = lustful
		add_trait = content
		add_trait = impatient
		add_trait = beauty_good_1
		add_trait = education_martial_prowess_2
		set_father = character:Reyne_22
		set_mother = character:LannisterLP_29
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8122.1.1
			}
			set_relation_friend = character:Targaryen_65 #Aegon II Targaryen
			set_relation_friend = character:Estermont_44 #Leon Estermont
			set_relation_friend = character:Knight_Kings_Landing_16 #Eddard Waters
			character:Targaryen_60 ?= { add_courtier = character:Reyne_63 } #Martyn joins Viserys I's court and makes friends
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8129.3.3
			}
			add_character_modifier = training_for_kingsguard #so the player can more easily make him a KG
			character:Targaryen_65 ?= { add_courtier = character:Reyne_63 } #to make sure Martyn is at Aegon's court
		}
	}
	character:Estermont_44 ?= { #Leon Estermont
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_1
		add_trait = gregarious
		add_trait = lustful
		add_trait = arrogant
		add_trait = impatient
		add_trait = education_martial_prowess_1
		set_father = character:Estermont_41 #Eldon Estermont
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8122.1.1
			}
			set_relation_friend = character:Targaryen_65 #Aegon II Targaryen
			set_relation_friend = character:Reyne_63 #Martyn Reyne
			set_relation_friend = character:Knight_Kings_Landing_16 #Eddard Waters
			character:Targaryen_60 ?= { add_courtier = character:Estermont_44 } #Leon joins Viserys I's court and makes friends
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8129.3.3
			}
			add_character_modifier = training_for_kingsguard #so the player can more easily make him a KG
			character:Targaryen_65 ?= { add_courtier = character:Estermont_44 } #to make sure Leon is at Aegon II's court
		}
	}
	character:Thorne_17 ?= { #Rickard Thorne (son of Ellera)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = honest
		add_trait = authoritative
		add_trait = brave
		add_trait = diligent
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				NOT = { has_trait = kingsguard } #so he's not automatically married off due to being a lot older in AGOT+
			}
			add_trait = refusing_marriage
			add_character_modifier = training_for_kingsguard
		}
	}
	character:Staunton_19 ?= { #Simon Staunton
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = stubborn
		add_trait = brave
		add_trait = diligent
		add_trait = just
		add_trait = education_martial_prowess_2
		if = { limit = { is_alive = yes } add_perk = mustering_the_troops_perk }
		if = { limit = { is_alive = yes } add_perk = local_inspection_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = enduring_hardships_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		add_martial_skill = 5 #to lead the defence himself... he must not be SO terrible
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Darklyn_18 ?= { #Steffon Darklyn (son of Harrold)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = education_martial_3
		add_trait = diligent
		add_trait = brave
		add_trait = compassionate
		add_trait = humble
		add_trait = loyal
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Knight_Kings_Landing_16 ?= { #Eddard Waters
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = ambitious
		add_trait = gregarious
		add_trait = diligent
		add_trait = education_martial_prowess_3
		set_culture = culture:crownlander
		give_nickname = nick_red_prefix
		set_mother = character:asoiaf_Eddard_Waters_mother #Summer Islander mother
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8122.1.1
			}
			set_relation_friend = character:Targaryen_65 #Aegon II Targaryen
			set_relation_friend = character:Reyne_63 #Martyn Reyne
			set_relation_friend = character:Estermont_44 #Leon Estermont
			character:Targaryen_60 ?= { add_courtier = character:Knight_Kings_Landing_16 } #Eddard joins Viserys I's court and makes friends
		}
		if = {
			limit = {
				is_alive = yes
				current_date >= 8129.3.3
			}
			add_character_modifier = training_for_kingsguard #so the player can more easily make him a KG
			character:Targaryen_65 ?= { add_courtier = character:Knight_Kings_Landing_16 } #to make sure Eddard is at Aegon II's court
		}
	}
	character:Truefyre_1 ?= { #Trystane Truefyre
		character:Ruin_Empress ?= { #sets correct parents
			trigger_event = { id = asoiaf_setup_events.0003 days = 1 } #absolutely no way that Viserys is his real father, assign dummy Lyseni instead
		}
	}
	character:Targaryen_66 ?= { #Helaena Targaryen (daughter of Viserys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_learning_2
		add_trait = eccentric
		add_trait = shy
		add_trait = compassionate
		add_trait = content
		add_trait = dragon_dreams
		add_trait = reclusive
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
		add_prowess_skill = -10 #base 0
	}
	character:Targaryen_67 ?= { #Aemond Targaryen (son of Viserys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = brave
		add_trait = ambitious
		add_trait = diligent
		add_trait = vengeful
		add_trait = lifestyle_blademaster
		add_trait = education_martial_prowess_4
		add_trait = aggressive_attacker
		add_trait = strong
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = {
			limit = { has_trait = dragonrider }
			asoiaf_clear_dragonrider_xp_effect = yes
			add_trait_xp = {
				trait = dragonrider
				track = dragon_training
				value = 30
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			#if = { #can't have both a bully relation and a grudge relation, so I'm going with grudge as it's more meaningful
			#	limit = {
			#		character:Targaryen_65 ?= { age < 16 }
			#	}
			#	set_relation_bully = character:Targaryen_65 #Aegon II Targaryen
			#}
		}
		if = {
			limit = {
				character:Targaryen_65 ?= { is_alive = yes } #Aegon II Targaryen
			}
			set_relation_grudge = { reason = asoiaf_aemond_aegon_grudge_desc target = character:Targaryen_65 }
		}
		if = {
			limit = {
				character:Targaryen_76 ?= { #Lucerys Velaryon (son of Rhaenyra)
					is_alive = yes
					age < 16
				}
				age < 16
				NOT = { has_trait = one_eyed } #the grudge over losing his eye hasn't formed yet, so for now Lucerys is just his bully
			}
			set_relation_bully = character:Targaryen_76
		}
		if = {
			limit = {
				character:Targaryen_76 ?= { is_alive = yes } #Lucerys Velaryon (son of Rhaenyra)
				has_trait = one_eyed #the childhood fight has ocurred
			}
			set_relation_grudge = { reason = asoiaf_aemond_lucerys_grudge_desc target = character:Targaryen_76 }
			if = {
				limit = { has_relation_rival = character:Targaryen_76 }
				remove_relation_rival = character:Targaryen_76 #it was a grudge, not a reciprocated rivalry
			}
		}
		if = {
			limit = {
				character:Targaryen_76 ?= { is_alive = yes } #Lucerys
				has_relation_rival = character:Targaryen_63 #if Aemond is Rhaenyra's rival before killing Lucerys
			}
			remove_relation_rival = character:Targaryen_63 #remove the rivaly; this just didn't exist
		}
	}
	character:Targaryen_68 ?= { #Daeron Targaryen (son of Viserys I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = intellect_good_1
		add_trait = education_martial_2
		add_trait = brave
		add_trait = humble
		add_trait = gregarious
		add_trait = compassionate
		add_trait = lifestyle_poet #good with his lute
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Targaryen_69 ?= { #Baela Targaryen (daughter of Daemon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = brave
		add_trait = authoritative
		add_trait = impatient
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				character:Targaryen_70 ?= { is_alive = yes }
			}
			set_relation_friend = character:Targaryen_70 #friends with Rhaena
		}
	}
	character:Targaryen_70 ?= { #Rhaena Targaryen (daughter of Daemon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = compassionate
		add_trait = patient
		add_trait = generous
		add_trait = forgiving
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_71 ?= { #Jaehaera Targaryen (daughter of Aegon II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = content
		add_trait = shy
		add_trait = eccentric
		add_trait = depressed_genetic
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Targaryen_72 ?= { #Jaehaerys Targaryen (son of Aegon II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = the_mark
		add_trait = impatient #it's not much to go on from S2E1, but it's better than no personality all all
		add_trait = content
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Targaryen_74 ?= { #Alleged son of Aemond Targaryen (son of Viserys I)
		add_trait = disputed_heritage
	}
	character:Targaryen_75 ?= { #Jacaerys Velaryon (son of Rhaenyra)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		if = {
			limit = {
				has_trait = strong
			}
			remove_trait = strong
		}
		add_trait = beauty_good_3
		add_trait = intellect_good_1
		add_trait = diligent
		add_trait = just #honorable
		add_trait = brave
		add_trait = gregarious
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				character:Targaryen_76 ?= { is_alive = yes }
			}
			set_relation_friend = character:Targaryen_76 #friends with Luke
		}
	}
	character:Targaryen_76 ?= { #Lucerys Velaryon (son of Rhaenyra)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		if = {
			limit = {
				has_trait = strong
			}
			remove_trait = strong
		}
		add_trait = beauty_good_1
		add_trait = diligent
		add_trait = trusting
		add_trait = compassionate
		add_trait = humble
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Targaryen_77 ?= { #Joffrey Velaryon (son of Rhaenyra)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		if = {
			limit = {
				has_trait = strong
			}
			remove_trait = strong
		}
		add_trait = brave
		add_trait = impatient
		add_trait = ambitious
		add_trait = stubborn
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Targaryen_78 ?= { #Aegon III Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = chaste
		add_trait = shy
		add_trait = temperate
		add_trait = calm #mentions of dragons sent him into a grief-striken rage, but we can assume being rageful isn't part of his personality and is instead a stress response to trauma
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_79 ?= { #Viserys II Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = intellect_good_3
		add_trait = diligent
		add_trait = patient
		add_trait = gregarious
		add_trait = authoritative
		add_trait = education_martial_prowess_3
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Targaryen_82 ?= { #Baelor I Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_4
		remove_trait = lunatic_1
		add_trait = lunatic_genetic
		add_trait = zealous
		add_trait = chaste
		add_trait = humble
		add_trait = temperate
		add_trait = improvident
		add_trait = lunatic_genetic
		add_trait = inappetetic
		add_trait = celibate
		add_trait = paragon
		add_trait = theologian
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = faithful_perk }
		if = { limit = { is_alive = yes } add_perk = zealous_proselytizer_perk }
		if = { limit = { is_alive = yes } add_perk = religious_icon_perk }
		if = { limit = { is_alive = yes } add_perk = prophet_perk }
		if = { limit = { is_alive = yes } add_perk = clerical_justifications_perk }
		if = { limit = { is_alive = yes } add_perk = church_and_state_perk }
		if = { limit = { is_alive = yes } add_perk = radiant_perk }
		if = { limit = { is_alive = yes } add_perk = defender_of_the_faith_perk }
		if = { limit = { is_alive = yes } add_perk = theologian_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Targaryen_83 ?= { #Daena Targaryen (daughter of Aegon III)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_1
		add_trait = beauty_good_3
		add_trait = brave
		add_trait = stubborn
		add_trait = ambitious
		add_trait = authoritative
		add_trait = strong
		add_trait = athletic
		add_trait = lifestyle_hunter
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Targaryen_84 ?= { #Rhaena Targaryen (daughter of Aegon III)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_3
		add_trait = beauty_good_2
		add_trait = zealous
		add_trait = humble
		add_trait = content
		add_trait = temperate
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Targaryen_85 ?= { #Elaena Targaryen (daughter of Aegon III)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_5
		add_trait = beauty_good_1
		add_trait = intellect_good_1
		add_trait = stubborn
		add_trait = diligent
		add_trait = arrogant
		add_trait = just
		add_trait = shrewd
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Targaryen_89 ?= { #Aemon Targaryen (son of Viserys II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = beauty_good_2
		add_trait = compassionate
		add_trait = just #honorable
		add_trait = chaste
		add_trait = brave
		add_trait = strong
		add_trait = loyal
		add_trait = education_martial_prowess_5
		add_trait = lifestyle_blademaster
		add_trait_xp = {
			trait = lifestyle_blademaster
			value = 100
		}
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 30
		}
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = kingsguard_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = promising_prospects_perk }
		if = { limit = { is_alive = yes } add_perk = loyalty_and_respect_perk }
		if = { limit = { is_alive = yes } add_perk = peacemaker_perk }
		if = { limit = { is_alive = yes } add_perk = gallant_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				NOT = { has_trait = kingsguard }
			}
			add_character_modifier = training_for_kingsguard
		}
	}
	character:Targaryen_96 ?= { #Brynden Rivers (Bloodraven)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		remove_trait = scarred
		add_trait = intellect_good_2
		add_trait = albino
		add_trait = patient
		add_trait = callous
		add_trait = just
		add_trait = deceitful
		add_trait = shrewd
		add_trait = loyal
		add_trait = lifestyle_mystic
		add_trait = greensight
		add_trait = education_martial_prowess_3
		add_trait = spider
		if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		if = { limit = { is_alive = yes } add_perk = convincing_perk }
		if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		if = { limit = { is_alive = yes } add_perk = omerta_perk }
		if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		if = { limit = { is_alive = yes } add_perk = spider_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Targaryen_107 ?= { #Baelor Targaryen (son of Daeron II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = heterochromia_eyes
		make_trait_inactive = heterochromia_brown3
		add_trait = education_martial_3
		add_trait = generous
		add_trait = compassionate
		add_trait = just
		add_trait = brave
		add_trait = aggressive_attacker
		add_trait = education_martial_prowess_4
		add_trait = lifestyle_blademaster
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
	}
	character:Targaryen_108 ?= { #Aerys I Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = physique_bad_2
		add_trait = education_learning_3
		add_trait = content
		add_trait = inquisitive
		add_trait = shy
		add_trait = forgiving
		add_trait = reclusive
		add_trait = celibate
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
			add_trait = pensive
		}
	}
	character:Targaryen_109 ?= { #Rhaegel Targaryen (son of Daeron II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = physique_bad_1
		add_trait = lunatic_genetic
		add_trait = education_diplomacy_1
		add_trait = gregarious
		add_trait = eccentric
		add_trait = content
		add_trait = fickle
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = curious
		}
	}
	character:Targaryen_110 ?= { #Maekar I Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_3
		add_trait = impatient
		add_trait = brave
		add_trait = diligent
		add_trait = callous
		add_trait = irritable
		add_trait = strong
		add_trait = unyielding_defender
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
		give_nickname = nick_the_anvil
	}
	character:Targaryen_111 ?= { #Valarr Targaryen (son of Baelor)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = heterochromia_eyes
		make_trait_inactive = heterochromia_brown3
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = temperate
		add_trait = brave
		add_trait = education_martial_prowess_3
		add_trait = lifestyle_blademaster
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
	}
	character:Targaryen_115 ?= { #Aelor Targaryen (son of Rhaegel)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = trusting
		add_trait = calm
		add_trait = forgiving
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = pensive
		}
	}
	character:Targaryen_116 ?= { #Aelora Targaryen (daughter of Rhaegel)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_1
		add_trait = impatient
		add_trait = compassionate
		add_trait = authoritative
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
		if = {
			limit = {
				character:Targaryen_115 ?= { is_alive = yes }
			}
			set_relation_best_friend = character:Targaryen_115 #Aelor Targaryen (son of Rhaegel)
		}
	}
	character:Targaryen_117 ?= { #Daenora Targaryen (daughter of Rhaegel)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = content
		add_trait = calm
		add_trait = temperate
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
			add_trait = pensive
		}
	}
	character:Targaryen_118 ?= { #Daeron Targaryen (son of Maekar I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_stewardship_1
		add_trait = lustful
		add_trait = compassionate
		add_trait = fickle
		add_trait = drunkard
		add_trait = dragon_dreams
		add_trait = education_martial_prowess_1
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		add_character_flag = { flag = no_drunkard_visual }
	}
	character:Targaryen_119 ?= { #Aerion Targaryen (son of Maekar I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		remove_trait = lunatic_1
		remove_trait = rakish
		remove_trait = disinherited
		add_trait = education_martial_2
		add_trait = beauty_good_3
		add_trait = lunatic_genetic
		add_trait = arrogant
		add_trait = sadistic
		add_trait = deceitful
		add_trait = arbitrary
		add_trait = education_martial_prowess_3
		add_trait = lifestyle_mystic
		add_trait = fashionable
		add_trait = dragon_dreams
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		add_trait_xp = {
			trait = tourney_participant
			track = wit
			value = 30
		}
		add_trait = torturer
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
		if = { limit = { is_alive = yes } add_perk = torturer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = charming
			add_trait = rowdy
		}
		add_secret = { type = secret_witch }
	}
	character:Targaryen_120 ?= { #Aemon Targaryen (son of Maekar I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_learning_5
		add_trait = intellect_good_2
		add_trait = diligent
		add_trait = temperate
		add_trait = compassionate
		add_trait = content
		add_trait = dragon_dreams
		add_trait = lifestyle_physician
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Targaryen_121 ?= { #Daella Targaryen (daughter of Maekar I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = compassionate
		add_trait = fickle
		add_trait = patient
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_122 ?= { #Aegon V Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = intellect_good_2
		add_trait = education_martial_2
		add_trait = compassionate
		add_trait = diligent
		add_trait = stubborn
		add_trait = just
		add_trait = dragon_dreams
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Targaryen_123 ?= { #Rhae Targaryen (daughter of Maekar I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = gregarious
		add_trait = compassionate
		add_trait = brave
		add_trait = diligent
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_124 ?= { #Vaella Targaryen (daughter of Duncan)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = intellect_bad_3
		add_trait = compassionate
		add_trait = content
		add_trait = trusting
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = pensive
		}
	}
	character:Targaryen_125 ?= { #Maegor Targaryen (son of Aerion)
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		if = {
			limit = { age < 16 }
			add_trait = bossy
		}
	}
	character:Targaryen_126 ?= { #Duncan Targaryen (son of Aegon V)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_martial_2
		add_trait = compassionate
		add_trait = generous
		add_trait = stubborn
		add_trait = brave
		add_trait = education_martial_prowess_2
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = charming
		}
	}
	character:Targaryen_127 ?= { #Jaehaerys II Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = intellect_good_1
		add_trait = physique_bad_3
		add_trait = temperate
		add_trait = diligent
		add_trait = just
		add_trait = brave
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
			add_trait = pensive
		}
	}
	character:Targaryen_128 ?= { #Shaera Targaryen (daughter of Aegon V)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_1
		add_trait = compassionate
		add_trait = stubborn
		add_trait = trusting
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = charming
		}
	}
	character:Targaryen_129 ?= { #Daeron Targaryen (son of Aegon V)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_3
		add_trait = beauty_good_2
		add_trait = just
		add_trait = brave
		add_trait = compassionate
		add_trait = stubborn
		add_trait = education_martial_prowess_3
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 30
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = charming
		}
	}
	character:Targaryen_130 ?= { #Rhaelle Targaryen (daughter of Aegon V)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_stewardship_2
		add_trait = beauty_good_2
		add_trait = arrogant
		add_trait = diligent
		add_trait = impatient
		add_trait = honest
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
			add_trait = bossy
		}
	}
	character:Targaryen_1 ?= { #Aerys II Targaryen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		#asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_intrigue_2
		add_trait = beauty_good_1
		if = {
			limit = { current_date < 8277.10.1 } #pre-madness traits
			add_trait = ambitious
			add_trait = arrogant
			add_trait = generous
			add_trait = fickle
		}
		if = { #personality change due imprisonment in Duskendale
			limit = { current_date >= 8277.10.1 }
			add_trait = arbitrary
			add_trait = wrathful
			add_trait = paranoid
			add_trait = sadistic
			add_trait = lunatic_1
			add_trait = fire_obsessed
			add_trait = uncouth
			add_trait = irritable
			add_trait = inappetetic
			add_trait = reclusive
			add_trait = torturer
			if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
			if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
			if = { limit = { is_alive = yes } add_perk = dreadful_perk }
			if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
			if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
			if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
			if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
			if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
			if = { limit = { is_alive = yes } add_perk = torturer_perk }
			add_secret = { type = secret_deviant } #aroused by burning people alive
		}
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = charming
		}
	}
	character:Targaryen_2 ?= { #Rhaella Targaryen (daughter of Jaehaerys II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = fecund
		add_trait = beauty_good_2
		add_trait = humble
		add_trait = shy
		add_trait = just
		add_trait = diligent
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_3 ?= { #Rhaegar Targaryen (son of Aerys II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_3
		add_trait = intellect_good_2
		add_trait = diligent
		add_trait = calm
		add_trait = brave
		add_trait = compassionate
		add_trait = lifestyle_blademaster
		add_trait = depressed_1
		add_trait = lifestyle_poet
		add_trait = education_martial_prowess_3
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 70
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 50
		}
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = promising_prospects_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Targaryen_8 ?= { #Viserys Targaryen (son of Aerys II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_1
		if = {
			limit = { has_trait = lunatic_1 }
			remove_trait = lunatic_1
		}
		add_trait = stubborn #childhood traits
		add_trait = impatient
		add_trait = arrogant
		add_trait = reckless
		if = {
			limit = {
				age < 18 #otherwise the traits will already be assigned
				character:Targaryen_1 ?= { is_alive = no } #Aerys II has been killed
			}
			set_global_variable = asoiaf_Targaryen_8_pending_traits_change_variable
		}
		if = { #personality change due to stresses of living on the run
			limit = { age >= 18 } #adult traits
			asoiaf_clear_personality_traits_effect = yes
			add_trait = arrogant
			add_trait = impatient
			add_trait = wrathful
			add_trait = ambitious
			add_trait = irritable
			asoiaf_clear_education_traits_effect = yes
			add_trait = education_martial_1
			add_trait = education_martial_prowess_1
			create_character_memory = { type = asoiaf_Targaryen_8_traits_change_memory }
		}
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		remove_claim = title:c_griffins_roost
		remove_claim = title:c_highwind
		remove_claim = title:c_griffin_river
		remove_claim = title:c_summerfield
		add_pressed_claim = title:k_dragonstone
		add_pressed_claim = title:d_dragonstone
		add_pressed_claim = title:c_dragonstone
		if = {
			limit = { current_date = 8284.6.2 } #Crowned Stag start date
			character:Targaryen_8.house = { set_house_head = character:Targaryen_8 } #set Viserys as House head instead of Aemon
		}
	}
	character:Targaryen_9 ?= { #Daenerys Targaryen (daughter of Aerys II)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_4
		add_trait = beauty_good_3
		add_trait = intellect_good_1
		add_trait = dragon_dreams
		add_trait = shy #childhood traits
		add_trait = temperate
		add_trait = trusting
		if = { #personality change due to marriage to Khal Drogo
			limit = { current_date >= 8297.12.21 } #adulthood traits; 297AC (has been married off)
			asoiaf_clear_personality_traits_effect = yes
			add_trait = authoritative
			add_trait = ambitious
			add_trait = just
			add_trait = wrathful
			set_global_variable = asoiaf_Targaryen_9_traits_change_variable #for proper hairstyle and to avoid the event firing again
		}
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = praetorian_guard_perk }
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = inspiring_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = ducal_conquest_perk }
		if = { limit = { is_alive = yes } add_perk = forced_vassalage_perk }
		if = { limit = { is_alive = yes } add_perk = adaptive_traditions_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		remove_claim = title:c_griffins_roost
		remove_claim = title:c_highwind
		remove_claim = title:c_griffin_river
		remove_claim = title:c_summerfield
		add_pressed_claim = title:k_dragonstone
		add_pressed_claim = title:d_dragonstone
		add_pressed_claim = title:c_dragonstone
	}
	character:Targaryen_11 ?= { #Aegon Targaryen (son of Rhaegar)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = intellect_good_1
		add_trait = diligent
		add_trait = authoritative
		add_trait = brave
		add_trait = ambitious
		add_trait = education_martial_prowess_3
		add_trait = aggressive_attacker
		add_trait = lifestyle_poet
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = parthian_tactics_perk }
		if = { limit = { is_alive = yes } add_perk = envelopment_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = engineered_for_destruction_perk }
		if = { limit = { is_alive = yes } add_perk = living_off_the_land_perk }
		if = { limit = { is_alive = yes } add_perk = sappers_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = inspiring_rule_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		learn_language = language_agot_rhoyne
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Hammer_2 ?= { #Hendry (father of Hugh)
		if = {
			limit = { exists = character:Hammer_1 }
			add_trait = fornicator
		}
	}
	character:Hammer_1 ?= { #Hugh the Hammer
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_good_2
		add_trait = education_martial_2
		add_trait = brave
		add_trait = trusting
		add_trait = wrathful
		add_trait = strong
		add_trait = education_martial_prowess_3
		if = {
			limit = { has_trait = dragonrider }
			add_trait = ambitious
		}
		if = { limit = { is_alive = yes } add_perk = cutting_corners_perk }
		if = { limit = { is_alive = yes } add_perk = professional_workforce_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		set_culture = culture:crownlander
		set_real_mother = character:Targaryen_51 #Saera Targaryen (daughter of Jaehaerys I); only tentatively set for now (no definitive evidence it actually WAS Saera yet)
		set_mother = character:Targaryen_51
		if = {
			limit = { is_lowborn = yes }
			add_trait = bastard
		}
		if = {
			limit = { is_lowborn = no }
			remove_trait = bastard
			add_trait = bastard_founder
		}
	}
	character:White_1 ?= { #Ulf the White
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_character_flag = { flag = no_drunkard_visual }
		add_trait = education_martial_1
		add_trait = impatient
		add_trait = ambitious
		add_trait = gregarious
		add_trait = lazy
		add_trait = education_martial_prowess_1
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		set_culture = culture:crownlander
		#set_real_father = character:Targaryen_46 #Baelon Targaryen (son of Jaehaerys I); disabled since there's little chance that Baelon would betray Alyssa (will enable if it's proven in some clear way)
	}
	character:Palehair_hh_1 = { #Dyanna (Gaemon Palehair's mother)
		change_first_name = Dyanna #Essie -> Dyanna
		asoiaf_clear_personality_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = compassionate
		add_trait = just
		add_trait = stubborn
		add_trait = trusting
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				NOT = { exists = character:Palehair_1 } #she hasn't been impregnated by Aegon yet
				age > 16 #she's old enough to have a job in the castle
			}
			title:c_kings_landing.holder = { add_courtier = character:Palehair_hh_1 } #Dyanna was a servant in the castle
		}
		if = {
			limit = {
				current_date >= 8126.1.1 #Dyanna impregnated by Aegon; Dyanna impregnated on 8125.3.1, but the history file doesn't make her pregnant, so to avoid having a grudge without the subsequent child, it's set on his birthday
			}
		}
		set_relation_grudge = { reason = asoiaf_dyanna_aegon_grudge_desc target = character:Targaryen_65 }
	}
	character:Celtigar_24 ?= { #Bartimos Celtigar (son of Harrold)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_2
		add_trait = honest
		add_trait = just
		add_trait = greedy
		add_trait = diligent
		add_trait = education_martial_prowess_1
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		add_trait = avaricious
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		add_prowess_skill = -10
		add_stewardship_skill = 5
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Tall_1 ?= { #Duncan the Tall
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = compassionate
		add_trait = brave
		add_trait = just
		add_trait = trusting
		add_trait = strong
		add_trait = beauty_good_1
		add_trait = physique_good_2
		add_trait = education_martial_prowess_2
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = roaming_perk }
		if = { limit = { is_alive = yes } add_perk = far_and_wide_perk }
		if = { limit = { is_alive = yes } add_perk = of_the_people_perk }
		if = { limit = { is_alive = yes } add_perk = just_one_more_hill_perk }
		if = { limit = { is_alive = yes } add_perk = travel_companion_perk }
		give_nickname = nick_the_tall
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = { current_date >= 8209.2.1 } #seems to be the only date of the tourney at Ashford
			set_relation_friend = { reason = friend_bonded_at_tourney target = character:Fossoway_New_Barrel_1 } #Raymun Fossoway (founder of the Line)
		}
	}
	character:Velaryon_3 ?= { #Monterys Velaryon (son of Monford)
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
	}
	character:Velaryon_4 ?= { #Jacaerys Velaryon (son of Monford)
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
	}
	character:Dayne_8 ?= { #Gerold Dayne (Darkstar)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_3
		add_trait = brave
		add_trait = callous
		add_trait = ambitious
		add_trait = deceitful
		add_trait = education_martial_prowess_4
		add_trait = aggressive_attacker
		add_trait = lifestyle_blademaster
		add_trait_xp = {
			trait = lifestyle_blademaster
			value = 100
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Tully_1 ?= { #Hoster Tully (son of Edmyn)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = patient
		add_trait = stubborn
		add_trait = diligent
		add_trait = gregarious
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				is_alive = yes
				current_date < 8263.1.1 #the date Hoster and Minisa marry in the history file
				exists = character:Whent_6
				NOT = { is_spouse_of = character:Whent_6 } #Minisa Whent (daughter of Walter)
			}
			create_betrothal = character:Whent_6
			character:Tully_67 ?= { #Edmyn Tully (son of Steffon)
				create_alliance = { #by betrothal
					target = character:Whent_1 #Osmund Whent (son of Lister)
					allied_through_owner = character:Tully_1
					allied_through_target = character:Whent_6
				}
			}
		}
	}
	character:Tully_9 ?= { #Brynden Tully (son of Edmyn)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = brave
		add_trait = diligent
		add_trait = stubborn
		add_trait = cynical
		add_trait = organizer
		add_trait = unyielding_defender
		add_trait = refusing_marriage
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Tully_4 ?= { #Catelyn Tully (daughter of Hoster)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = brave
		add_trait = diligent
		add_trait = honest
		add_trait = stubborn
		add_trait = reckless
		add_intrigue_skill = 5 #she's not THAT bad at intrigue
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Tully_5 ?= { #Lady Stoneheart
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = vengeful
		add_trait = callous
		#add_trait = ruthless
		add_trait = arbitrary
		add_trait = mute
		add_trait = scarred
	}
	character:Tully_6 ?= { #Lysa Tully (daughter of Hoster)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_1
		add_trait = deceitful
		add_trait = trusting
		add_trait = wrathful
		add_trait = arrogant
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Tully_7 ?= { #Edmure Tully (son of Hoster)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = impatient
		add_trait = compassionate
		add_trait = diligent
		add_trait = cautious_leader
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Arryn_8 ?= { #Robin Arryn (son of Jon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_bad_2
		add_trait = beauty_good_1
		add_trait = arbitrary
		add_trait = impatient
		add_trait = irritable
	}
	character:Blackwood_102 ?= { #Willem Blackwood (son of Edmund) -> Willem Blackwood (son of Edwyn)
		set_father = character:Blackwood_57 #Edwyn Blackwood (son of Royce)	
		asoiaf_clear_personality_traits_effect = yes
		add_trait = just
		add_trait = callous #ruthless
		add_trait = honest
		add_trait = brave
		add_trait = loyal
		add_trait = knight
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		character:Blackwood_60 ?= {
			if = {
				limit = {
					is_alive = yes
					is_landed = yes
				}
				add_courtier = character:Blackwood_102
				designate_diarch = character:Blackwood_102 #set Willem as regent-presumptive so when he dies he'll be so
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Bracken_57 ?= { #Jerrel Bracken (son of Humfrey)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_1
		add_trait = arrogant
		add_trait = stubborn
		add_trait = rude
		add_trait = authoritative
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Frey_50 ?= { #Amerei Frey (Gatehouse Ami)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_1
		add_trait = beauty_good_2
		add_trait = lustful
		add_trait = fickle
		add_trait = greedy
		add_trait = gregarious
		add_trait = lifestyle_reveler
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Frey_52 ?= { #Walda Frey (Fat Walda)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = gluttonous
		add_trait = compassionate
		add_trait = content
		add_trait = trusting
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Frey_82 ?= { #Roslin Frey (daughter of Walder)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_2
		add_trait = shy
		add_trait = compassionate
		add_trait = humble
		add_trait = content
		add_trait = lifestyle_poet #talent for music
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Targaryen_93 ?= { #Second Viserys (son of Aegon IV) -> Aemond, for variation
		change_first_name = Aemond
	}
	character:Slynt_1 ?= { #Janos Slynt
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_2
		add_trait = ambitious
		add_trait = greedy
		add_trait = deceitful
		add_trait = wrathful
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Deem_1 ?= { #Allar Deem
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = callous
		add_trait = diligent
		add_trait = arbitrary
		add_trait = loyal
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		add_martial_skill = 5
		add_intrigue_skill = 5
	}
	character:Hollard_10 ?= { #Dontos Hollard (the Red)
		add_character_flag = { flag = no_drunkard_visual }
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_intrigue_2
		add_trait = gluttonous
		add_trait = trusting
		add_trait = greedy
		add_trait = compassionate
		add_trait = drunkard
		add_trait = education_martial_prowess_1
		add_trait = lifestyle_reveler
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Lowborn_Kings_Landing_1 ?= { #Shae (lover of Tyrion)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_1
		add_trait = stubborn
		add_trait = vengeful
		add_trait = brave
		add_trait = cynical
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Blackwater_1 ?= { #Bronn of the Blackwater
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_2
		add_trait = beauty_good_1
		add_trait = greedy
		add_trait = brave
		add_trait = ambitious
		add_trait = cynical
		add_trait = education_martial_prowess_3
		add_trait = shrewd
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = roaming_perk }
		if = { limit = { is_alive = yes } add_perk = just_one_more_hill_perk }
		if = { limit = { is_alive = yes } add_perk = travel_companion_perk }
		if = { limit = { is_alive = yes } add_perk = mercenary_contacts_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = enduring_hardships_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = soldiers_of_lesser_fortune_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
	}
	character:Drowned_Men_4 ?= { #Lodos
		add_trait = drowned_man #for some reason he doesn't have it already
	}
	character:Lannister_90 ?= { #Jason Lannister (twin of Tyland)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = gregarious
		add_trait = lustful
		add_trait = diligent
		add_trait = education_martial_prowess_3
		add_trait = education_republican_knowledge_2
		add_trait = lifestyle_hunter
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Lannister_91 ?= { #Tyland Lannister (twin of Jason)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = diligent
		add_trait = patient
		add_trait = forgiving
		add_trait = callous #ruthless
		add_trait = shrewd
		add_trait = loyal
		add_trait = education_republican_knowledge_4
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Lannister_129 ?= { #Tytos Lannister (son of Gerold)
		if = {
			limit = { has_claim_on = title:c_trembley } #he GAVE it to someone else, hence he does not get a claim
			remove_claim = title:c_trembley
		}
		if = {
			limit = { has_claim_on = title:c_fang_hill } #he GAVE it to someone else, hence he does not get a claim
			remove_claim = title:c_fang_hill
		}
		if = {
			limit = { has_strong_claim_on = title:c_coldmoat } #should be weak claim
			make_claim_weak = title:c_coldmoat
		}
		if = {
			limit = { has_strong_claim_on = title:c_derring_downs } #should be weak claim
			make_claim_weak = title:c_derring_downs
		}
		if = {
			limit = { has_strong_claim_on = title:c_brandybottom } #should be weak claim
			make_claim_weak = title:c_brandybottom
		}
		if = {
			limit = { has_strong_claim_on = title:c_leafy_lake } #should be weak claim
			make_claim_weak = title:c_leafy_lake
		}
	}
	character:Lannister_1 ?= { #Tywin Lannister (son of Tytos)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_5
		add_trait = intellect_good_2
		add_trait = diligent
		add_trait = ambitious
		add_trait = authoritative
		add_trait = callous
		add_trait = shrewd
		add_trait = organizer
		add_trait = education_martial_prowess_2
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = parthian_tactics_perk }
		if = { limit = { is_alive = yes } add_perk = envelopment_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = praetorian_guard_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		add_martial_skill = 5 #"the best (martial) mind, some would say"
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		if = {
			limit = { has_claim_on = title:c_trembley } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_trembley
		}
		if = {
			limit = { has_claim_on = title:c_fang_hill } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_fang_hill
		}
		if = {
			limit = { has_strong_claim_on = title:c_coldmoat } #should be weak claim
			make_claim_weak = title:c_coldmoat
		}
		if = {
			limit = { has_strong_claim_on = title:c_derring_downs } #should be weak claim
			make_claim_weak = title:c_derring_downs
		}
		if = {
			limit = { has_strong_claim_on = title:c_brandybottom } #should be weak claim
			make_claim_weak = title:c_brandybottom
		}
		if = {
			limit = { has_strong_claim_on = title:c_leafy_lake } #should be weak claim
			make_claim_weak = title:c_leafy_lake
		}
	}
	character:Lannister_2 ?= { #Kevan Lannister (son of Tytos)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = stubborn #honorable
		add_trait = authoritative
		add_trait = diligent
		add_trait = content
		add_trait = education_stewardship_3
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		add_martial_skill = 5 #he was a competent commander
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		if = {
			limit = {
				is_alive = yes
				character:Swyft_4 ?= { is_alive = yes } #Dorna Swyft (daughter of Harys)
				NOT = { is_spouse_of = character:Swyft_4 }
			}
			create_betrothal = character:Swyft_4 #Kevan and Dorna betrothed as early as possible so that they get their canon children in earlier bookmarks
			character:Lannister_129 ?= { #Tytos Lannister (son of Gerold)
				create_alliance = { # By betrothal
					target = character:Swyft_1 #Harys Swyft (son of Sheldon)
					allied_through_owner = character:Lannister_2
					allied_through_target = character:Swyft_4
				}
			}
		}
		if = {
			limit = { has_claim_on = title:c_trembley } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_trembley
		}
		if = {
			limit = { has_claim_on = title:c_fang_hill } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_fang_hill
		}
	}
	character:Lannister_3 ?= { #Genna Lannister (daughter of Tytos)
		if = {
			limit = { has_claim_on = title:c_trembley } #her father GAVE it to someone else, hence neither she nor her children get a claim
			remove_claim = title:c_trembley
		}
		if = {
			limit = { has_claim_on = title:c_fang_hill } #her father GAVE it to someone else, hence neither she nor her children get a claim
			remove_claim = title:c_fang_hill
		}
	}
	character:Lannister_4 ?= { #Tygett Lannister (son of Tytos)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_3
		add_trait = compassionate
		add_trait = ambitious
		add_trait = vengeful
		add_trait = diligent
		add_trait = lifestyle_blademaster
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = { has_claim_on = title:c_trembley } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_trembley
		}
		if = {
			limit = { has_claim_on = title:c_fang_hill } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_fang_hill
		}
	}
	character:Lannister_5 ?= { #Gerion Lannister (son of Tytos)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = gregarious
		add_trait = impatient
		add_trait = brave
		add_trait = compassionate
		add_trait = reckless
		add_trait = education_martial_prowess_4
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = { has_claim_on = title:c_trembley } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_trembley
		}
		if = {
			limit = { has_claim_on = title:c_fang_hill } #his father GAVE it to someone else, hence neither he nor his children get a claim
			remove_claim = title:c_fang_hill
		}
	}
	character:Lannister_6 ?= { #Cersei Lannister (daughter of Tywin)
		add_character_flag = { flag = no_drunkard_visual }
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_3
		add_trait = beauty_good_3
		add_trait = deceitful
		add_trait = vengeful
		add_trait = ambitious
		add_trait = arrogant
		add_trait = fashionable
		add_trait = schemer
		add_trait = seducer
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		if = { limit = { is_alive = yes } add_perk = mortal_adoration_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = smooth_operator_perk }
		if = { limit = { is_alive = yes } add_perk = seducer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		add_diplomacy_skill = 3 #Despite what the fanbase things, Cersei is not a dullard and should have higher stats
		add_stewardship_skill = 3
		add_learning_skill = 5
		add_intrigue_skill = -5 #her traits inflate it too much
		if = {
			limit = { has_trait = drunkard } #drinking more than usual doesn't make you a drunkard
			remove_trait = drunkard
		}
		if = {
			limit = { has_relation_lover = character:Lannister_7 } #is lover of Jaime
			add_character_flag = {
				flag = block_seduce_attempts
				years = 30
			}
		}
		if = {
			limit = {
				has_relation_rival = character:Lannister_8 #Tyrion
			}
			remove_relation_rival = character:Lannister_8
			set_relation_grudge = { reason = asoiaf_cersei_tyrion_grudge_desc target = character:Lannister_8 } #the rivalry is mostly one-way from Cersei
		}
		if = {
			limit = {
				asoiaf_canon_children_enabled_trigger = yes
				is_spouse_of = character:Baratheon_2 #Robert I
				NOT = { exists = character:Baratheon_asoiaf_baby } #Alaric
			}
			character:Lannister_6 ?= { #character:Lannister_6 MUST be here or else it throws an error; although still doesn't get her pregnant for some reason
				make_pregnant = { #to get Alaric out of the way asap
					father = character:Baratheon_2
					#number_of_children = 1
				}
				event_pregnancy_effect = yes
			}
		}
		if = {
			limit = {
				current_date >= 8299.7.1 #Margaery moves to King's Landing and is betrothed to Joffrey
				character:Lannister_6 ?= { is_alive = yes } #Cersei is alive
			}
			set_relation_rival = { reason = asoiaf_cersei_margaery_rival_desc target = character:Tyrell_14 } #Margaery Tyrell (daughter of Mace)
		}
		if = {
			limit = {
				current_date >= 8300.5.17 #the day Cersei is imprisoned by the High Sparrow
				character:Most_Devout_22 ?= { is_alive = yes }
			}
			set_relation_grudge = { reason = asoiaf_cersei_high_sparrow_grudge_desc target = character:Most_Devout_22 } #hates High Sparrow
		}
		if = {
			limit = {
				current_date >= 8300.5.17 #the day Cersei is imprisoned by the High Sparrow
				character:Most_Devout_23 ?= { is_alive = yes }
			}
			set_relation_grudge = { reason = asoiaf_cersei_unella_grudge_desc target = character:Most_Devout_23 } #hates Septa Unella
		}
	}
	character:Lannister_7 ?= { #Jaime Lannister (son of Tywin)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_martial_2
		add_trait = brave
		add_trait = impatient
		add_trait = arrogant
		add_trait = callous
		add_trait = education_martial_prowess_4
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 60
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 40
		}
		if = {
			limit = { has_relation_lover = character:Lannister_6 } #is lover of Cersei
			add_character_flag = {
				flag = block_seduce_attempts
				years = 30
			}
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_character_modifier = training_for_kingsguard #so that there's at least a fleeting chance he'll join, like in the lore
		}
		add_diplomacy_skill = 5 #no reason for all these stats to be so low
		add_martial_skill = 2
		add_stewardship_skill = 5
		add_intrigue_skill = 3
		add_learning_skill = 9
	}
	character:Lannister_8 ?= { #Tyrion Lannister (son of Tywin)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_childhood_traits_effect = yes
		add_trait = education_intrigue_4
		add_trait = dwarf
		add_trait = intellect_good_3
		add_trait = cynical
		add_trait = inquisitive
		add_trait = lustful
		add_trait = just
		add_trait = shrewd
		add_trait = lifestyle_reveler
		add_trait = rakish
		add_trait = scholar
		add_trait = schemer
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		add_character_flag = { flag = no_drunkard_visual }
		add_intrigue_skill = 5 #so it at least compares to that of Cersei
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = curious
		}
	}
	character:Tarth_6 ?= { #Brienne of Tarth (daughter of Selwyn)
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_good_2
		add_trait = loyal
	}
	character:Lannister_9 ?= { #Lancel Lannister (son of Kevan)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_2
		add_trait = education_martial_prowess_2
		add_trait = beauty_good_3
		if = {
			limit = { #regular traits
				NOR = {
					has_trait = wounded_3
					has_trait = scarred
				}
			}
			add_trait = arrogant
			add_trait = trusting
			add_trait = greedy
			add_trait = calm
			add_trait = fashionable
		}
		if = {
			limit = { #post-injury traits
				OR = {
					has_trait = wounded_3
					has_trait = scarred
				}
			}
			add_trait = zealous
			add_trait = arrogant
			add_trait = stubborn
			add_trait = brave
			add_character_modifier = vow_of_poverty_modifier #hasn't yet joined the Sparrows/Warrior's Sons, but has begun wearing simple robes
		}
		if = {
			limit = {
				has_trait = order_member #replace with Warrior's Sons when the time comes
			}
			add_character_modifier = the_seven_zealot_modifier #scar on his forehead
		}
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = bossy
		}
	}
	character:Lannister_10 ?= { #Willem Lannister (son of Kevan)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_2
		add_trait = trusting
		add_trait = calm
		add_trait = content
		add_trait = shy
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = pensive
		}
	}
	character:Lannister_11 ?= { #Martyn Lannister (son of Kevan)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_2
		add_trait = trusting
		add_trait = stubborn
		add_trait = compassionate
		add_trait = inquisitive
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = curious
		}
		if = {
			limit = {
				is_alive = yes
				character:Lannister_10 ?= { is_alive = yes }
			}
			set_relation_friend = character:Lannister_10 #Willem Lannister (son of Kevan)
		}
	}
	character:Lannister_12 ?= { #Janei Lannister (daughter of Kevan)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = beauty_good_2
		add_trait = diligent
		add_trait = compassionate
		add_trait = stubborn
		add_trait = authoritative
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
			add_trait = bossy
		}
		if = { #bonus skills to offset the huge jump in her age if using the Alternative Ages game rule
			limit = { has_game_rule = asoiaf_alternative_ages_rule_on }
			add_diplomacy_skill = 5
			add_martial_skill = 1
			add_stewardship_skill = 5
			add_intrigue_skill = 10
			add_learning_skill = 5
			add_prowess_skill = 5
		}
	}
	character:Tyrell_11 ?= { #Willas Tyrell (son of Mace)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = intellect_good_1
		add_trait = education_diplomacy_3
		add_trait = compassionate
		add_trait = forgiving
		add_trait = patient
		add_trait = lifestyle_hunter
		add_trait_xp = {
			trait = lifestyle_hunter
			track = falconer
			value = 50
		}
		add_trait = education_martial_prowess_2
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Tyrell_12 ?= { #Garlan Tyrell (son of Mace)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_3
		add_trait = compassionate
		add_trait = brave
		add_trait = just #honorable
		add_trait = diligent
		add_trait = athletic
		add_trait = education_martial_prowess_3
		add_trait = lifestyle_blademaster
		add_trait_xp = {
			trait = lifestyle_blademaster
			value = 75
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Tyrell_13 ?= { #Loras Tyrell (son of Mace)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_3
		add_trait = education_martial_2
		add_trait = brave
		add_trait = generous
		add_trait = stubborn
		add_trait = arrogant
		add_trait = fashionable
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 100
		}
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 50
		}
		add_trait_xp = {
			trait = tourney_participant
			track = wit
			value = 50
		}
		remove_trait = lifestyle_blademaster
		add_trait = education_martial_prowess_3
		add_trait = gallant
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = kingsguard_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = promising_prospects_perk }
		if = { limit = { is_alive = yes } add_perk = loyalty_and_respect_perk }
		if = { limit = { is_alive = yes } add_perk = peacemaker_perk }
		if = { limit = { is_alive = yes } add_perk = gallant_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			set_relation_crush = character:Baratheon_4
		}
		if = {
			limit = { character:Baratheon_4 ?= { is_alive = no } }
			add_trait = celibate
		}
		if = {
			limit = {
				NOT = { has_trait = kingsguard }
			}
			add_character_modifier = training_for_kingsguard #so he can more easily join
		}
	}
	character:Tyrell_14 ?= { #Margaery Tyrell (daughter of Mace)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_3
		add_trait = deceitful
		add_trait = generous
		add_trait = ambitious
		add_trait = compassionate
		add_trait = shrewd
		add_trait = lifestyle_hunter
		add_trait_xp = {
			trait = lifestyle_hunter
			track = falconer
			value = 50
		}
		add_trait = schemer
		add_trait = seducer
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		if = { limit = { is_alive = yes } add_perk = mortal_adoration_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = smooth_operator_perk }
		if = { limit = { is_alive = yes } add_perk = seducer_perk }
		add_stewardship_skill = 8
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		if = {
			limit = { #is married to Renly, Joffrey, or Tommen
				OR = {
					is_spouse_of = character:Baratheon_4 #Renly
					is_spouse_of = character:Baratheon_5 #Joffrey
					is_spouse_of = character:Baratheon_7 #Tommen
				}
			}
			add_character_flag = {
				flag = block_seduce_attempts
				years = 8 #enough time for them to grow up and initiate their own seductions against her
			}
		}
		if = {
			limit = {
				current_date >= 8299.7.1 #she moves to King's Landing and is betrothed to Joffrey
				character:Stark_8 ?= { is_alive = yes } #Sansa is alive (not "vanished")
			}
			set_relation_friend = { reason = asoiaf_margaery_sansa_friend_desc target = character:Stark_8 } #Sansa Stark (daughter of Eddard)
		}
	}
	character:Fossoway_New_Barrel_1 ?= { #Raymun Fossoway (founder of the Line)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_childhood_traits_effect = yes
		add_trait = education_martial_2
		add_trait = trusting
		add_trait = brave
		add_trait = compassionate
		add_trait = gregarious
		add_trait = education_martial_prowess_2
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = curious
		}
	}
	character:Redwyne_2 ?= { #Olenna Redwyne (the Queen of Thorns)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_childhood_traits_effect = yes
		add_trait = education_intrigue_4
		add_trait = shrewd
		add_trait = administrator
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = large_levies_perk }
		if = { limit = { is_alive = yes } add_perk = soon_forgiven_perk }
		if = { limit = { is_alive = yes } add_perk = toe_the_line_perk }
		if = { limit = { is_alive = yes } add_perk = honored_to_serve_perk }
		if = { limit = { is_alive = yes } add_perk = administrator_perk }
		add_intrigue_skill = 2
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = bossy
		}
	}
	character:Redwyne_5 ?= { #Horas Redwyne (son of Paxter)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_childhood_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = brave
		add_trait = fickle
		add_trait = unyielding_defender
		add_trait = education_martial_prowess_2
		add_trait = lifestyle_poet
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
		if = {
			limit = {
				character:Redwyne_6 ?= { is_alive = yes } #Hobber Redwyne (son of Paxter)
			}
			set_relation_friend = character:Redwyne_6
		}
	}
	character:Redwyne_6 ?= { #Hobber Redwyne (son of Paxter)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_childhood_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = fickle
		add_trait = callous
		add_trait = education_martial_prowess_2
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 10
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
	}
	character:Redwyne_7 ?= { #Desmera Redwyne (daughter of Paxter)
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
	}
	character:Baelish_2 ?= { #Petyr Baelish
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = intellect_good_3
		add_trait = education_intrigue_4
		add_trait = education_republican_knowledge_5
		remove_trait = scarred
		add_trait = deceitful
		add_trait = callous
		add_trait = ambitious
		add_trait = patient
		add_trait = shrewd
		add_trait = schemer
		add_trait = spider
		add_diplomacy_skill = 2
		add_intrigue_skill = 4
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = tax_man_perk }
		if = { limit = { is_alive = yes } add_perk = cutting_corners_perk }
		if = { limit = { is_alive = yes } add_perk = professional_workforce_perk }
		if = { limit = { is_alive = yes } add_perk = centralization_perk }
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = power_at_home_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		if = { limit = { is_alive = yes } add_perk = convincing_perk }
		if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		if = { limit = { is_alive = yes } add_perk = omerta_perk }
		if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		if = { limit = { is_alive = yes } add_perk = spider_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Florent_2 ?= { #Alester Florent (son of Imry)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = stubborn
		add_trait = ambitious
		add_trait = gregarious
		add_trait = just
		add_trait = avaricious
		if = { limit = { is_alive = yes } add_perk = faithful_perk }
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Florent_10 ?= { #Selyse Florent (daughter of Ryam)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = stubborn
		add_trait = callous
		add_trait = calm
		if = {
			limit = {
				has_religion = religion:the_rhllor_religion
			}
			add_trait = zealous
		}
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		if = {
			limit = {
				OR = {
					character:Baratheon_asoiaf_3_1 ?= { is_alive = no }
					character:Baratheon_asoiaf_3_2 ?= { is_alive = no }
					character:Baratheon_asoiaf_3_3 ?= { is_alive = no }
				}
			}
			add_character_modifier = asoiaf_stress_jarred_children #keeps her stillborn children in jars
		}
	}
	character:Florent_8 ?= { #Melessa Florent (daughter of Alester)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = intellect_good_1
		add_trait = compassionate
		add_trait = stubborn
		add_trait = trusting
		add_trait = generous
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Ashford_24 ?= { #Alyn Ashford (father of Gwin)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_stewardship_2
		add_trait = greedy
		add_trait = content
		add_trait = just
		add_trait = calm
		add_trait = profligate
		add_trait = shrewd
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
			add_trait = charming
		}
	}
	character:Ashford_27 ?= { #Gwin Ashford (daughter of Alyn)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_intrigue_2
		add_trait = inquisitive
		add_trait = honest
		add_trait = just
		add_trait = temperate
		add_intrigue_skill = 8 #she's not THAT bad at snooping around
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = curious
		}
	}
	character:Tarly_1 ?= { #Randyll Tarly (son of Talbert)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_5
		add_trait = callous
		add_trait = brave
		add_trait = stubborn #ruthless
		add_trait = authoritative
		add_trait = lifestyle_hunter
		add_trait = education_martial_prowess_3
		add_trait = organizer
		add_trait = overseer
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = soldiers_of_lesser_fortune_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = enduring_hardships_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_conscription_perk }
		if = { limit = { is_alive = yes } add_perk = absolute_control_perk }
		if = { limit = { is_alive = yes } add_perk = overseer_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				is_alive = yes
				character:Florent_8 ?= { is_alive = yes } #Melessa Florent (daughter of Alester)
				NOT = { is_spouse_of = character:Florent_8 }
			}
			create_betrothal = character:Florent_8
			character:Tarly_39 ?= {
				create_alliance = { # By betrothal
					target = character:Florent_2 #Alester Florent (son of Imry)
					allied_through_owner = character:Tarly_39
					allied_through_target = character:Florent_2
				}
			}
		}
	}
	character:Tarly_2 ?= { #Samwell Tarly (son of Randyll)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = intellect_good_2
		add_trait = education_learning_4
		add_trait = craven
		add_trait = gluttonous
		add_trait = compassionate
		add_trait = content
		add_trait = loyal
		add_trait = education_martial_prowess_1
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Tarly_3 ?= { #Talla Tarly (daughter of Randyll)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = compassionate
		add_trait = forgiving
		add_trait = generous
		add_trait = content
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				is_alive = yes
				character:Fossoway_6 ?= { is_alive = yes } #Symun Fossoway (son of Bennis)
				NOT = { is_spouse_of = character:Fossoway_6 }
				current_date >= 8299.1.1 #in the scene during 303AC she sounds like it's a recent betrothal
			}
			create_betrothal = character:Fossoway_6
			character:Tarly_1 ?= {
				create_alliance = { # By betrothal
					target = character:Fossoway_1 #Bennis Fossoway (son of Alyn)
					allied_through_owner = character:Tarly_1
					allied_through_target = character:Fossoway_1
				}
			}
		}
	}
	character:Tarly_6 ?= { #Dickon Tarly (son of Randyll)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_martial_2
		add_trait = stubborn
		add_trait = brave
		add_trait = compassionate
		add_trait = honest
		add_trait = lifestyle_hunter
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:LannisterLP_3 ?= { #Reginald Lannister of Lannisport (the one that Tywin tells-off in Harrenhal)
		if = {
			limit = {
				NOT = { has_trait = lazy }
			}
			add_trait = lazy
		}
	}
	character:Pycelle_1 ?= {
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_4
		add_trait = greedy
		add_trait = deceitful
		add_trait = lustful
		add_trait = patient
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Most_Devout_20 ?= { #High Septon (The Fat One)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_learning_2
		add_trait = content
		add_trait = gluttonous
		add_trait = greedy
		add_trait = calm
		add_trait = lifestyle_reveler
		add_trait = whole_of_body
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Most_Devout_21 ?= { #High Septon (The One After the Fat One)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_3
		add_trait = patient
		add_trait = lustful
		add_trait = content
		add_trait = arrogant
		add_trait = rakish
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Most_Devout_22 ?= { #The High Sparrow
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_4
		add_trait = intellect_good_1
		if = {
			limit = {
				is_alive = yes
				current_date < 8298.1.1 #the day he abandons his name and becomes a Sparrow
			}
			change_first_name = Jonathan
		}
		if = {
			limit = { NOT = { has_trait = septon } } #traits before becoming a Septon
			add_trait = diligent
			add_trait = greedy
			add_trait = gregarious
			add_trait = authoritative
			add_trait = fashionable
			add_trait = profligate
			add_trait = lifestyle_reveler
		}
		if = {
			limit = { has_trait = septon } #traits after becoming a Septon
			add_trait = zealous
			add_trait = authoritative
			add_trait = humble
			add_trait = ambitious
			add_trait = shrewd
			add_trait = theologian
			if = { limit = { is_alive = yes } add_perk = faithful_perk }
			if = { limit = { is_alive = yes } add_perk = zealous_proselytizer_perk }
			if = { limit = { is_alive = yes } add_perk = religious_icon_perk }
			if = { limit = { is_alive = yes } add_perk = prophet_perk }
			if = { limit = { is_alive = yes } add_perk = clerical_justifications_perk }
			if = { limit = { is_alive = yes } add_perk = church_and_state_perk }
			if = { limit = { is_alive = yes } add_perk = radiant_perk }
			if = { limit = { is_alive = yes } add_perk = defender_of_the_faith_perk }
			if = { limit = { is_alive = yes } add_perk = theologian_perk }
			if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
			if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
			if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
			if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
			if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
			if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
			if = { limit = { is_alive = yes } add_perk = inspiring_rule_perk }
		}
		add_trait = whole_of_body
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Tully_hh_4 ?= { #Septa Mordane
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_learning_3
		add_trait = stubborn
		add_trait = chaste
		add_trait = diligent
		add_trait = just
		add_trait = loyal
		add_trait = shrewd
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Most_Devout_23 ?= { #Septa Unella
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_learning_2
		add_trait = zealous
		add_trait = sadistic
		add_trait = calm
		add_trait = humble
		add_trait = torturer
		if = { limit = { is_alive = yes } add_perk = faithful_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
		if = { limit = { is_alive = yes } add_perk = torturer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Tyrell_4 ?= { #Luthor Tyrell (son of Garlan)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_diplomacy_1
		add_trait = compassionate
		add_trait = gregarious
		add_trait = trusting
		add_trait = fickle
		add_trait = dull
		add_trait = fecund
		add_trait = lifestyle_hunter
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = charming
		}
	}
	character:Tyrell_8 ?= { #Mace Tyrell (son of Luthor)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = gregarious
		add_trait = compassionate
		add_trait = trusting
		add_trait = gluttonous
		add_trait = dull
		add_trait = lifestyle_poet
		add_trait = lifestyle_reveler
		add_trait = lifestyle_hunter
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = { limit = { is_alive = yes } add_perk = roaming_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = charming
		}
	}
	character:Baratheon_34 ?= { #Boremund Baratheon (son of Rogar)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		add_trait = physique_good_1
		add_trait = beauty_good_1
		add_trait = education_diplomacy_3
		add_trait = just
		add_trait = honest
		add_trait = stubborn
		add_trait = diligent
		add_trait = education_martial_prowess_3
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Baratheon_36 ?= { #Borros Baratheon (son of Boremund)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = ambitious
		add_trait = wrathful
		add_trait = impatient
		add_trait = education_martial_prowess_3
		add_trait = lifestyle_reveler
		add_learning_skill = -10 #illiterate
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Baratheon_50 ?= { #Cassandra Baratheon (daughter of Borros)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_1
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Baratheon_51 ?= { #Maris Baratheon (daughter of Borros)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_2
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Baratheon_52 ?= { #Floris Baratheon (daughter of Borros)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_1
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Baratheon_53 ?= { #Ellyn Baratheon (daughter of Borros)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		add_trait = education_stewardship_2
		add_trait = temperate
		add_trait = content
		add_trait = generous
		add_trait = arrogant
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Baratheon_71 ?= { #Lyonel Baratheon (son of Symeon)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_2
		add_trait = gregarious
		add_trait = brave
		add_trait = just
		add_trait = stubborn
		add_trait = education_martial_prowess_4
		add_trait = lifestyle_blademaster
		add_trait = lifestyle_reveler
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = foot
			value = 30
		}
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
	}
	character:Baratheon_1 ?= { #Steffon Baratheon (son of Ormund)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = education_martial_3
		add_trait = brave
		add_trait = diligent
		add_trait = gregarious
		add_trait = just
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				is_alive = yes
				current_date < 8262.1.1 #the date Steffon and Cassana marry in the history file
				exists = character:Estermont_5
				NOT = { is_spouse_of = character:Estermont_5 } #Cassana Estermont (daughter of Edwyn)
			}
			create_betrothal = character:Estermont_5
			character:Baratheon_75 ?= { #Ormund Baratheon (son of Lyonel)
				create_alliance = { # By betrothal
					target = character:Estermont_1 #Edwyn Estermont (son of Gunthor)
					allied_through_owner = character:Baratheon_1
					allied_through_target = character:Estermont_5
				}
			}
		}
	}
	character:Baratheon_2 ?= { #Robert I Baratheon
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_good_1 #He's strong (mostly) because of training, not because he's Gregor Clegane
		add_prowess_skill = -5 #his prowess is being inflated too much, so bring his base down a bit
		add_character_flag = { flag = no_drunkard_visual }
		if = {
			limit = {
				current_date >= 8283.6.1 #Robert inherits the throne; Crowned Stag bookmark
				is_alive = yes #Robert is alive
				character:Stark_3 = { is_alive = yes } #Eddard is alive
			}
			remove_hook = { target = character:Stark_3 } #no hook so Robb doesn't become stormlander each playthrough
		}
		if = {
			limit = {
				has_relation_rival = character:Targaryen_3 #Rhaegar
			}
			remove_relation_rival = character:Targaryen_3
			set_relation_grudge = { reason = asoiaf_robert_rhaegar_grudge_desc target = character:Targaryen_3 } #the "rivalry" is one-way hatred from Robert
		}
		if = {
			limit = {
				current_date >= 8283.9.1 #Robert takes the throne
				NOT = { has_relation_rival = character:Targaryen_8 } #Viserys
			}
			set_relation_grudge = { reason = asoiaf_robert_targaryen_grudge_desc target = character:Targaryen_8 }
		}
		if = {
			limit = {
				current_date >= 8283.9.1 #Robert takes the throne
				NOT = { has_relation_rival = character:Targaryen_9 } #Daenerys
			}
			set_relation_grudge = { reason = asoiaf_robert_targaryen_grudge_desc target = character:Targaryen_9 }
		}
	}
	character:Baratheon_3 ?= { #Stannis Baratheon (son of Steffon)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = callous
		add_trait = just
		add_trait = stubborn
		add_trait = vengeful
		add_trait = strategist
		add_trait = overseer
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = engineered_for_destruction_perk }
		if = { limit = { is_alive = yes } add_perk = living_off_the_land_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = soldiers_of_lesser_fortune_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = enduring_hardships_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_conscription_perk }
		if = { limit = { is_alive = yes } add_perk = absolute_control_perk }
		if = { limit = { is_alive = yes } add_perk = overseer_perk }
		if = { limit = { is_alive = yes } add_perk = power_at_home_perk }
		if = { limit = { is_alive = yes } add_perk = mercenary_contacts_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				character:Baratheon_3.father = { is_alive = no } #Steffon Baratheon is dead
			}
			add_secret = { type = secret_non_believer }
		}
		if = {
			limit = {
				character:Baratheon_4 = { has_title = title:c_storms_end } #Renly has Storm's End
			}
			set_relation_grudge = { reason = asoiaf_stannis_renly_grudge_desc target = character:Baratheon_4 } #Renly
		}
		if = {
			limit = {
				current_date >= 8283.9.1 #Robert I becomes king, meaning the Siege of Storm's End is over
			}
			set_relation_grudge = { reason = asoiaf_stannis_mace_grudge_desc target = character:Tyrell_8 } #Mace Tyrell
		}
		if = { #Opinion boost for Stannis/Melisandre so they don't hate each other
			limit = {
				character:Melisandre_1 ?= { is_courtier_of = character:Baratheon_3 }
			}
			add_opinion = {
				modifier = respect_opinion
				opinion = 200
				target = character:Melisandre_1
			}
			reverse_add_opinion = {
				modifier = respect_opinion
				opinion = 200
				target = character:Melisandre_1
			}
		}
	}
	character:Baratheon_4 ?= { #Renly Baratheon (son of Steffon)
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = beauty_good_2
		add_trait = compassionate
		add_trait = temperate
		add_trait = gregarious
		add_trait = arrogant
		add_trait = education_martial_prowess_2
		add_trait = diplomat
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = inspiring_rule_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = praetorian_guard_perk }
		if = { limit = { is_alive = yes } add_perk = true_ruler_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = ducal_conquest_perk }
		if = { limit = { is_alive = yes } add_perk = forced_vassalage_perk }
		if = { limit = { is_alive = yes } add_perk = adaptive_traditions_perk }
		if = { limit = { is_alive = yes } add_perk = flexible_truces_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_negotiations_perk }
		if = { limit = { is_alive = yes } add_perk = embassies_perk }
		if = { limit = { is_alive = yes } add_perk = accomplished_forger_perk }
		if = { limit = { is_alive = yes } add_perk = diplomat_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = positions_of_power_perk }
		if = { limit = { is_alive = yes } add_perk = power_at_home_perk }
		if = { limit = { is_alive = yes } add_perk = mustering_the_troops_perk }
		add_martial_skill = 3
		add_stewardship_skill = 7
		add_learning_skill = 3
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			set_relation_crush = character:Tyrell_13
		}
	}
	character:Stokeworth_6 ?= { #Lollys Stokeworth (daughter of Tanda)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = shy
		add_trait = compassionate
		add_trait = content
		add_trait = craven
		add_trait = reclusive
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
		if = {
			limit = {
				is_alive = yes
				age < 16
				character:Stokeworth_5 ?= { #Falyse Stokeworth (daughter of Tanda)
					is_alive = yes
					age < 16
				}
			}
			set_relation_bully = character:Stokeworth_5
		}
	}
	character:Stark_84 ?= { #Rickon Stark (son of Benjen)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = diligent
		add_trait = trusting
		add_trait = gregarious
		add_trait = honest
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				is_alive = yes
				character:Glover_21 ?= { is_alive = yes } #Gilliane Glover (daughter of Edric)
				NOT = { is_spouse_of = character:Glover_21 }
			}
			create_betrothal = character:Glover_21
			character:Stark_84 ?= {
				create_alliance = { # By betrothal
					target = character:Glover_20 #Edric Glover (son of Barthogan)
					allied_through_owner = character:Stark_84
					allied_through_target = character:Glover_21
				}
			}
		}
	}
	character:Stark_86 ?= { #Cregan Stark (son of Rickon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_4
		add_trait = beauty_good_1
		add_trait = honest #honorable
		add_trait = diligent
		add_trait = just
		add_trait = brave
		add_trait = strong
		add_trait = fecund
		add_trait = education_martial_prowess_4
		add_trait = lifestyle_blademaster
		add_trait_xp = {
			trait = lifestyle_blademaster
			value = 100
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Stark_88 ?= { #Sara Snow (bastard of Rickon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = beauty_good_2
		add_trait = gregarious
		add_trait = compassionate
		add_trait = brave
		add_trait = honest
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Stark_94 ?= { #Rickard Stark (son of Cregan)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = diligent
		add_trait = gregarious
		add_trait = brave
		add_trait = just #honorable
		add_trait = lifestyle_poet #sang a song for Alysanne Blackwood during her wedding to Cregan
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Stark_102 ?= { #Barthogan Stark (son of Cregan)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = brave
		add_trait = diligent
		add_trait = impatient
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Stark_1 ?= { #Rickard Stark (son of Edwyle)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = just #honorable
		add_trait = authoritative
		add_trait = calm
		add_trait = ambitious
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Stark_2 ?= { #Brandon Stark (son of Rickard)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = beauty_good_2
		add_trait = education_martial_prowess_3
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 65
		}
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
	}
	character:Stark_3 ?= { #Eddard Stark (son of Rickard)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = honest
		add_trait = diligent #honorable
		add_trait = trusting
		add_trait = just
		add_trait = education_martial_prowess_3
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Stark_5 ?= { #Lyanna Stark (daughter of Rickard)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_diplomacy_2
		add_trait = education_martial_prowess_2
		add_trait = compassionate
		add_trait = brave
		add_trait = stubborn
		add_trait = tourney_participant
		add_trait_xp = {
			trait = tourney_participant
			track = horse
			value = 30
		}
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = rowdy
		}
	}
	character:Stark_4 ?= { #Benjen Stark (son of Rickard)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = education_martial_prowess_3
	}
	character:Stark_6 ?= { #Robb Stark (son of Eddard)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_4
		add_trait = beauty_good_2
		add_trait = brave
		add_trait = just
		add_trait = trusting #honorable
		add_trait = authoritative
		add_trait = flexible_leader
		add_trait = forest_fighter
		add_trait = education_martial_prowess_3
		add_trait = strategist
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = parthian_tactics_perk }
		if = { limit = { is_alive = yes } add_perk = envelopment_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = engineered_for_destruction_perk }
		if = { limit = { is_alive = yes } add_perk = living_off_the_land_perk }
		if = { limit = { is_alive = yes } add_perk = sappers_perk }
		if = { limit = { is_alive = yes } add_perk = strategist_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = enduring_hardships_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = mustering_the_troops_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = charming
		}
		if = {
			limit = { #guarantees he gets martial_4 on book bookmarks
				age >= 14
				age < 16
			}
			change_age = 2
			add_trait = education_martial_4
			change_age = -2
		}
	}
	character:Manderly_10 ?= { #Wylla Manderly (daughter of Wylis)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_intrigue_1
		add_trait = beauty_good_1
		add_trait = just
		add_trait = honest
		add_trait = stubborn
		add_trait = brave
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = rowdy
		}
	}
	character:Cassel_3 ?= { #Rodrik Cassel
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = education_martial_prowess_2
		add_trait = brave
		add_trait = diligent
		add_trait = stubborn
		add_trait = trusting
		add_trait = loyal
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Cassel_4 ?= { #Jory Cassel
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = education_martial_prowess_2
		add_trait = diligent
		add_trait = compassionate
		add_trait = just
		add_trait = brave
		add_trait = loyal
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Reed_1 ?= { #Howland Reed (son of Edwyle)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = education_learning_4
		add_trait = shy
		add_trait = just
		add_trait = brave
		add_trait = patient
		add_trait = education_martial_prowess_3
		add_trait = lifestyle_mystic
		add_trait = loyal
		if = {
			limit = { age < 16 }
			set_focus = education_learning
			add_trait = pensive
		}
	}
	character:Reed_2 ?= { #Meera Reed (daughter of Howland)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_2
		add_trait = brave
		add_trait = paranoid
		add_trait = compassionate
		add_trait = diligent
		add_trait = lifestyle_hunter
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
	}
	character:Reed_3 ?= { #Jojen Reed (son of Howland)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = physique_bad_2
		add_trait = education_learning_3
		add_trait = honest
		add_trait = stubborn
		add_trait = authoritative
		add_trait = calm
		add_trait = lifestyle_mystic
		add_trait = greensight
		if = {
			limit = { age < 16 }
			set_focus = education_learning
			add_trait = pensive
		}
	}
	character:Greyjoy_5 ?= { #Balon Greyjoy (son of Quellon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_3
		add_trait = ambitious
		add_trait = stubborn
		add_trait = brave
		add_trait = rude
		add_trait = education_martial_prowess_3
		add_trait = viking
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = engineered_for_destruction_perk }
		if = { limit = { is_alive = yes } add_perk = living_off_the_land_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
		if = {
			limit = {
				is_alive = yes
				current_date < 8272.1.1 #the date Balon and Alannys marry in the history file
				exists = character:Harlaw_8
				NOT = { is_spouse_of = character:Harlaw_8 } #Alannys Harlaw (daughter of Sigfryd)
			}
			create_betrothal = character:Harlaw_8
			character:Greyjoy_1 ?= { #Quellon Greyjoy (son of Torwyn)
				create_alliance = { #by betrothal
					target = character:Harlaw_1 #Sigrin Harlaw (son of Theon)
					allied_through_owner = character:Greyjoy_5
					allied_through_target = character:Harlaw_8
				}
			}
		}
	}
	character:Greyjoy_11 ?= { #Rodrik Greyjoy (son of Balon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_character_flag = { flag = no_drunkard_visual }
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = brave
		add_trait = gregarious
		add_trait = wrathful
		add_trait = drunkard
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
	}
	character:Greyjoy_12 ?= { #Maron Greyjoy (son of Balon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_2
		add_trait = arrogant
		add_trait = deceitful
		add_trait = callous
		add_trait = brave
		add_trait = education_martial_prowess_2
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
		}
	}
	character:Greyjoy_13 ?= { #Yara Greyjoy (daughter of Balon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = intellect_good_1
		add_trait = education_martial_3
		add_trait = brave
		add_trait = arrogant
		add_trait = diligent
		add_trait = gregarious
		add_trait = shieldmaiden
		add_trait = education_martial_prowess_3
		add_trait = reaver
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = organized_retreat_perk }
		if = { limit = { is_alive = yes } add_perk = hit_and_run_perk }
		if = { limit = { is_alive = yes } add_perk = engineered_for_destruction_perk }
		if = { limit = { is_alive = yes } add_perk = living_off_the_land_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
	}
	character:Greyjoy_14 ?= { #Theon Greyjoy (son of Balon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_2
		add_trait = ambitious
		add_trait = lustful
		add_trait = arrogant
		add_trait = brave
		add_trait = education_martial_prowess_3
		add_trait = military_engineer
		add_trait = lifestyle_hunter
		if = { limit = { is_alive = yes } add_perk = bellum_justum_perk }
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
		if = {
			limit = {
				has_trait = eunuch #He's been broken by torture
			}
			asoiaf_clear_personality_traits_effect = yes
			asoiaf_clear_lifestyle_traits_effect = yes
			add_trait = paranoid
			add_trait = shy
			add_trait = humble
			add_trait = craven
		}
	}
	character:Lannister_29 ?= { #Lann the Clever
		set_character_faith = faith:fots_seven
		set_culture = culture:andal #GRRM confirmed in an interview that the name "Lannister" is Andal, indirectly confirming that Lann the Clever was Andal
	}
	character:Lannister_17 ?= { #Joanna Lannister (daughter of Jason)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_2
		add_trait = compassionate
		add_trait = forgiving
		add_trait = content
		add_trait = diligent
		add_trait = family_first
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = confidants_perk }
		if = { limit = { is_alive = yes } add_perk = flatterer_perk }
		if = { limit = { is_alive = yes } add_perk = familial_familiar_perk }
		if = { limit = { is_alive = yes } add_perk = groomed_to_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thicker_than_water_perk }
		if = { limit = { is_alive = yes } add_perk = friendly_counsel_perk }
		if = { limit = { is_alive = yes } add_perk = sound_foundations_perk }
		if = { limit = { is_alive = yes } add_perk = family_man_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Lannister_24 ?= { #Daven Lannister (son of Stafford)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_3
		add_trait = brave
		add_trait = gregarious
		add_trait = honest #blunt and outspoken
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = well_prepared_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = bossy
		}
		if = {
			limit = {
				character:Lannister_24.father ?= { is_alive = no } #Stafford died in the WotFK and Daven swore vengance
			}
			add_trait = vengeful
		}
	}
	character:Lannister_134 ?= { #Alton Lannister (son of Tybolt)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = trusting
		add_trait = diligent
		add_trait = inquisitive
		add_trait = honest
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = curious
		}
		if = {
			limit = {
				current_date >= 8296.1.1 #date of Willem Frey's wedding tourney in which Alton squired for Jaime; ALTON IS 14
				current_date < 8298.1.1 #Alton knighted at 16
			}
			agot_set_as_ward_and_squire_history_effect = { KNIGHT = character:Lannister_7 } #Jaime Lannister (son of Tywin)
		}
		if = {
			limit = {
				current_date >= 8298.1.1 #Alton knighted at 16
			}
			add_trait = knight
		}
	}
	character:Lannister_hh_4 ?= { #Vylarr (Lannister Captain of the Household Guard)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = education_martial_2
		add_trait = diligent
		add_trait = temperate
		add_trait = stubborn
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:LannisterLP_49 ?= { #Cynda Lannister (mother of Alton)
		add_trait = gluttonous
		add_trait = comfort_eater #she's one of the few fat Lannisters
	}
	character:Baratheon_5 ?= { #Joffrey I Baratheon
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_intrigue_2
		add_trait = sadistic
		add_trait = impatient
		add_trait = arrogant
		add_trait = wrathful
		add_trait = lifestyle_hunter
		add_trait = fashionable
		add_trait = education_martial_prowess_1
		add_trait = torturer
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
		if = { limit = { is_alive = yes } add_perk = torturer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
			add_trait = rowdy
		}
	}
	character:Baratheon_6 ?= { #Myrcella Baratheon (daughter of Robert I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_3
		add_trait = intellect_good_1
		add_trait = compassionate
		add_trait = trusting
		add_trait = stubborn
		add_trait = generous
		add_trait = lifestyle_traveler #"Myrcella loves the open water"
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = curious
		}
	}
	character:Baratheon_7 ?= { #Tommen I Baratheon
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_2
		add_trait = beauty_good_2
		add_trait = compassionate
		add_trait = content
		add_trait = trusting
		add_trait = fickle
		add_trait = lifestyle_traveler #loves sailing
		add_stewardship_skill = 5 #he's not THAT bad at governance, especially with good advisors
		add_learning_skill = 5
		if = { limit = { is_alive = yes } add_perk = benevolent_intent_perk }
		if = { limit = { is_alive = yes } add_perk = inspiring_rule_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = chains_of_loyalty_perk }
		if = { limit = { is_alive = yes } add_perk = likable_perk }
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = pensive
		}
		if = {
			limit = {
				is_alive = yes
				age < 16
				character:Baratheon_5 ?= { #Joffrey I Baratheon
					is_alive = yes
					age < 16
				}
			}
			set_relation_bully = character:Baratheon_5
		}
		if = {
			limit = {
				age < 16 #crush relation only works for kids
				has_title = title:h_the_iron_throne #Margaery has begun to seduce him
			}
			set_relation_crush = character:Tyrell_14 #Margaery Tyrell (daughter of Mace)
		}
	}
	character:Baratheon_10 ?= { #Mya Stone (bastard of Robert I)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = beauty_good_2
		add_trait = brave
		add_trait = stubborn
		add_trait = gregarious
		add_trait = lustful
		if = {
			limit = { age < 16 }
			set_focus = education_martial
			add_trait = rowdy
			set_relation_crush = character:Redfort_5
		}
	}
	character:Santagar_6 ?= { #Aron Santagar (son of Sylva)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = arrogant
		add_trait = honest
		add_trait = temperate
		add_trait = just
		add_trait = fashionable
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Uller_6 ?= { #Ellaria Sand
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_3
		add_trait = beauty_good_2
		add_trait = impatient
		add_trait = brave
		add_trait = vengeful
		add_trait = callous
		add_trait = fashionable
		add_trait = lifestyle_reveler
		add_trait = refusing_marriage
		add_trait = schemer
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Martell_9 ?= { #Arianne Martell (daughter of Doran)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = lustful
		add_trait = brave
		add_trait = ambitious
		add_trait = impatient
		add_trait = shrewd
		add_trait = seducer
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		if = { limit = { is_alive = yes } add_perk = mortal_adoration_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = smooth_operator_perk }
		if = { limit = { is_alive = yes } add_perk = seducer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
	}
	character:Martell_10 ?= { #Quentyn Martell (son of Doran)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = temperate
		add_trait = diligent
		add_trait = brave
		add_trait = chaste
		add_trait = education_martial_prowess_3
		add_trait = scholar
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		learn_language = language_agot_valyrian
		learn_language = language_agot_free_cities
		learn_language = language_agot_ghis
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Martell_11 ?= { #Trystane Martell (son of Doran)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = beauty_good_3
		add_trait = just
		add_trait = brave
		add_trait = diligent #honorable
		add_trait = compassionate
		add_trait = loyal
		add_trait = education_martial_prowess_2
		add_trait = gallant
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = kingsguard_perk }
		if = { limit = { is_alive = yes } add_perk = courtship_perk }
		if = { limit = { is_alive = yes } add_perk = promising_prospects_perk }
		if = { limit = { is_alive = yes } add_perk = loyalty_and_respect_perk }
		if = { limit = { is_alive = yes } add_perk = peacemaker_perk }
		if = { limit = { is_alive = yes } add_perk = gallant_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Martell_12 ?= { #Obara Sand (bastard of Oberyn)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = wrathful
		add_trait = arrogant
		add_trait = callous
		add_trait = impatient
		add_trait = education_martial_prowess_4
		add_trait = strong
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = a_mans_home_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Martell_14 ?= { #Tyene Sand (bastard of Oberyn)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_intrigue_3
		add_trait = beauty_good_2
		add_trait = deceitful
		add_trait = gregarious
		add_trait = impatient
		add_trait = brave
		add_trait = education_martial_prowess_3
		add_trait = seducer
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = like_weed_in_a_garden_perk }
		if = { limit = { is_alive = yes } add_perk = unshackled_lust_perk }
		if = { limit = { is_alive = yes } add_perk = subtle_desire_perk }
		if = { limit = { is_alive = yes } add_perk = mortal_adoration_perk }
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = graceful_recovery_perk }
		if = { limit = { is_alive = yes } add_perk = smooth_operator_perk }
		if = { limit = { is_alive = yes } add_perk = seducer_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		if = {
			limit = { has_game_rule = asoiaf_alternative_ages_rule_on } #ages are adjusted to make this relationship make sense
			set_mother = character:Uller_6 #Ellaria Sand
		}
	}
	character:Royce_37 ?= { #Rhea Royce (daughter of Yorbert)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = intellect_good_1
		add_trait = brave
		add_trait = just #honorable
		add_trait = stubborn
		add_trait = lifestyle_hunter
		add_trait = education_martial_prowess_2
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Royce_93 ?= { #Gerold Royce (son of Gunthor)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = intellect_good_1
		add_trait = brave
		add_trait = just
		add_trait = stubborn
		add_trait = honest
		add_trait = lifestyle_hunter
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = {
				character:Royce_37 ?= { is_alive = no } #Rhea Royce is dead
				character:Targaryen_61 ?= { is_alive = yes } #Daemon Targaryen is alive
			}
			set_relation_grudge = { reason = asoiaf_gerold_daemon_grudge_desc target = character:Targaryen_61 } #Daemon
		}
	}
	character:Waynwood_5 ?= { #Anya Waynwood
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = diligent
		add_trait = authoritative
		add_trait = just
		add_trait = cynical
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
		}
	}
	character:Egen_3 ?= { #Vardis Egen
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = brave
		add_trait = just
		add_trait = diligent
		add_trait = honest
		add_trait = loyal
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Moore_2 ?= { #Mandon Moore (son of Jon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_lifestyle_traits_effect = yes
		add_trait = education_martial_3
		add_trait = callous
		add_trait = diligent
		add_trait = brave
		add_trait = arbitrary
		add_trait = loyal
		add_trait = education_martial_prowess_3
		add_trait = lifestyle_blademaster
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Strong_28 ?= { #Simon Strong
		add_character_modifier = asoiaf_health_boost
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_3
		add_trait = intellect_good_1
		add_trait = diligent
		add_trait = honest
		add_trait = gregarious
		add_trait = just #honorable
		add_trait = loyal
		add_trait = shrewd
		add_trait = fashionable
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
		character:Strong_31 ?= {
			if = {
				limit = {
					is_alive = yes
					is_landed = yes
				}
				designate_diarch = character:Strong_28 #to guarantee that he's the regent back home in Harrenhal
			}
		}
	}
	character:Strong_29 ?= { #Lyonel Strong (son of Osmund)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = intellect_good_1
		add_trait = diligent
		add_trait = just
		add_trait = honest
		add_trait = calm
		add_trait = loyal
		add_trait = scholar
		add_trait = diplomat
		if = { limit = { is_alive = yes } add_perk = pedagogy_perk }
		if = { limit = { is_alive = yes } add_perk = open_minded_perk }
		if = { limit = { is_alive = yes } add_perk = apostate_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = { limit = { is_alive = yes } add_perk = scholarly_circles_perk }
		if = { limit = { is_alive = yes } add_perk = learn_on_the_job_perk }
		if = { limit = { is_alive = yes } add_perk = sanctioned_loopholes_perk }
		if = { limit = { is_alive = yes } add_perk = scholar_perk }
		if = { limit = { is_alive = yes } add_perk = thoughtful_perk }
		if = { limit = { is_alive = yes } add_perk = ducal_conquest_perk }
		if = { limit = { is_alive = yes } add_perk = forced_vassalage_perk }
		if = { limit = { is_alive = yes } add_perk = adaptive_traditions_perk }
		if = { limit = { is_alive = yes } add_perk = flexible_truces_perk }
		if = { limit = { is_alive = yes } add_perk = defensive_negotiations_perk }
		if = { limit = { is_alive = yes } add_perk = embassies_perk }
		if = { limit = { is_alive = yes } add_perk = accomplished_forger_perk }
		if = { limit = { is_alive = yes } add_perk = diplomat_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Strong_34 ?= { #Alys Rivers
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = honest
		add_trait = ambitious
		add_trait = stubborn
		add_trait = authoritative
		add_trait = greensight
		add_trait = lifestyle_physician
		add_trait = refusing_marriage
		if = { limit = { is_alive = yes } add_perk = enticing_opportunity_perk }
		if = { limit = { is_alive = yes } add_perk = home_advantage_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		set_character_faith = faith:old_gods_wnw
	}
	character:Strong_30 ?= { #Harwin Strong (son of Lyonel)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = brave
		add_trait = gregarious
		add_trait = compassionate
		add_trait = wrathful
		add_trait = loyal
		add_trait = strong
		add_trait = education_martial_prowess_4
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Strong_31 ?= { #Larys Strong (son of Lyonel)
		asoiaf_clear_personality_traits_effect = yes
		add_trait = patient
		add_trait = deceitful
		add_trait = callous
		add_trait = cynical #ruthless
		add_trait = schemer
		add_trait = disloyal
		add_trait = spider
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		if = { limit = { is_alive = yes } add_perk = convincing_perk }
		if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		if = { limit = { is_alive = yes } add_perk = omerta_perk }
		if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		if = { limit = { is_alive = yes } add_perk = spider_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		add_prowess_skill = -10 #base 0
	}
	character:Clegane_4 ?= { #Sandor Clegane (the Hound)
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		#make Sandor a sword person
		remove_character_flag = swordshield_person
		remove_character_flag = maceshield_person
		remove_character_flag = spearshield_person
		remove_character_flag = sword_person
		remove_character_flag = axe_person
		remove_character_flag = spear_person
		remove_character_flag = mace_person
		remove_character_flag = hammer_person
		hidden_effect = {
			add_character_flag = sword_person
			set_variable = {
				name = signature_weapon
				value = flag:sword
			}
		}
	}
	character:Mopatis_1 ?= { #Illyrio Mopatis
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_stewardship_4
		add_trait = calm
		add_trait = gluttonous
		add_trait = ambitious
		add_trait = patient
		add_trait = shrewd
		add_trait = fashionable
		add_trait = education_republican_knowledge_4
		add_trait = education_martial_prowess_2
		add_trait = schemer
		add_trait = avaricious
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Hotah_1_B ?= { #Areo Hotah
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_2
		add_trait = brave
		add_trait = diligent
		add_trait = authoritative
		add_trait = patient
		add_trait = education_martial_prowess_4
		add_trait = loyal
		add_trait = strong
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = { is_courtier_of = character:Martell_4 }
			set_relation_friend = { reason = asoiaf_areo_doran_friend_desc target = character:Martell_4 }
		}
	}
	character:Targaryen_hh_3 ?= { #Mysaria (the White Worm)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_1
		add_trait = intellect_good_1
		add_trait = deceitful
		add_trait = just
		add_trait = stubborn
		add_trait = patient
		add_trait = shrewd
		add_trait = spider
		if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		if = { limit = { is_alive = yes } add_perk = convincing_perk }
		if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		if = { limit = { is_alive = yes } add_perk = omerta_perk }
		if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		if = { limit = { is_alive = yes } add_perk = spider_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		#not quite the level of schemer
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		if = {
			limit = { age >= 15 } #father impregnates her and she injures her womb to prevent pregnancy
			add_trait = infertile
		}
	}
	character:Seaworth_1 ?= { #Davos Seaworth
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = intellect_good_1
		add_trait = just
		add_trait = brave
		add_trait = honest
		add_trait = compassionate
		add_trait = loyal
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Stark_hh_1 ?= { #Maester Luwin
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_learning_3
		add_trait = compassionate
		add_trait = just
		add_trait = diligent
		add_trait = content
		add_trait = loyal
		add_trait = whole_of_body
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Baratheon_hh_3 ?= { #Maester Cressen
		add_character_modifier = asoiaf_health_boost
		asoiaf_clear_personality_traits_effect = yes
		add_trait = just
		add_trait = content
		add_trait = humble
		add_trait = honest
		add_trait = loyal
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
	}
	character:Strickland_1 ?= { #Harry Strickland (Homeless)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = beauty_good_2
		add_trait = greedy
		add_trait = ambitious
		add_trait = gregarious
		add_trait = patient
		add_trait = education_martial_prowess_2
		add_trait = avaricious
		if = { limit = { is_alive = yes } add_perk = golden_obligations_perk }
		if = { limit = { is_alive = yes } add_perk = heregeld_perk }
		if = { limit = { is_alive = yes } add_perk = detailed_ledgers_perk }
		if = { limit = { is_alive = yes } add_perk = war_profiteer_perk }
		if = { limit = { is_alive = yes } add_perk = fearful_troops_perk }
		if = { limit = { is_alive = yes } add_perk = at_any_cost_perk }
		if = { limit = { is_alive = yes } add_perk = it_is_my_domain_perk }
		if = { limit = { is_alive = yes } add_perk = golden_aplomb_perk }
		if = { limit = { is_alive = yes } add_perk = avaricious_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_stewardship
		}
	}
	character:Maester_Kings_Landing_2 ?= { #Qyburn
		asoiaf_clear_personality_traits_effect = yes
		add_trait = diligent
		add_trait = inquisitive #ruthless
		add_trait = calm
		add_trait = callous
		add_trait = loyal
		add_trait = schemer
		add_trait = whole_of_body
		if = { limit = { is_alive = yes } add_perk = master_of_masses_perk }
		if = { limit = { is_alive = yes } add_perk = instructed_spies_perk }
		if = { limit = { is_alive = yes } add_perk = convincing_perk }
		if = { limit = { is_alive = yes } add_perk = charismatic_perk }
		if = { limit = { is_alive = yes } add_perk = little_birds_perk }
		if = { limit = { is_alive = yes } add_perk = innocent_children_perk }
		if = { limit = { is_alive = yes } add_perk = omerta_perk }
		if = { limit = { is_alive = yes } add_perk = sleeper_cells_perk }
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = court_of_shadows_perk }
		if = { limit = { is_alive = yes } add_perk = prepared_for_anything_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = twice_schemed_perk }
		if = { limit = { is_alive = yes } add_perk = schemer_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = anatomical_studies_perk }
		if = { limit = { is_alive = yes } add_perk = carefree_perk }
		if = { limit = { is_alive = yes } add_perk = mental_resilience_perk }
		if = { limit = { is_alive = yes } add_perk = restraint_perk }
		if = { limit = { is_alive = yes } add_perk = know_thyself_perk }
		if = { limit = { is_alive = yes } add_perk = wash_your_hands_perk }
		if = { limit = { is_alive = yes } add_perk = iron_constitution_perk }
		if = { limit = { is_alive = yes } add_perk = healthy_perk }
		if = { limit = { is_alive = yes } add_perk = whole_of_body_perk }
		if = { limit = { is_alive = yes } add_perk = scientific_perk }
		if = { limit = { is_alive = yes } add_perk = planned_cultivation_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_learning
		}
		if = {
			limit = {
				current_date >= 8299.10.1 #Qyburn comes to the capital and serves Cersei
			}
			set_relation_friend = character:Lannister_6 #Cersei Lannister (daughter of Tywin)
		}
	}
	character:Karstark_4 ?= { #Rickard Karstark (son of Brandon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_martial_3
		add_trait = vengeful
		add_trait = stubborn
		add_trait = wrathful
		add_trait = brave
		add_trait = education_martial_prowess_3
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Umber_8 ?= { #Jon Umber (Greatjon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_good_3
		add_trait = education_martial_2
		add_trait = gregarious
		add_trait = forgiving
		add_trait = brave
		add_trait = wrathful
		add_trait = strong
		add_trait = aggressive_attacker
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = befriend_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Umber_11 ?= { #Jon Umber (Smalljon)
		asoiaf_clear_personality_traits_effect = yes #in the SHOW, his personality traits would be: ambitious, callous, authoritative, honest (or arrogant?)
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = physique_good_2
		add_trait = education_martial_3
		add_trait = brave
		add_trait = honest
		add_trait = just
		add_trait = authoritative #to blend in a bit of his show authoritativeness
		add_trait = shrewd #to blend in a bit of his show cunning
		add_trait = aggressive_attacker #to blend in a bit of his show aggressiveness
		add_trait = education_martial_prowess_4
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk } #to blend in a bit of his show dreadfulness
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
	}
	character:Poole_3 ?= { #Jeyne Poole (daughter of Vayon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = shy
		add_trait = generous
		add_trait = content
		add_trait = patient
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = pensive
		}
	}
	character:Poole_Dummy_3 ?= { #Jeyne Poole DUMMY (daughter of Vayon)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		add_trait = education_diplomacy_1
		add_trait = shy
		add_trait = generous
		add_trait = content
		add_trait = patient
		if = {
			limit = { age < 16 }
			set_focus = education_diplomacy
			add_trait = pensive
		}
	}
	character:Dormand_10 ?= { #Jon Dormund -> Artys Dormand
		add_trait = knight
	}
	character:Stark_7 ?= { #Jon Snow
		asoiaf_clear_education_traits_effect = yes
		asoiaf_clear_personality_traits_effect = yes
		add_trait = education_martial_2
		add_trait = beauty_good_2
		add_trait = brave
		add_trait = just
		add_trait = honest
		add_trait = trusting #honorable
		add_trait = education_martial_prowess_3
		if = { limit = { is_alive = yes } add_perk = stalwart_leader_perk }
		if = { limit = { is_alive = yes } add_perk = chivalric_dominance_perk }
		if = { limit = { is_alive = yes } add_perk = never_back_down_perk }
		if = { limit = { is_alive = yes } add_perk = meritocracy_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { age < 16 }
			set_focus = education_martial
		}
		if = {
			limit = { #guarantees he gets martial_4 on book bookmarks
				age >= 14
				age < 16
			}
			change_age = 2
			add_trait = education_martial_2
			change_age = -2
		}
	}
	character:Bolton_5 ?= { #Ramsay Bolton (bastard son of Roose)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		if = {
			limit = { has_trait = lunatic_1 } #he's not crazy, just cruel and sadistic
			remove_trait = lunatic_1
		}
		add_trait = beauty_good_2
		add_trait = sadistic
		add_trait = ambitious
		add_trait = arbitrary
		add_trait = deceitful
		add_trait = disloyal
		add_trait = reckless
		add_trait = education_martial_prowess_2
		add_trait = torturer
		if = { limit = { is_alive = yes } add_perk = truth_is_relative_perk }
		if = { limit = { is_alive = yes } add_perk = digging_for_dirt_perk }
		if = { limit = { is_alive = yes } add_perk = kidnapper_perk }
		if = { limit = { is_alive = yes } add_perk = swift_execution_perk }
		if = { limit = { is_alive = yes } add_perk = a_job_done_right_perk }
		if = { limit = { is_alive = yes } add_perk = dark_insights_perk }
		if = { limit = { is_alive = yes } add_perk = divine_retribution_perk }
		if = { limit = { is_alive = yes } add_perk = dreadful_perk }
		if = { limit = { is_alive = yes } add_perk = thriving_in_chaos_perk }
		if = { limit = { is_alive = yes } add_perk = malice_implicit_perk }
		if = { limit = { is_alive = yes } add_perk = fear_tax_perk }
		if = { limit = { is_alive = yes } add_perk = forever_infamous_perk }
		if = { limit = { is_alive = yes } add_perk = prison_feudal_complex_perk }
		if = { limit = { is_alive = yes } add_perk = torturer_perk }
		add_intrigue_skill = 4 #will all the duplicity he gets up to, he's clearly a good schemer; makes it more likely to succeed in murdering any siblings
		if = {
			limit = { age < 16 }
			set_focus = education_intrigue
		}
		if = {
			limit = {
				character:Bolton_hh_10 ?= { is_alive = yes }
			}
			set_relation_friend = character:Bolton_hh_10 #Locke; they seemed like buddies from what we saw
		}
		if = {
			limit = {
				character:Bolton_4 ?= { is_alive = yes } #if Domeric Bolton is alive
			}
			set_relation_grudge = { reason = asoiaf_ramsay_sibling_grudge_desc target = character:Bolton_4 } #Ramsay will hold a grudge
		}
	}
	character:Wylde_27 ?= { #Jasper Wylde (son of Gawen)
		asoiaf_clear_personality_traits_effect = yes
		asoiaf_clear_inherited_traits_effect = yes
		asoiaf_clear_education_traits_effect = yes
		add_trait = education_diplomacy_3
		add_trait = fecund
		add_trait = callous
		add_trait = honest
		add_trait = stubborn
		add_trait = lustful
		add_trait = shrewd
		add_trait = education_martial_prowess_2
		if = { limit = { is_alive = yes } add_perk = serve_the_crown_perk }
		if = { limit = { is_alive = yes } add_perk = strict_organization_perk }
		if = { limit = { is_alive = yes } add_perk = hard_rule_perk }
		if = { limit = { is_alive = yes } add_perk = firm_hand_perk }
		if = {
			limit = { has_nickname = nick_the_ironrod } #it's "Ironrod", not "the Ironrod"
			remove_nickname = yes
			give_nickname = nick_ironrod
		}
	}
	character:dragon_sunfyre ?= { #Sunfyre
		asoiaf_clear_dragon_personality_traits_effect = yes
		asoiaf_clear_dragon_inherited_traits_effect = yes
		asoiaf_clear_dragon_education_traits_effect = yes
		add_trait = education_dragon_5
		add_trait = dragon_majestic
		add_trait = dragon_supporting
		add_trait = dragon_friendly
		add_trait = dragon_accepting
	}
	character:dragon_vermax ?= { #Vermax
		asoiaf_clear_dragon_personality_traits_effect = yes
		asoiaf_clear_dragon_inherited_traits_effect = yes
		asoiaf_clear_dragon_education_traits_effect = yes
		add_trait = education_dragon_3
		add_trait = dragon_solitary
		add_trait = dragon_aggressive
		add_trait = dragon_impulsive
	}
	character:dragon_shrykos ?= { #Shrykos
	asoiaf_clear_dragon_personality_traits_effect = yes
		asoiaf_clear_dragon_inherited_traits_effect = yes
		add_trait = dragon_spindly
		add_trait = dragon_ugly
		add_trait = dragon_aggressive
		add_trait = dragon_bloodthirsty
		add_trait = dragon_impulsive
	}
	character:dragon_tyraxes ?= { #Tyraxes
		set_father = character:dragon_caraxes
	}
	character:dragon_stormcloud ?= { #Stormcloud
		set_father = character:dragon_caraxes
	}
}

asoiaf_setup_health_fertility_effect = {
	every_living_character = {
		limit = {
			NOR = {
				is_human = no
				has_trait = infertile
				has_trait = physique_bad_3
				has_trait = infirm
				has_trait = incapable
				AND = {
					exists = character:asoiaf_Hammer_2 #Hugh/Kat's daughter
					this = character:asoiaf_Hammer_2
				}
				AND = {
					exists = character:Targaryen_128 #Shaera Targaryen (daughter of Aegon V)
					this = character:Targaryen_128
				}
				AND = {
					exists = character:Targaryen_1 #Aerys II Targaryen
					this = character:Targaryen_1
					current_date > 8277.1.1 #after Viserys is born limit the children
				}
				AND = {
					exists = character:Targaryen_2 #Rhaella Targaryen (daughter of Jaehaerys II)
					this = character:Targaryen_2
					current_date > 8277.1.1 #after Viserys is born limit the children
				}
				#AND = {
				#	exists = character:Targaryen_3 #Rhaegar Targaryen (son of Aerys II)
				#	this = character:Targaryen_3
				#	current_date > 8282.1.1 #after Aegon is born limit the children
				#}
				#AND = { #disabled since the Canon Children system terminates her pregnancies with Robert automatically now
				#	exists = character:Lannister_6 #Cersei Lannister (daughter of Tywin)
				#	this = character:Lannister_6
				#	current_date > 8284.1.1
				#}
				AND = {
					exists = character:Baratheon_2 #Robert I Baratheon
					this = character:Baratheon_2
					current_date > 8284.1.1
				}
				AND = {
					exists = character:Lannister_129 #Tytos Lannister (son of Gerold)
					this = character:Lannister_129
					exists = character:Lannister_4 #Tygett is born
				}
				AND = {
					exists = character:Hightower_9 #Alerie Hightower (daughter of Leyton)
					this = character:Hightower_9
					current_date > 8284.1.1 #once Margaery is born limit the children
				}
			}
		}
		add_character_modifier = {
			modifier = asoiaf_fertility_health_boost
			years = 10
		}
	}
}

asoiaf_setup_coa_fixes_effect = {
	trigger_event = { id = asoiaf_setup_events.2 days = 1 }
}

asoiaf_alternative_dayne_sigil_effect = {
	dynasty:dynn_Dayne ?= { set_coa = dynn_Dayne_alt }
	title:k_the_torrentine ?= { set_coa = dynn_Dayne_alt }
	house:house_DayneHH ?= { set_coa = house_DayneHH_alt }
	house:house_Aster ?= { set_coa = house_Aster_alt }
}

asoiaf_sexuality_fix_effect = {
	character:Targaryen_45 ?= { #Aemon Targaryen (son of Jaehaerys I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_46 ?= { #Baelon Targaryen (son of Jaehaerys I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_47 ?= { #Alyssa Targaryen (daughter of Jaehaerys I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_60 ?= { #Viserys I Targaryen
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_61 ?= { #Daemon Targaryen (son of Baelon)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Arryn_84 ?= { #Jeyne Arryn (daughter of Elbert)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_homosexual
		set_sexuality = homosexual
	}
	character:Velaryon_31 ?= { #Corlys Velaryon (the Sea Snake)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Velaryon_34 ?= { #Laena Velaryon (daughter of Corlys)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Velaryon_35 ?= { #Laenor Velaryon (son of Corlys)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_homosexual
		set_sexuality = homosexual
	}
	character:Velaryon_43 ?= { #Addam Velaryon (son of Corlys)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Velaryon_44 ?= { #Alyn Velaryon (son of Corlys)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Florent_8 ?= { #Melessa Florent (daughter of Alester)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tarly_1 ?= { #Randyll Tarly (son of Talbert)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Hollard_10 ?= { #Dontos Hollard (the Red)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tarly_2 ?= { #Samwell Tarly (son of Randyll)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tarly_3 ?= { #Talla Tarly (daughter of Randyll)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tarly_6 ?= { #Dickon Tarly (son of Randyll)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Lonmouth_16 ?= { #Joffrey Lonmouth (son of Alys)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_homosexual
		set_sexuality = homosexual
	}
	character:Palehair_hh_1 ?= { #Dyanna
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_homosexual
		set_sexuality = homosexual
	}
	character:Palehair_hh_2 ?= { #Sylvenna Sand
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_homosexual
		set_sexuality = homosexual
	}
	character:Targaryen_63 ?= { #Rhaenyra Targaryen (daughter of Viserys I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_hh_3 ?= { #Mysaria the White Worm
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_85 ?= { #Elaena Targaryen (daughter of Aegon III)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Strong_30 ?= { #Harwrin Strong (son of Lyonel)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Strong_31 ?= { #Larys Strong (son of Lyonel)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
		add_secret = { type = secret_deviant } #likes feet
	}
	character:Cole_11 ?= { #Criston Cole (the Kingmaker)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_65 ?= { #Aegon II Targaryen
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_67 ?= { #Aemond Targaryen (son of Viserys I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_75 ?= { #Jacaerys Velaryon (son of Rhaenyra)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_76 ?= { #Lucerys Velaryon (son of Rhaenyra)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Hightower_97 ?= { #Alicent Hightower (daughter of Otto)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_111 ?= { #Valarr Targaryen (son of Baelor)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_118 ?= { #Daeron Targaryen (son of Maekar I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_121 ?= { #Daella Targaryen (daughter of Maekar I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Stark_5 ?= { #Lyanna Stark (daughter of Rickard)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Stark_7 ?= { #Jon Snow (son of Rhaegar & Lyanna)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Poole_3 ?= { #Jeyne Poole (daughter of Vayon)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Poole_Dummy_3 ?= { #Jeyne Poole (daughter of Vayon) - DUMMY
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Arryn_3 ?= { #Jon Arryn (son of Jasper)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Bolton_5 ?= { #Ramsay Bolton (bastard of Roose)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_8 ?= { #Viserys Targaryen (son of Aerys II)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_9 ?= { #Daenerys Targaryen (daughter of Aerys II)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_1 ?= { #Aerys II Targaryen
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Targaryen_3 ?= { #Rhaegar Targaryen (son of Aerys II)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Giantsbane_1 ?= { #Tormund Giantsbane
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Rayder_1 ?= { #Mance Rayder (King Beyond the Wall)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Lannister_6 ?= { #Cersei Lannister (daughter of Tywin)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Greyjoy_6 ?= { #Euron Greyjoy (son of Quellon)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Moore_2 ?= { #Mandon Moore (son of Jon)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_asexual
		set_sexuality = asexual
	}
	character:Eunuch_1 ?= { #Varys the Spider
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_asexual
		set_sexuality = asexual
	}
	character:Trant_2 ?= { #Meryn Trant (son of Donnel)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
		add_secret = {
			type = secret_deviant #pedophile
		}
	}
	character:Greyjoy_13 ?= { #Asha/Yara Greyjoy (daughter of Balon)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_bisexual
		set_sexuality = bisexual
	}
	character:Martell_14 ?= { #Tyene Sand (bastard of Oberyn)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Manderly_7 ?= { #Wylis Manderly (son of Wyman)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Florent_10 ?= { #Selyse Florent (daughter of Ryam)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Saan_2 ?= { #Salladhor Saan (son of Samarro)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tyrell_4 ?= { #Luthor Tyrell (son of Garlan)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tyrell_8 ?= { #Mace Tyrell (son of Luthor)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Tyrell_14 ?= { #Margaery Tyrell (daughter of Mace)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Dayne_8 ?= { #Gerold Dayne (Darkstar)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Baratheon_3 ?= { #Stannis Baratheon (son of Steffon)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Baratheon_5 ?= { #Joffrey I Baratheon
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Baratheon_6 ?= { #Myrcella Baratheon (daughter of Robert I)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Baratheon_7 ?= { #Tommen I Baratheon
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Redwyne_4 ?= { #Paxter Redwyne (son of Denys)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_homosexual
		set_sexuality = homosexual
		add_secret = { type = secret_deviant } #"likes his boys *very* young"
	}
	character:Redwyne_5 ?= { #Horas Redwyne (son of Paxter)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
	character:Redwyne_6 ?= { #Hobber Redwyne (son of Paxter)
		asoiaf_sexuality_remove_flags_effect = yes
		add_character_flag = sexuality_ensure_heterosexual
		set_sexuality = heterosexual
	}
}

asoiaf_new_dnas_effect = {
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_valyrian_rule_on
		}
		character:Targaryen_27 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_27 }
		character:Targaryen_28 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_28 }
		character:Targaryen_29 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_29 }
		character:Targaryen_35 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_35 }
		character:Targaryen_36 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_36 }
		character:Targaryen_45 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_45 }
		character:Targaryen_46 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_46 }
		character:Targaryen_48 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_48 }
		character:Targaryen_50 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_50 }
		character:Targaryen_47 ?= {
			copy_inheritable_appearance_from = character:Targaryen_asoiaf_47
			remove_trait = heterochromia_eyes
			agot_remove_heterochromia_eyes_effect = yes
		}
		character:Targaryen_51 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_51 }
		character:Targaryen_59 ?= {
			copy_inheritable_appearance_from = character:Targaryen_asoiaf_59
			make_trait_inactive = cool_neutral_silver_hair #Rhaenys has silver hair in HotD
		}
		character:Targaryen_60 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_60 }
		character:Targaryen_61 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_61 }
		character:Targaryen_63 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_63 }
		character:Targaryen_65 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_65 }
		character:Targaryen_66 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_66 }
		character:Targaryen_67 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_67 }
		character:Targaryen_68 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_68 }
		character:Targaryen_69 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_69 }
		character:Targaryen_70 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_70 }
		character:Targaryen_71 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_71 }
		character:Targaryen_72 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_72 }
		character:Targaryen_73 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_73 }
		character:Targaryen_75 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_75 }
		character:Targaryen_76 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_76 }
		character:Targaryen_77 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_77 }
		character:Targaryen_78 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_78 }
		character:Targaryen_79 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_79 }
		character:Targaryen_81 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_81 }
		character:Targaryen_82 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_82 }
		character:Targaryen_83 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_83 }
		character:Targaryen_84 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_84 }
		character:Targaryen_85 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_85 }
		character:Targaryen_89 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_89 }
		character:Targaryen_90 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_90 }
		character:Targaryen_96 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_96 }
		character:Targaryen_107 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_107 }
		character:Targaryen_110 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_110 }
		character:Targaryen_111 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_111 }
		character:Targaryen_118 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_118 }
		character:Targaryen_119 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_119 }
		character:Targaryen_120 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_120 }
		character:Targaryen_122 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_122 }
		character:Targaryen_126 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_126 }
		character:Targaryen_hh_10 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_hh_10 }
		character:Targaryen_127 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_127 }
		character:Targaryen_128 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_128 }
		character:Targaryen_129 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_129 }
		character:Targaryen_130 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_130 }
		character:Targaryen_1 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_1 }
		character:Targaryen_2 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_2 }
		character:Targaryen_3 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_3 }
		character:Targaryen_8 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_8 }
		character:Targaryen_9 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_9 }
		character:Targaryen_10 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_10 }
		character:Targaryen_11 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_11 }
		character:Targaryen_12 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_12 }
		character:Velaryon_31 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_31 }
		character:Velaryon_34 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_34 }
		character:Velaryon_35 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_35 }
		character:Velaryon_43 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_43 }
		character:Velaryon_44 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_44 }
		character:Velaryon_2 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_2 }
		character:Velaryon_5 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_5 }
		character:Velaryon_3 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_3 }
		character:Velaryon_4 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_4 }
		character:Celtigar_24 ?= { copy_inheritable_appearance_from = character:Celtigar_asoiaf_24 }
		character:Hammer_1 ?= { copy_inheritable_appearance_from = character:Hammer_asoiaf_1 }
		character:White_1 ?= { copy_inheritable_appearance_from = character:asoiaf_White_1 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_north_rule_on
		}
		character:Stark_67 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_67 }
		character:Stark_84 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_84 }
		character:Stark_86 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_86 }
		character:Stark_1 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_1 }
		character:Stark_2 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_2 }
		character:Stark_3 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_3 }
		character:Stark_4 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_4 }
		character:Stark_5 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_5 }
		character:Stark_6 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_6 }
		character:Stark_7 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_7 }
		character:Stark_8 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_8 }
		character:Stark_Dummy_8 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_8 }
		character:Stark_9 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_9 }
		character:Stark_Dummy_9 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_9 }
		character:Stark_10 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_10 }
		character:Stark_Dummy_10 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_10 }
		character:Stark_11 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_11 }
		character:Stark_Dummy_11 ?= { copy_inheritable_appearance_from = character:Stark_asoiaf_11 }
		character:Stark_hh_2 ?= { copy_inheritable_appearance_from = character:Stark_hh_asoiaf_2 }
		character:Karstark_4 ?= { copy_inheritable_appearance_from = character:Karstark_asoiaf_4 }
		character:Umber_8 ?= { copy_inheritable_appearance_from = character:Umber_asoiaf_8 }
		character:Umber_11 ?= { copy_inheritable_appearance_from = character:Umber_asoiaf_11 }
		character:Poole_2 ?= { copy_inheritable_appearance_from = character:Poole_asoiaf_2 }
		character:Poole_3 ?= { copy_inheritable_appearance_from = character:Poole_asoiaf_3 }
		character:Poole_Dummy_3 ?= { copy_inheritable_appearance_from = character:Poole_asoiaf_3 }
		character:Cassel_3 ?= { copy_inheritable_appearance_from = character:Cassel_asoiaf_3 }
		character:Cassel_4 ?= { copy_inheritable_appearance_from = character:Cassel_asoiaf_4 }
		character:Reed_1 ?= { copy_inheritable_appearance_from = character:Reed_asoiaf_1 }
		character:Reed_2 ?= { copy_inheritable_appearance_from = character:Reed_asoiaf_2 }
		character:Reed_3 ?= { copy_inheritable_appearance_from = character:Reed_asoiaf_3 }
		character:Bolton_1 ?= { copy_inheritable_appearance_from = character:Bolton_asoiaf_1 }
		character:Bolton_4 ?= { copy_inheritable_appearance_from = character:Bolton_asoiaf_4 }
		character:Bolton_5 ?= { copy_inheritable_appearance_from = character:Bolton_asoiaf_5 }
		character:Mormont_2 ?= { copy_inheritable_appearance_from = character:Mormont_asoiaf_2 }
		character:Mormont_3 ?= { copy_inheritable_appearance_from = character:Mormont_asoiaf_3 }
		character:Manderly_6 ?= { copy_inheritable_appearance_from = character:Manderly_asoiaf_6 }
		character:Manderly_7 ?= { copy_inheritable_appearance_from = character:Manderly_asoiaf_7 }
		character:Manderly_8 ?= { copy_inheritable_appearance_from = character:Manderly_asoiaf_8 }
		character:Manderly_10 ?= { copy_inheritable_appearance_from = character:Manderly_asoiaf_10 }
		character:Forrester_2 ?= { copy_inheritable_appearance_from = character:Forrester_asoiaf_2 }
		character:Forrester_3 ?= { copy_inheritable_appearance_from = character:Forrester_asoiaf_3 }
		character:Forrester_4 ?= { copy_inheritable_appearance_from = character:Forrester_asoiaf_4 }
		character:Forrester_5 ?= { copy_inheritable_appearance_from = character:Forrester_asoiaf_5 }
		character:Degore_1 ?= { copy_inheritable_appearance_from = character:Degore_asoiaf_1 }
		character:Tuttle_4 ?= { copy_inheritable_appearance_from = character:Tuttle_asoiaf_4 }
		character:Tuttle_5 ?= { copy_inheritable_appearance_from = character:Tuttle_asoiaf_5 }
		character:Glover_4 ?= { copy_inheritable_appearance_from = character:Glover_asoiaf_4 }
		character:Glover_5 ?= { copy_inheritable_appearance_from = character:Glover_asoiaf_5 }
		character:Whitehill_1 ?= { copy_inheritable_appearance_from = character:Whitehill_asoiaf_1 }
		character:Whitehill_5 ?= { copy_inheritable_appearance_from = character:Whitehill_asoiaf_5 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_crownlands_rule_on
		}
		character:Slynt_1 ?= { copy_inheritable_appearance_from = character:Slynt_asoiaf_1 }
		character:Hollard_10 ?= { copy_inheritable_appearance_from = character:Hollard_asoiaf_10 }
		character:Blackwater_1 ?= { copy_inheritable_appearance_from = character:Blackwater_asoiaf_1 }
		character:Staunton_19 ?= { copy_inheritable_appearance_from = character:Staunton_asoiaf_19 }
		character:Darklyn_18 ?= { copy_inheritable_appearance_from = character:Darklyn_asoiaf_18 }
		character:Stokeworth_1 ?= { copy_inheritable_appearance_from = character:Stokeworth_asoiaf_1 }
		character:Stokeworth_2 ?= { copy_inheritable_appearance_from = character:Stokeworth_asoiaf_2 }
		character:Stokeworth_3 ?= { copy_inheritable_appearance_from = character:Stokeworth_asoiaf_3 }
		character:Stokeworth_4 ?= { copy_inheritable_appearance_from = character:Stokeworth_asoiaf_4 }
		character:Stokeworth_5 ?= { copy_inheritable_appearance_from = character:Stokeworth_asoiaf_5 }
		character:Stokeworth_6 ?= { copy_inheritable_appearance_from = character:Stokeworth_asoiaf_6 }
		character:Branfield_1 ?= { copy_inheritable_appearance_from = character:Branfield_asoiaf_1 }
		character:Branfield_2 ?= { copy_inheritable_appearance_from = character:Branfield_asoiaf_2 }
		character:Thorne_17 ?= { copy_inheritable_appearance_from = character:Thorne_asoiaf_17 }
		character:Thorne_3 ?= { copy_inheritable_appearance_from = character:Thorne_asoiaf_3 }
		character:Cargyll_11 ?= { copy_inheritable_appearance_from = character:Cargyll_asoiaf_11 }
		character:Cargyll_12 ?= { copy_inheritable_appearance_from = character:Cargyll_asoiaf_11 }
		character:Tall_1 ?= { copy_inheritable_appearance_from = character:Tall_asoiaf_1 }
		character:Pycelle_1 ?= { copy_inheritable_appearance_from = character:Pycelle_asoiaf_1 }
		character:Baratheon_hh_3 ?= { copy_inheritable_appearance_from = character:Cressen_asoiaf_1 }
		character:Stark_hh_1 ?= { copy_inheritable_appearance_from = character:Stark_hh_asoiaf_1 }
		character:Most_Devout_20 ?= { copy_inheritable_appearance_from = character:asoiaf_Most_Devout_20 }
		character:Most_Devout_21 ?= { copy_inheritable_appearance_from = character:asoiaf_Most_Devout_21 }
		character:Most_Devout_23 ?= { copy_inheritable_appearance_from = character:asoiaf_Most_Devout_23 }
		character:Maester_Kings_Landing_2 ?= { copy_inheritable_appearance_from = character:asoiaf_Maester_Kings_Landing_2 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_vale_rule_on
		}
		character:Arryn_80 ?= { copy_inheritable_appearance_from = character:Arryn_asoiaf_80 }
		character:Arryn_84 ?= { copy_inheritable_appearance_from = character:Arryn_asoiaf_84 }
		character:Arryn_3 ?= { copy_inheritable_appearance_from = character:Arryn_asoiaf_3 }
		character:Arryn_8 ?= { copy_inheritable_appearance_from = character:Arryn_asoiaf_8 }
		character:Arryn_hh_4 ?= { copy_inheritable_appearance_from = character:Arryn_hh_asoiaf_4 }
		character:Waynwood_5 ?= { copy_inheritable_appearance_from = character:Waynwood_asoiaf_5 }
		character:Hardyng_5 ?= { copy_inheritable_appearance_from = character:Hardyng_asoiaf_5 }
		character:Egen_3 ?= { copy_inheritable_appearance_from = character:Egen_asoiaf_3 }
		character:Baelish_2 ?= { copy_inheritable_appearance_from = character:Baelish_asoiaf_2 }
		character:Royce_37 ?= { copy_inheritable_appearance_from = character:Royce_asoiaf_37 }
		character:Royce_1 ?= { copy_inheritable_appearance_from = character:Royce_asoiaf_1 }
		character:Moore_2 ?= { copy_inheritable_appearance_from = character:Moore_asoiaf_2 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_riverlands_rule_on
		}
		character:Tully_1 ?= { copy_inheritable_appearance_from = character:Tully_asoiaf_1 }
		character:Tully_4 ?= { copy_inheritable_appearance_from = character:Tully_asoiaf_4 }
		character:Tully_5 ?= { copy_inheritable_appearance_from = character:Tully_asoiaf_4 }
		character:Tully_6 ?= { copy_inheritable_appearance_from = character:Tully_asoiaf_6 }
		character:Tully_7 ?= { copy_inheritable_appearance_from = character:Tully_asoiaf_7 }
		character:Tully_9 ?= { copy_inheritable_appearance_from = character:Tully_asoiaf_9 }
		character:Strong_28 ?= { copy_inheritable_appearance_from = character:Strong_asoiaf_28 }
		character:Strong_29 ?= { copy_inheritable_appearance_from = character:Strong_asoiaf_29 }
		character:Strong_34 ?= { copy_inheritable_appearance_from = character:Strong_asoiaf_34 }
		character:Strong_30 ?= { copy_inheritable_appearance_from = character:Strong_asoiaf_30 }
		character:Strong_31 ?= { copy_inheritable_appearance_from = character:Strong_asoiaf_31 }
		character:Frey_1 ?= { copy_inheritable_appearance_from = character:Frey_asoiaf_1 }
		character:Frey_50 ?= { copy_inheritable_appearance_from = character:Frey_asoiaf_50 }
		character:Frey_52 ?= { copy_inheritable_appearance_from = character:Frey_asoiaf_52 }
		character:Frey_82 ?= { copy_inheritable_appearance_from = character:Frey_asoiaf_82 }
		character:Tully_hh_4 ?= { copy_inheritable_appearance_from = character:asoiaf_Tully_hh_4 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_westerlands_rule_on
		}
		character:Lannister_90 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_90 }
		character:Lannister_91 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_91 }
		character:Lannister_129 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_129 }
		character:Lannister_1 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_1 }
		character:Lannister_2 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_2 }
		character:Lannister_3 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_3 }
		character:Lannister_4 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_4 }
		character:Lannister_5 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_5 }
		character:Lannister_6 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_6 }
		character:Lannister_7 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_7 }
		character:Lannister_8 ?= {
			copy_inheritable_appearance_from = character:Lannister_asoiaf_8
			remove_trait = heterochromia_eyes
			agot_remove_heterochromia_eyes_effect = yes
		}
		character:Lannister_9 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_9 }
		character:Lannister_10 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_10 }
		character:Lannister_11 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_11 }
		character:Lannister_12 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_12 }
		character:Lannister_17 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_17 }
		character:Lannister_24 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_24 }
		character:Lannister_134 ?= { copy_inheritable_appearance_from = character:Lannister_asoiaf_134 }
		character:Lannister_hh_4 ?= { copy_inheritable_appearance_from = character:asoiaf_Lannister_hh_4 }
		character:Clegane_3 ?= { copy_inheritable_appearance_from = character:Clegane_asoiaf_3 }
		character:Clegane_4 ?= { copy_inheritable_appearance_from = character:Clegane_asoiaf_4 }
		character:Reyne_63 ?= { copy_inheritable_appearance_from = character:Reyne_asoiaf_63 }
		character:Lefford_1 ?= { copy_inheritable_appearance_from = character:Lefford_asoiaf_1 }
		character:Lefford_2 ?= { copy_inheritable_appearance_from = character:Lefford_asoiaf_2 }
		character:Lefford_5 ?= { copy_inheritable_appearance_from = character:Lefford_asoiaf_5 }
		character:Westerling_23 ?= { copy_inheritable_appearance_from = character:Westerling_asoiaf_23 }
		character:Marbrand_4 ?= { copy_inheritable_appearance_from = character:Marbrand_asoiaf_4 }
		character:Broome_35 ?= { copy_inheritable_appearance_from = character:Broome_asoiaf_35 }
		character:Swyft_1 ?= { copy_inheritable_appearance_from = character:Swyft_asoiaf_1 }
		character:Payne_6 ?= { copy_inheritable_appearance_from = character:Payne_asoiaf_6 }
		character:Vikary_1 ?= { copy_inheritable_appearance_from = character:Vikary_asoiaf_1 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_reach_rule_on
		}
		character:Tyrell_4 ?= { copy_inheritable_appearance_from = character:Tyrell_asoiaf_4 }
		character:Tyrell_8 ?= { copy_inheritable_appearance_from = character:Tyrell_asoiaf_8 }
		character:Tyrell_11 ?= { copy_inheritable_appearance_from = character:Tyrell_asoiaf_11 }
		character:Tyrell_12 ?= { copy_inheritable_appearance_from = character:Tyrell_asoiaf_12 }
		character:Tyrell_13 ?= { copy_inheritable_appearance_from = character:Tyrell_asoiaf_13 }
		character:Tyrell_14 ?= { copy_inheritable_appearance_from = character:Tyrell_asoiaf_14 }
		character:Fossoway_New_Barrel_1 ?= { copy_inheritable_appearance_from = character:asoiaf_Fossoway_New_Barrel_1 }
		character:Redwyne_2 ?= { copy_inheritable_appearance_from = character:Redwyne_asoiaf_2 }
		character:Redwyne_4 ?= { copy_inheritable_appearance_from = character:asoiaf_Redwyne_4 }
		character:Redwyne_5 ?= { copy_inheritable_appearance_from = character:asoiaf_Redwyne_5 }
		character:Redwyne_6 ?= { copy_inheritable_appearance_from = character:asoiaf_Redwyne_5 }
		character:Redwyne_7 ?= { copy_inheritable_appearance_from = character:asoiaf_Redwyne_7 }
		character:Tarly_1 ?= { copy_inheritable_appearance_from = character:Tarly_asoiaf_1 }
		character:Tarly_2 ?= { copy_inheritable_appearance_from = character:Tarly_asoiaf_2 }
		character:Tarly_3 ?= { copy_inheritable_appearance_from = character:Tarly_asoiaf_3 }
		character:Tarly_4 ?= { copy_inheritable_appearance_from = character:Tarly_asoiaf_4 }
		character:Tarly_5 ?= { copy_inheritable_appearance_from = character:Tarly_asoiaf_5 }
		character:Tarly_6 ?= { copy_inheritable_appearance_from = character:Tarly_asoiaf_6 }
		character:Florent_2 ?= { copy_inheritable_appearance_from = character:Florent_asoiaf_2 }
		character:Florent_8 ?= { copy_inheritable_appearance_from = character:Florent_asoiaf_8 }
		character:Florent_10 ?= { copy_inheritable_appearance_from = character:Florent_asoiaf_10 }
		character:Ashford_24 ?= { copy_inheritable_appearance_from = character:Ashford_asoiaf_24 }
		character:Ashford_27 ?= { copy_inheritable_appearance_from = character:Ashford_asoiaf_27 }
		character:Hightower_89 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_89 }
		character:Hightower_90 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_90 }
		character:Hightower_92 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_92 }
		character:Hightower_97 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_97 }
		character:Hightower_99 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_99 }
		character:Hightower_3 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_3 }
		character:Hightower_5 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_5 }
		character:Hightower_7 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_7 }
		character:Hightower_9 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_9 }
		character:Hightower_14 ?= { copy_inheritable_appearance_from = character:Hightower_asoiaf_14 }
		character:Beesbury_21 ?= { copy_inheritable_appearance_from = character:Beesbury_asoiaf_21 }
		character:Oakheart_4 ?= { copy_inheritable_appearance_from = character:Oakheart_asoiaf_4 }
		character:Strickland_1 ?= { copy_inheritable_appearance_from = character:Strickland_asoiaf_1 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_stormlands_rule_on
		}
		character:Baratheon_34 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_34 }
		character:Baratheon_36 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_36 }
		character:Baratheon_50 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_50 }
		character:Baratheon_52 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_52 }
		character:Baratheon_71 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_71 }
		character:Baratheon_1 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_1 }
		character:Baratheon_2 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_2 }
		character:Baratheon_3 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_3 }
		character:Baratheon_4 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_4 }
		character:Baratheon_5 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_5 }
		character:Baratheon_6 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_6 }
		character:Baratheon_7 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_7 }
		character:Baratheon_10 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_10 }
		character:Baratheon_13 ?= { copy_inheritable_appearance_from = character:Baratheon_asoiaf_13 }
		character:Wylde_27 ?= { copy_inheritable_appearance_from = character:Wylde_asoiaf_27 }
		character:Estermont_44 ?= { copy_inheritable_appearance_from = character:Estermont_asoiaf_44 }
		character:Lonmouth_16 ?= { copy_inheritable_appearance_from = character:Lonmouth_asoiaf_16 }
		character:Tarth_1 ?= { copy_inheritable_appearance_from = character:Tarth_asoiaf_1 }
		character:Tarth_3 ?= { copy_inheritable_appearance_from = character:Tarth_asoiaf_3 }
		character:Tarth_6 ?= { copy_inheritable_appearance_from = character:Tarth_asoiaf_6 }
		character:Selmy_3 ?= { copy_inheritable_appearance_from = character:Selmy_asoiaf_3 }
		character:Trant_2 ?= { copy_inheritable_appearance_from = character:Trant_asoiaf_2 }
		character:Cole_11 ?= { copy_inheritable_appearance_from = character:Cole_asoiaf_11 }
		character:Seaworth_1 ?= { copy_inheritable_appearance_from = character:Seaworth_asoiaf_1 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_dorne_rule_on
		}
		character:Martell_4 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_4 }
		character:Martell_7 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_7 }
		character:Martell_8 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_8 }
		character:Martell_9 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_9 }
		character:Martell_10 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_10 }
		character:Martell_11 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_11 }
		character:Martell_12 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_12 }
		character:Martell_14 ?= { copy_inheritable_appearance_from = character:Martell_asoiaf_14 }
		character:Dayne_5 ?= { copy_inheritable_appearance_from = character:Dayne_asoiaf_5 }
		character:Dayne_4 ?= { copy_inheritable_appearance_from = character:Dayne_asoiaf_4 }
		character:Dayne_8 ?= { copy_inheritable_appearance_from = character:Dayne_asoiaf_8 }
		character:Santagar_6 ?= { copy_inheritable_appearance_from = character:Santagar_asoiaf_6 }
		character:Uller_6 ?= { copy_inheritable_appearance_from = character:Uller_asoiaf_6 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_iron_isles_rule_on
		}
		character:Greyjoy_5 ?= { copy_inheritable_appearance_from = character:Greyjoy_asoiaf_5 }
		character:Greyjoy_6 ?= {
			copy_inheritable_appearance_from = character:Greyjoy_asoiaf_6
			remove_trait = heterochromia_eyes
			agot_remove_heterochromia_eyes_effect = yes
		}
		character:Greyjoy_9 ?= { copy_inheritable_appearance_from = character:Greyjoy_asoiaf_9 }
		character:Greyjoy_13 ?= { copy_inheritable_appearance_from = character:Greyjoy_asoiaf_13 }
		character:Greyjoy_14 ?= { copy_inheritable_appearance_from = character:Greyjoy_asoiaf_14 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_beyond_the_wall_rule_on
		}
		character:Rayder_1 ?= { copy_inheritable_appearance_from = character:Rayder_asoiaf_1 }
		character:Giantsbane_1 ?= { copy_inheritable_appearance_from = character:Giantsbane_asoiaf_1 }
	}
	if = {
		limit = {
			has_game_rule = asoiaf_new_dnas_essos_rule_on
		}
		character:asoiaf_Melisandre_1 ?= { copy_inheritable_appearance_from = character:Melisandre_asoiaf_1 }
		character:Melisandre_1 ?= { copy_inheritable_appearance_from = character:Melisandre_asoiaf_1 }
		character:Baratheon_hh_7 ?= { copy_inheritable_appearance_from = character:Baratheon_hh_asoiaf_4 }
		character:Eunuch_1 ?= { copy_inheritable_appearance_from = character:Eunuch_asoiaf_1 }
		character:Brotherhood_8 ?= { copy_inheritable_appearance_from = character:Thoros_asoiaf_1 }
		character:Mopatis_1 ?= { copy_inheritable_appearance_from = character:Mopatis_asoiaf_1 }
		character:Stormcrows_1 ?= { copy_inheritable_appearance_from = character:Stormcrows_asoiaf_1 }
		character:Hotah_1_B ?= { copy_inheritable_appearance_from = character:Hotah_asoiaf_1 }
		character:Hotah_rs_2 ?= { #Areo Hotah's mother turns Summer Islander
			copy_inheritable_appearance_from = character:Hotah_asoiaf_1
			set_character_faith = faith:summer_gods_pan
			set_culture = culture:jhalai
		}
		character:Targaryen_hh_3 ?= { copy_inheritable_appearance_from = character:Mysaria_asoiaf_1 }
		character:Lowborn_Kings_Landing_1 ?= { copy_inheritable_appearance_from = character:Shae_asoiaf_1 }
		character:Saan_2 ?= {
			copy_inheritable_appearance_from = character:Saan_asoiaf_2
			if = {
				limit = { exists = character:Saan_asoiaf_2_mother }
				set_mother = character:Saan_asoiaf_2_mother #Salladhor's Summer Islander mother
			}
			add_trait = wild_oat
			character:Saan_asoiaf_2_mother ?= {
				add_trait = fornicator
			}
			character:Saan_2.father ?= {
				add_trait = adulterer
			}
		}
	}
}

asoiaf_hbo_velaryons_dna_effect = {
	if = {
		limit = { has_game_rule = asoiaf_hbo_velaryons_rule_on }
		character:Rosby_13 ?= { #Make Tanda Rosby a Summer Islander of a different dynasty
			change_first_name = Chatana
			set_mother = character:asoiaf_Corlys_grandmother
			set_father = character:asoiaf_Corlys_grandfather
			set_house =  character:asoiaf_Corlys_grandfather.house
			set_culture = character:asoiaf_Corlys_grandfather.culture
			set_character_faith = character:asoiaf_Corlys_grandfather.faith
			copy_inheritable_appearance_from = character:asoiaf_Corlys_grandmother

		}
		character:Velaryon_31 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_31_HBO }
		character:Velaryon_34 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_34_HBO }
		character:Velaryon_35 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_35_HBO }
		character:Velaryon_43 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_43_HBO }
		character:Velaryon_44 ?= { copy_inheritable_appearance_from = character:Velaryon_asoiaf_44_HBO }
		character:Targaryen_69 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_69_HBO }
		character:Targaryen_70 ?= { copy_inheritable_appearance_from = character:Targaryen_asoiaf_70_HBO }
	}
}