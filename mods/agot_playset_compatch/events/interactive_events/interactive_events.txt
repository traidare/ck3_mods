namespace = interactive
interactive.0013 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_defect_enemy_spokesman
				exists = var:miv_defect_enemy_batchwar
				exists = var:miv_defect_enemy_batchside
			}
			var:miv_defect_enemy_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_defect_enemy_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_defect_enemy_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_defect_enemy_fellows
						var:miv_defect_enemy_fellows >= 1
					}
				}
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_oathbreaker
					TITLE = miv_toast_defect_enemy_many
					TOOLTIP = miv_toast_defect_enemy_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_defect_enemy_fellows
				}
			}
			else = {
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_oathbreaker
					TITLE = miv_toast_defect_enemy_one
					TOOLTIP = miv_toast_defect_enemy_one_desc
				}
			}
		}
		remove_variable = miv_defect_enemy_spokesman
		remove_variable = miv_defect_enemy_pending
		remove_variable = miv_defect_enemy_batchwar
		remove_variable = miv_defect_enemy_batchside
	}
}

interactive.0019 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_defect_us_spokesman
				exists = var:miv_defect_us_batchwar
				exists = var:miv_defect_us_batchside
			}
			var:miv_defect_us_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_defect_us_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_defect_us_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_defect_us_fellows
						var:miv_defect_us_fellows >= 1
					}
				}
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_flush_vassal
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_defection
					TITLE = miv_toast_defect_us_many
					TOOLTIP = miv_toast_defect_us_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_defect_us_fellows
				}
			}
			else = {
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_flush_vassal
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_defection
					TITLE = miv_toast_defect_us_one
					TOOLTIP = miv_toast_defect_us_one_desc
				}
			}
		}
		remove_variable = miv_defect_us_spokesman
		remove_variable = miv_defect_us_pending
		remove_variable = miv_defect_us_batchwar
		remove_variable = miv_defect_us_batchside
	}
}

interactive.0014 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_refuse_spokesman
				exists = var:miv_refuse_batchwar
				exists = var:miv_refuse_batchside
			}
			var:miv_refuse_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_refuse_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_refuse_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_refuse_fellows
						var:miv_refuse_fellows >= 1
					}
				}
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_refusal
					TITLE = miv_toast_refuse_many
					TOOLTIP = miv_toast_refuse_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_refuse_fellows
				}
			}
			else = {
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_refusal
					TITLE = miv_toast_refuse_one
					TOOLTIP = miv_toast_refuse_one_desc
				}
			}
		}
		remove_variable = miv_refuse_spokesman
		remove_variable = miv_refuse_pending
		remove_variable = miv_refuse_batchwar
		remove_variable = miv_refuse_batchside
	}
}

interactive.0015 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_oath_spokesman
				exists = var:miv_oath_batchwar
				exists = var:miv_oath_batchside
			}
			var:miv_oath_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_oath_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_oath_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_oath_fellows
						var:miv_oath_fellows >= 1
					}
				}
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_oathbreaker
					TITLE = miv_toast_oath_many
					TOOLTIP = miv_toast_oath_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_oath_fellows
				}
			}
			else = {
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_oathbreaker
					TITLE = miv_toast_oath_one
					TOOLTIP = miv_toast_oath_one_desc
				}
			}
		}
		remove_variable = miv_oath_spokesman
		remove_variable = miv_oath_pending
		remove_variable = miv_oath_batchwar
		remove_variable = miv_oath_batchside
	}
}

interactive.0016 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_unfit_spokesman
				exists = var:miv_unfit_batchwar
				exists = var:miv_unfit_batchside
			}
			var:miv_unfit_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_unfit_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_unfit_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_unfit_fellows
						var:miv_unfit_fellows >= 1
					}
				}
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_indisposed
					TITLE = miv_toast_unfit_many
					TOOLTIP = miv_toast_unfit_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_unfit_fellows
				}
			}
			else = {
				interactive_bannermen_toast_side_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_indisposed
					TITLE = miv_toast_unfit_one
					TOOLTIP = miv_toast_unfit_one_desc
				}
			}
		}
		remove_variable = miv_unfit_spokesman
		remove_variable = miv_unfit_pending
		remove_variable = miv_unfit_batchwar
		remove_variable = miv_unfit_batchside
	}
}

interactive.0017 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_loyal_spokesman
				exists = var:miv_loyal_batchwar
				exists = var:miv_loyal_batchside
			}
			var:miv_loyal_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_loyal_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_loyal_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_loyal_fellows
						var:miv_loyal_fellows >= 1
					}
				}
				interactive_bannermen_toast_war_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_loyalist
					TITLE = miv_toast_loyal_many
					TOOLTIP = miv_toast_loyal_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_loyal_fellows
				}
			}
			else = {
				interactive_bannermen_toast_war_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_loyalist
					TITLE = miv_toast_loyal_one
					TOOLTIP = miv_toast_loyal_one_desc
				}
			}
		}
		remove_variable = miv_loyal_spokesman
		remove_variable = miv_loyal_pending
		remove_variable = miv_loyal_batchwar
		remove_variable = miv_loyal_batchside
	}
}

interactive.0018 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = var:miv_neutral_spokesman
				exists = var:miv_neutral_batchwar
				exists = var:miv_neutral_batchside
			}
			var:miv_neutral_batchwar = {
				save_scope_as = miv_batch_war
			}
			var:miv_neutral_batchside = {
				save_scope_as = miv_batch_side
			}
			var:miv_neutral_spokesman = {
				save_scope_as = miv_flush_vassal
			}
			if = {
				limit = {
					scope:miv_flush_vassal = {
						exists = var:miv_neutral_fellows
						var:miv_neutral_fellows >= 1
					}
				}
				interactive_bannermen_toast_war_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_neutrality
					TITLE = miv_toast_neutral_many
					TOOLTIP = miv_toast_neutral_many_desc
				}
				scope:miv_flush_vassal = {
					remove_variable = miv_neutral_fellows
				}
			}
			else = {
				interactive_bannermen_toast_war_effect = {
					WAR = scope:miv_batch_war
					SIDE_OF = scope:miv_batch_side
					VASSAL = scope:miv_flush_vassal
					TYPE = msg_war_neutrality
					TITLE = miv_toast_neutral_one
					TOOLTIP = miv_toast_neutral_one_desc
				}
			}
		}
		remove_variable = miv_neutral_spokesman
		remove_variable = miv_neutral_pending
		remove_variable = miv_neutral_batchwar
		remove_variable = miv_neutral_batchside
	}
}

interactive.0001 = {
	title = interactive.0001.title
	desc = interactive.0001.desc
	theme = war
	left_portrait = scope:defender
	right_portrait = scope:attacker
	option = {
		name = interactive.0001.a
		save_scope_as = recipient
		if = {
			limit = {
				is_vassal_or_below_of = scope:attacker
			}
			scope:war = {
				hidden_effect = {
					set_called_to = scope:recipient
				}
				add_attacker = prev
			}
			scope:attacker = {
				add_opinion = {
					modifier = grateful_opinion
					target = scope:recipient
					opinion = 10
				}
			}
		}
		else_if = {
			limit = {
				is_vassal_or_below_of = scope:defender
			}
			scope:war = {
				hidden_effect = {
					set_called_to = scope:recipient
				}
				add_defender = prev
			}
			scope:defender = {
				add_opinion = {
					modifier = grateful_opinion
					target = scope:recipient
					opinion = 10
				}
			}
		}
	}
	option = {
		name = interactive.0001.b
		save_scope_as = recipient
		if = {
			limit = {
				always = no
				scope:attacker = {
					interactive_bannermen_is_rebel_trigger = {
						LIEGE = scope:defender
					}
				}
			}
			hidden_effect = {
				scope:recipient = {
					save_scope_as = VASSAL
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = defect_enemy
					EVENT = 0013
					HOLDER = scope:defender
					WAR = scope:war
					SIDE_OF = scope:defender
					VASSAL = scope:recipient
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = defect_us
					EVENT = 0019
					HOLDER = scope:defender
					WAR = scope:war
					SIDE_OF = scope:recipient
					VASSAL = scope:recipient
				}
			}
		}
	}
	option = {
		name = interactive.0001.c
		save_scope_as = recipient
		if = {
			limit = {
				is_vassal_or_below_of = scope:attacker
			}
			scope:war = {
				hidden_effect = {
					set_called_to = scope:recipient
				}
				add_defender = prev
			}
			if = {
				limit = {
					has_game_rule = miv_oathbreakers_enabled
					scope:attacker = {
						NOT = {
							interactive_bannermen_is_rebel_trigger = {
								LIEGE = scope:defender
							}
						}
					}
				}
				scope:attacker = {
					add_opinion = {
						modifier = miv_oathbreaker
						target = scope:recipient
					}
				}
				hidden_effect = {
					scope:recipient = {
						save_scope_as = VASSAL
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = defect_enemy
						EVENT = 0013
						HOLDER = scope:defender
						WAR = scope:war
						SIDE_OF = scope:defender
						VASSAL = scope:recipient
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = defect_us
						EVENT = 0019
						HOLDER = scope:defender
						WAR = scope:war
						SIDE_OF = scope:recipient
						VASSAL = scope:recipient
					}
				}
			}
			else = {
				scope:attacker = {
					add_opinion = {
						modifier = betrayed_me_opinion
						target = scope:recipient
						opinion = -50
					}
				}
			}
			scope:defender = {
				add_opinion = {
					modifier = grateful_opinion
					target = scope:recipient
					opinion = 50
				}
			}
		}
		else_if = {
			limit = {
				is_vassal_or_below_of = scope:defender
			}
			scope:war = {
				hidden_effect = {
					set_called_to = scope:recipient
				}
				add_attacker = prev
			}
			if = {
				limit = {
					has_game_rule = miv_oathbreakers_enabled
					scope:attacker = {
						NOT = {
							interactive_bannermen_is_rebel_trigger = {
								LIEGE = scope:defender
							}
						}
					}
				}
				scope:defender = {
					add_opinion = {
						modifier = miv_oathbreaker
						target = scope:recipient
					}
				}
				hidden_effect = {
					scope:recipient = {
						save_scope_as = VASSAL
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = defect_enemy
						EVENT = 0013
						HOLDER = scope:defender
						WAR = scope:war
						SIDE_OF = scope:defender
						VASSAL = scope:recipient
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = defect_us
						EVENT = 0019
						HOLDER = scope:defender
						WAR = scope:war
						SIDE_OF = scope:recipient
						VASSAL = scope:recipient
					}
				}
			}
			else = {
				scope:defender = {
					add_opinion = {
						modifier = betrayed_me_opinion
						target = scope:recipient
						opinion = -50
					}
				}
			}
			scope:attacker = {
				add_opinion = {
					modifier = grateful_opinion
					target = scope:recipient
					opinion = 50
				}
			}
			if = {
				limit = {
					exists = scope:attacker.joined_faction
					scope:attacker = {
						interactive_bannermen_is_rebel_trigger = {
							LIEGE = scope:defender
						}
					}
				}
				join_faction_skip_check = scope:attacker.joined_faction
			}
		}
	}
}

interactive.0003 = {
	type = letter_event
	sender = scope:actor
	opening = vassal_interaction.0001.opening
	desc = vassal_interaction.0001.desc
	immediate = {
		scope:target = {
			primary_defender = {
				save_scope_as = defender
			}
		}
		save_scope_value_as = {
			name = response
			value = flag:gold
		}
	}
	option = {
		name = vassal_interaction.0001.a
		show_as_tooltip = {
			scope:target = {
				end_war = white_peace
			}
			scope:actor = {
				add_opinion = {
					target = root
					modifier = pleased_opinion
					opinion = low_positive_opinion
				}
			}
		}
		scope:actor = {
			trigger_event = {
				id = vassal_interaction.0002
				days = {
					2
					5
				}
			}
		}
		ai_chance = {
			base = 0
			modifier = {
				add = 100
				scope:response = flag:accept
			}
		}
	}
	option = {
		name = vassal_interaction.0001.b
		trigger = {
			can_add_hook = {
				target = scope:actor
				type = favor_hook
			}
		}
		custom_tooltip = vassal_interaction.0001.request_favor_tt
		show_as_tooltip = {
			random_list = {
				1 = {
					desc = vassal_interaction.0001.accept
					show_chance = no
					scope:target = {
						end_war = white_peace
					}
					add_hook = {
						target = scope:actor
						type = favor_hook
					}
				}
				1 = {
					desc = vassal_interaction.0001.decline
					show_chance = no
					add_prestige = medium_prestige_loss
				}
			}
		}
		scope:actor = {
			trigger_event = {
				id = vassal_interaction.0003
				days = {
					2
					5
				}
			}
		}
		ai_chance = {
			base = 0
			modifier = {
				add = 100
				scope:response = flag:favor
			}
		}
	}
	option = {
		name = vassal_interaction.0001.c
		custom_tooltip = vassal_interaction.0001.request_gold_tt
		show_as_tooltip = {
			random_list = {
				1 = {
					desc = vassal_interaction.0001.accept
					show_chance = no
					scope:target = {
						end_war = white_peace
					}
					scope:actor = {
						pay_short_term_gold = {
							target = root
							gold = scope:recipient.stop_war_attacker_bribe_size
						}
					}
				}
				1 = {
					desc = vassal_interaction.0001.decline
					show_chance = no
					add_prestige = medium_prestige_loss
				}
			}
		}
		scope:actor = {
			trigger_event = {
				id = vassal_interaction.0004
				days = {
					2
					5
				}
			}
		}
		ai_chance = {
			base = 0
			modifier = {
				add = 100
				scope:response = flag:gold
			}
		}
	}
	option = {
		name = vassal_interaction.0001.d
		trigger = {
			NOT = {
				has_usable_hook = scope:actor
			}
		}
		show_as_tooltip = {
			scope:actor = {
				add_opinion = {
					target = root
					modifier = refusal_opinion
					opinion = low_negative_opinion
				}
			}
		}
		scope:actor = {
			trigger_event = {
				id = vassal_interaction.0005
				days = {
					2
					5
				}
			}
		}
		ai_chance = {
			base = 0
			modifier = {
				add = 100
				scope:response = flag:refuse
			}
		}
	}
	option = {
		name = vassal_interaction.0001.e
		trigger = {
			has_usable_hook = scope:actor
		}
		show_as_tooltip = {
			use_hook = scope:actor
		}
		save_scope_value_as = {
			name = vassal_used_hook
			value = yes
		}
		scope:actor = {
			trigger_event = {
				id = vassal_interaction.0005
				days = {
					2
					5
				}
			}
		}
		ai_chance = {
			base = 0
			modifier = {
				add = 100
				scope:response = flag:refuse
			}
		}
	}
}

interactive.0004 = {
	hidden = yes
	immediate = {
		save_temporary_scope_as = vassal
		scope:miv_bannermen_liege ?= {
			if = {
				limit = {
					NOT = {
						this = scope:vassal
					}
					is_alive = yes
					scope:vassal = {
						is_alive = yes
					}
				}
				save_temporary_scope_as = liege_to_join
				every_character_war = {
					save_temporary_scope_as = war
					if = {
						limit = {
							scope:war = {
								any_war_attacker = {
									this = scope:liege_to_join
								}
							}
							scope:war = {
								interactive_bannermen_callable_war_trigger = {
									LIEGE = scope:liege_to_join
								}
							}
							OR = {
								scope:war = {
									primary_attacker = scope:liege_to_join
								}
								scope:liege_to_join = {
									is_ai = no
								}
								AND = {
									has_game_rule = bannermen_extended_mode_1_enabled
									scope:liege_to_join = {
										highest_held_title_tier >= tier_empire
									}
								}
								AND = {
									has_game_rule = bannermen_extended_mode_2_enabled
									scope:liege_to_join = {
										highest_held_title_tier >= tier_kingdom
									}
								}
								AND = {
									has_game_rule = bannermen_extended_mode_3_enabled
									scope:liege_to_join = {
										highest_held_title_tier >= tier_duchy
									}
								}
								scope:war = {
									interactive_agot_megawar_empire_override_trigger = {
										LIEGE = scope:liege_to_join
									}
								}
							}
						}
						save_scope_as = miv_muster_war
						if = {
							limit = {
								scope:vassal = {
									NOT = {
										is_at_war_with = scope:war.primary_defender
									}
								}
							}
							scope:war = {
								if = {
									limit = {
										NOT = {
											any_war_defender = {
												is_at_war_with = scope:vassal
											}
										}
										NOR = {
											any_war_attacker = {
												this = scope:vassal
											}
											any_war_defender = {
												this = scope:vassal
											}
										}
									}
									add_attacker = scope:vassal
									scope:vassal = {
										interactive_bannermen_toll_pay_effect = {
											LIEGE = scope:liege_to_join
											WAR = scope:war
										}
									}
								}
							}
						}
					}
					else_if = {
						limit = {
							scope:war = {
								any_war_defender = {
									this = scope:liege_to_join
								}
							}
							scope:war = {
								interactive_bannermen_callable_war_trigger = {
									LIEGE = scope:liege_to_join
								}
							}
							OR = {
								scope:war = {
									primary_defender = scope:liege_to_join
								}
								scope:liege_to_join = {
									is_ai = no
								}
								AND = {
									has_game_rule = bannermen_extended_mode_1_enabled
									scope:liege_to_join = {
										highest_held_title_tier >= tier_empire
									}
								}
								AND = {
									has_game_rule = bannermen_extended_mode_2_enabled
									scope:liege_to_join = {
										highest_held_title_tier >= tier_kingdom
									}
								}
								AND = {
									has_game_rule = bannermen_extended_mode_3_enabled
									scope:liege_to_join = {
										highest_held_title_tier >= tier_duchy
									}
								}
								scope:war = {
									interactive_agot_megawar_empire_override_trigger = {
										LIEGE = scope:liege_to_join
									}
								}
							}
						}
						save_scope_as = miv_muster_war
						if = {
							limit = {
								scope:vassal = {
									NOT = {
										is_at_war_with = scope:war.primary_attacker
									}
								}
							}
							scope:war = {
								if = {
									limit = {
										NOT = {
											any_war_attacker = {
												is_at_war_with = scope:vassal
											}
										}
										NOR = {
											any_war_attacker = {
												this = scope:vassal
											}
											any_war_defender = {
												this = scope:vassal
											}
										}
									}
									add_defender = scope:vassal
									scope:vassal = {
										interactive_bannermen_toll_pay_effect = {
											LIEGE = scope:liege_to_join
											WAR = scope:war
										}
									}
								}
							}
						}
					}
				}
				if = {
					limit = {
						any_character_war = {
							is_civil_war = no
							interactive_bannermen_callable_war_trigger = {
								LIEGE = scope:liege_to_join
							}
						}
						NOT = {
							any_character_war = {
								is_civil_war = no
								interactive_bannermen_callable_war_trigger = {
									LIEGE = scope:liege_to_join
								}
								any_war_participant = {
									this = scope:vassal
								}
							}
						}
						exists = scope:miv_muster_war
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = unfit
						EVENT = 0016
						HOLDER = scope:liege_to_join
						WAR = scope:miv_muster_war
						SIDE_OF = scope:liege_to_join
						VASSAL = scope:vassal
					}
				}
			}
		}
	}
}

interactive.0007 = {
	hidden = yes
	immediate = {
		save_temporary_scope_as = vassal
		scope:vassal.liege ?= {
			if = {
				limit = {
					NOT = {
						this = scope:vassal
					}
				}
				save_temporary_scope_as = liege_to_join
				every_character_war = {
					limit = {
						primary_attacker = {
							interactive_bannermen_is_rebel_trigger = {
								LIEGE = scope:liege_to_join
							}
							is_landed = yes
						}
						NOT = {
							any_war_participant = {
								this = scope:vassal
							}
						}
					}
					save_temporary_scope_as = war_to_join
					if = {
						limit = {
							has_game_rule = bannermen_mode_enabled
						}
						interactive_bannermen_civilwar_effect = yes
					}
					else_if = {
						limit = {
							scope:vassal = {
								is_capable_adult = yes
								is_imprisoned = no
								NOT = {
									AND = {
										exists = joined_faction
										NOT = {
											joined_faction = {
												OR = {
													faction_is_type = crown_loyalist_faction
													AND = {
														has_global_variable = AGOT_is_loaded
														faction_is_type = agot_loyalist_faction
													}
												}
											}
										}
									}
								}
								NOT = {
									scope:liege_to_join = {
										has_opinion_modifier = {
											modifier = miv_oathbreaker
											target = scope:vassal
										}
									}
								}
								NOT = {
									opinion = {
										target = scope:liege_to_join
										value <= -80
									}
								}
								NOT = {
									is_allied_to = scope:war_to_join.primary_attacker
								}
								OR = {
									has_relation_friend = scope:liege_to_join
									has_relation_best_friend = scope:liege_to_join
									has_relation_blood_brother = scope:liege_to_join
									has_relation_lover = scope:liege_to_join
									has_relation_soulmate = scope:liege_to_join
									has_relation_rival = scope:war_to_join.primary_attacker
									has_relation_nemesis = scope:war_to_join.primary_attacker
									opinion = {
										target = scope:liege_to_join
										value >= 40
									}
									opinion = {
										target = scope:war_to_join.primary_attacker
										value <= -50
									}
									is_allied_to = scope:liege_to_join
									ai_honor >= medium_positive_ai_value
									AND = {
										exists = joined_faction
										joined_faction = {
											OR = {
												faction_is_type = crown_loyalist_faction
												AND = {
													has_global_variable = AGOT_is_loaded
													faction_is_type = agot_loyalist_faction
												}
											}
										}
									}
									is_heir_of = scope:liege_to_join
									has_trait = loyal
									scope:liege_to_join = {
										has_strong_hook = scope:vassal
									}
								}
							}
						}
						scope:war_to_join = {
							if = {
								limit = {
									NOT = { any_war_participant = { this = scope:vassal } }
									NOT = {
										any_war_participant = {
											is_at_war_with = scope:vassal
										}
									}
								}
								if = {
									limit = {
										NOT = {
											any_war_attacker = {
												is_at_war_with = scope:vassal
											}
										}
									}
									add_defender = scope:vassal
								}
							}
						}
						if = {
							limit = {
								has_game_rule = civil_war_indirect_vassals_enabled
							}
							scope:vassal = {
								save_temporary_scope_as = vassal_liege_to_join
								every_vassal = {
									limit = {
										max_military_strength > interactive_minimum_vassal_strength
										highest_held_title_tier >= tier_county
										is_landed = yes
										NOT = {
											is_at_war_with = scope:war_to_join.primary_defender
										}
										NOT = {
											is_at_war_with = scope:war_to_join.primary_attacker
										}
										is_ai = yes
									}
									save_temporary_scope_as = vassals_vassal
									if = {
										limit = {
											scope:vassals_vassal = {
												is_capable_adult = yes
												is_imprisoned = no
												NOT = {
													scope:vassal_liege_to_join = {
														has_opinion_modifier = {
															modifier = miv_oathbreaker
															target = scope:vassals_vassal
														}
													}
												}
												OR = {
													has_relation_friend = scope:vassal_liege_to_join
													has_relation_best_friend = scope:vassal_liege_to_join
													has_relation_blood_brother = scope:vassal_liege_to_join
													has_relation_lover = scope:vassal_liege_to_join
													has_relation_soulmate = scope:vassal_liege_to_join
													has_relation_rival = scope:war_to_join.primary_attacker
													has_relation_nemesis = scope:war_to_join.primary_attacker
													opinion = {
														target = scope:vassal_liege_to_join
														value >= 40
													}
													opinion = {
														target = scope:war_to_join.primary_attacker
														value <= -50
													}
													is_allied_to = scope:vassal_liege_to_join
													ai_honor >= medium_positive_ai_value
													AND = {
														exists = joined_faction
														joined_faction = {
															OR = {
																faction_is_type = crown_loyalist_faction
																AND = {
																	has_global_variable = AGOT_is_loaded
																	faction_is_type = agot_loyalist_faction
																}
															}
														}
													}
													is_heir_of = scope:vassal_liege_to_join
													has_trait = loyal
													scope:vassal_liege_to_join = {
														has_strong_hook = scope:vassals_vassal
													}
												}
											}
										}
										scope:war_to_join = {
											if = {
												limit = {
													NOT = { any_war_participant = { this = scope:vassals_vassal } }
													NOT = {
														any_war_participant = {
															is_at_war_with = scope:vassals_vassal
														}
													}
												}
												if = {
													limit = {
														NOT = {
															any_war_attacker = {
																is_at_war_with = scope:vassals_vassal
															}
														}
													}
													add_defender = scope:vassals_vassal
												}
											}
										}
									}
								}
							}
						}
					}
					else_if = {
						limit = {
							scope:vassal = {
								is_capable_adult = yes
								is_imprisoned = no
								OR = {
									NOT = {
										has_dread_level_towards = {
											target = scope:liege_to_join
											level = 2
										}
									}
									AND = {
										exists = joined_faction
										NOT = {
											joined_faction = {
												OR = {
													faction_is_type = crown_loyalist_faction
													AND = {
														has_global_variable = AGOT_is_loaded
														faction_is_type = agot_loyalist_faction
													}
												}
											}
										}
									}
									is_allied_to = scope:war_to_join.primary_attacker
								}
								NOT = {
									opinion = {
										target = scope:war_to_join.primary_attacker
										value <= -80
									}
								}
								NOT = {
									is_allied_to = scope:liege_to_join
								}
								OR = {
									has_relation_friend = scope:war_to_join.primary_attacker
									has_relation_best_friend = scope:war_to_join.primary_attacker
									has_relation_blood_brother = scope:war_to_join.primary_attacker
									has_relation_lover = scope:war_to_join.primary_attacker
									has_relation_soulmate = scope:war_to_join.primary_attacker
									has_relation_rival = scope:liege_to_join
									has_relation_nemesis = scope:liege_to_join
									opinion = {
										target = scope:war_to_join.primary_attacker
										value >= 40
									}
									opinion = {
										target = scope:liege_to_join
										value <= -50
									}
									is_allied_to = scope:war_to_join.primary_attacker
									ai_honor <= medium_negative_ai_value
									AND = {
										exists = joined_faction
										NOT = {
											joined_faction = {
												OR = {
													faction_is_type = crown_loyalist_faction
													AND = {
														has_global_variable = AGOT_is_loaded
														faction_is_type = agot_loyalist_faction
													}
												}
											}
										}
									}
									is_heir_of = scope:war_to_join.primary_attacker
									has_trait = disloyal
									scope:war_to_join.primary_attacker = {
										has_strong_hook = scope:vassal
									}
									scope:liege_to_join = {
										has_opinion_modifier = {
											modifier = miv_oathbreaker
											target = scope:vassal
										}
									}
								}
							}
						}
						scope:war_to_join = {
							if = {
								limit = {
									NOT = { any_war_participant = { this = scope:vassal } }
									NOT = {
										any_war_participant = {
											is_at_war_with = scope:vassal
										}
									}
								}
								if = {
									limit = {
										NOT = {
											any_war_defender = {
												is_at_war_with = scope:vassal
											}
										}
									}
									add_attacker = scope:vassal
								}
							}
						}
						if = {
							limit = {
								exists = scope:war_to_join.primary_attacker.joined_faction
							}
							scope:vassal = {
								if = {
									limit = {
										exists = joined_faction
										NOT = {
											joined_faction = scope:war_to_join.primary_attacker.joined_faction
										}
									}
									leave_faction = joined_faction
								}
								join_faction_skip_check = scope:war_to_join.primary_attacker.joined_faction
							}
						}
						if = {
							limit = {
								has_game_rule = civil_war_indirect_vassals_enabled
							}
							scope:vassal = {
								save_temporary_scope_as = vassal_liege_to_join
								every_vassal = {
									limit = {
										max_military_strength > interactive_minimum_vassal_strength
										highest_held_title_tier >= tier_county
										is_landed = yes
										NOT = {
											is_at_war_with = scope:war_to_join.primary_defender
										}
										NOT = {
											is_at_war_with = scope:war_to_join.primary_attacker
										}
										is_ai = yes
									}
									save_temporary_scope_as = vassals_vassal
									if = {
										limit = {
											scope:vassals_vassal = {
												is_capable_adult = yes
												is_imprisoned = no
												NOT = {
													scope:vassal_liege_to_join = {
														has_opinion_modifier = {
															modifier = miv_oathbreaker
															target = scope:vassals_vassal
														}
													}
												}
												OR = {
													has_relation_friend = scope:vassal_liege_to_join
													has_relation_best_friend = scope:vassal_liege_to_join
													has_relation_blood_brother = scope:vassal_liege_to_join
													has_relation_lover = scope:vassal_liege_to_join
													has_relation_soulmate = scope:vassal_liege_to_join
													has_relation_rival = scope:war_to_join.primary_defender
													has_relation_nemesis = scope:war_to_join.primary_defender
													opinion = {
														target = scope:vassal_liege_to_join
														value >= 40
													}
													opinion = {
														target = scope:war_to_join.primary_defender
														value <= -50
													}
													is_allied_to = scope:vassal_liege_to_join
													ai_honor >= medium_positive_ai_value
													AND = {
														exists = joined_faction
														joined_faction = {
															OR = {
																faction_is_type = crown_loyalist_faction
																AND = {
																	has_global_variable = AGOT_is_loaded
																	faction_is_type = agot_loyalist_faction
																}
															}
														}
													}
													is_heir_of = scope:vassal_liege_to_join
													has_trait = loyal
													scope:vassal_liege_to_join = {
														has_strong_hook = scope:vassals_vassal
													}
												}
											}
										}
										scope:war_to_join = {
											if = {
												limit = {
													NOT = { any_war_participant = { this = scope:vassals_vassal } }
													NOT = {
														any_war_participant = {
															is_at_war_with = scope:vassals_vassal
														}
													}
												}
												if = {
													limit = {
														NOT = {
															any_war_defender = {
																is_at_war_with = scope:vassals_vassal
															}
														}
													}
													add_attacker = scope:vassals_vassal
												}
											}
										}
									}
								}
							}
						}
					}
					else = {
						interactive_bannermen_batch_toast_effect = {
							KEY = refuse
							EVENT = 0014
							HOLDER = scope:liege_to_join
							WAR = scope:war_to_join
							SIDE_OF = scope:liege_to_join
							VASSAL = scope:vassal
						}
					}
				}
			}
		}
	}
}

interactive.0008 = {
	hidden = yes
	immediate = {
		save_scope_as = VASSAL
		save_temporary_scope_as = vassal
		set_variable = {
			name = miv_betrayed
			value = yes
			days = 1
		}
		scope:miv_bannermen_liege ?= {
			if = {
				limit = {
					NOT = {
						this = scope:vassal
					}
					is_alive = yes
					scope:vassal = {
						is_alive = yes
					}
				}
				save_temporary_scope_as = betrayed_liege
				every_character_war = {
					save_temporary_scope_as = war
					if = {
						limit = {
							scope:vassal = {
								has_variable = miv_betrayed
							}
						}
						save_scope_as = miv_betrayal_war
					}
					if = {
						limit = {
							scope:war = {
								is_civil_war = no
								any_war_attacker = {
									this = scope:betrayed_liege
								}
								NOR = {
									any_war_attacker = {
										this = scope:vassal
									}
									any_war_defender = {
										this = scope:vassal
									}
								}
							}
							scope:vassal = {
								NOT = {
									is_at_war_with = scope:war.primary_attacker
								}
							}
						}
						scope:war = {
							if = {
								limit = {
									NOT = {
										any_war_attacker = {
											is_at_war_with = scope:vassal
										}
									}
								}
								add_defender = scope:vassal
								scope:vassal = {
									remove_variable = miv_betrayed
								}
							}
						}
					}
					else_if = {
						limit = {
							scope:war = {
								is_civil_war = no
								any_war_defender = {
									this = scope:betrayed_liege
								}
								NOR = {
									any_war_attacker = {
										this = scope:vassal
									}
									any_war_defender = {
										this = scope:vassal
									}
								}
							}
							scope:vassal = {
								NOT = {
									is_at_war_with = scope:war.primary_defender
								}
							}
						}
						scope:war = {
							if = {
								limit = {
									NOT = {
										any_war_defender = {
											is_at_war_with = scope:vassal
										}
									}
								}
								add_attacker = scope:vassal
								scope:vassal = {
									remove_variable = miv_betrayed
								}
							}
						}
					}
				}
				if = {
					limit = {
						scope:vassal = {
							NOT = {
								has_variable = miv_betrayed
							}
						}
						exists = scope:miv_betrayal_war
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = defect_enemy
						EVENT = 0013
						HOLDER = scope:betrayed_liege
						WAR = scope:miv_betrayal_war
						SIDE_OF = scope:betrayed_liege
						VASSAL = scope:vassal
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = defect_us
						EVENT = 0019
						HOLDER = scope:betrayed_liege
						WAR = scope:miv_betrayal_war
						SIDE_OF = scope:vassal
						VASSAL = scope:vassal
					}
				}
				else_if = {
					limit = {
						NOT = {
							has_opinion_modifier = {
								modifier = miv_oathbreaker
								target = scope:vassal
							}
						}
						exists = scope:miv_betrayal_war
					}
					interactive_bannermen_batch_toast_effect = {
						KEY = oath
						EVENT = 0015
						HOLDER = scope:betrayed_liege
						WAR = scope:miv_betrayal_war
						SIDE_OF = scope:betrayed_liege
						VASSAL = scope:vassal
					}
				}
				add_opinion = {
					target = scope:vassal
					modifier = miv_oathbreaker
				}
			}
		}
	}
}

interactive.0009 = {
	hidden = yes
	immediate = {
		if = {
			limit = {
				exists = scope:miv_support_war
				exists = scope:miv_support_liege
				exists = scope:miv_support_vassal
				scope:miv_support_liege = {
					is_alive = yes
				}
				scope:miv_support_vassal = {
					is_alive = yes
				}
				scope:miv_support_vassal = {
					NOT = {
						is_at_war_with = scope:miv_support_liege
					}
				}
				scope:miv_support_war = {
					NOR = {
						any_war_attacker = {
							this = scope:miv_support_vassal
						}
						any_war_defender = {
							this = scope:miv_support_vassal
						}
					}
					OR = {
						any_war_attacker = {
							this = scope:miv_support_liege
						}
						any_war_defender = {
							this = scope:miv_support_liege
						}
					}
				}
			}
			scope:miv_support_war = {
				if = {
					limit = {
						is_attacker = scope:miv_support_liege
					}
					if = {
						limit = {
							NOT = {
								any_war_defender = {
									is_at_war_with = scope:miv_support_vassal
								}
							}
						}
						add_attacker = scope:miv_support_vassal
						scope:miv_support_vassal = {
							interactive_bannermen_toll_pay_effect = {
								LIEGE = scope:miv_support_liege
								WAR = scope:miv_support_war
							}
						}
					}
				}
				else = {
					if = {
						limit = {
							NOT = {
								any_war_attacker = {
									is_at_war_with = scope:miv_support_vassal
								}
							}
						}
						add_defender = scope:miv_support_vassal
						scope:miv_support_vassal = {
							interactive_bannermen_toll_pay_effect = {
								LIEGE = scope:miv_support_liege
								WAR = scope:miv_support_war
							}
						}
					}
				}
			}
		}
		if = {
			limit = {
				exists = scope:miv_support_war
				exists = scope:miv_support_liege
				exists = scope:miv_support_vassal
				scope:miv_support_liege = {
					is_alive = yes
				}
				scope:miv_support_vassal = {
					is_alive = yes
				}
				scope:miv_support_war = {
					is_civil_war = no
					NOT = {
						any_war_participant = {
							this = scope:miv_support_vassal
						}
					}
				}
			}
			scope:miv_support_liege = {
				interactive_bannermen_batch_toast_effect = {
					KEY = unfit
					EVENT = 0016
					HOLDER = scope:miv_support_liege
					WAR = scope:miv_support_war
					SIDE_OF = scope:miv_support_liege
					VASSAL = scope:miv_support_vassal
				}
			}
		}
	}
}

interactive.0010 = {
	hidden = yes
	immediate = {
		save_temporary_scope_as = vassal
		if = {
			limit = {
				exists = scope:miv_cw_war
				exists = liege
				NOT = {
					liege = scope:vassal
				}
				scope:miv_cw_war = {
					NOR = {
						any_war_attacker = {
							this = scope:vassal
						}
						any_war_defender = {
							this = scope:vassal
						}
					}
				}
			}
			liege = {
				save_temporary_scope_as = liege
			}
			interactive_bannermen_civilwar_commit_effect = yes
			if = {
				limit = {
					NOT = {
						interactive_bannermen_fit_trigger = yes
					}
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = neutral
					EVENT = 0018
					HOLDER = scope:liege
					WAR = scope:miv_cw_war
					SIDE_OF = scope:liege
					VASSAL = scope:vassal
				}
			}
			else_if = {
				limit = {
					scope:miv_cw_war = {
						OR = {
							any_war_attacker = {
								this = scope:liege
							}
							any_war_defender = {
								this = scope:liege
							}
						}
					}
					interactive_bannermen_civilwar_loyal_trigger = {
						LIEGE = scope:liege
					}
				}
				scope:miv_cw_war = {
					if = {
						limit = {
							is_attacker = scope:liege
						}
						if = {
							limit = {
								NOT = {
									any_war_defender = {
										is_at_war_with = scope:vassal
									}
								}
							}
							add_attacker = scope:vassal
						}
					}
					else = {
						if = {
							limit = {
								NOT = {
									any_war_attacker = {
										is_at_war_with = scope:vassal
									}
								}
							}
							add_defender = scope:vassal
						}
					}
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = loyal
					EVENT = 0017
					HOLDER = scope:liege
					WAR = scope:miv_cw_war
					SIDE_OF = scope:liege
					VASSAL = scope:vassal
				}
				if = {
					limit = {
						has_game_rule = civil_war_indirect_vassals_enabled
						exists = var:miv_cw_depth
						var:miv_cw_depth < 2
					}
					save_temporary_scope_as = liege
					every_vassal = {
						limit = {
							interactive_bannermen_civilwar_eligible_trigger = {
								LIEGE = scope:liege
							}
						}
						save_temporary_scope_as = vassal
						interactive_bannermen_civilwar_cascade_effect = yes
					}
				}
			}
			else_if = {
				limit = {
					scope:miv_cw_war = {
						OR = {
							any_war_attacker = {
								this = scope:liege
							}
							any_war_defender = {
								this = scope:liege
							}
						}
					}
					interactive_bannermen_civilwar_neutral_trigger = {
						LIEGE = scope:liege
					}
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = neutral
					EVENT = 0018
					HOLDER = scope:liege
					WAR = scope:miv_cw_war
					SIDE_OF = scope:liege
					VASSAL = scope:vassal
				}
			}
			else_if = {
				limit = {
					scope:miv_cw_war = {
						OR = {
							any_war_attacker = {
								this = scope:liege
							}
							any_war_defender = {
								this = scope:liege
							}
						}
					}
				}
				scope:miv_cw_war = {
					if = {
						limit = {
							is_attacker = scope:liege
						}
						if = {
							limit = {
								NOT = {
									any_war_attacker = {
										is_at_war_with = scope:vassal
									}
								}
							}
							add_defender = scope:vassal
						}
					}
					else = {
						if = {
							limit = {
								NOT = {
									any_war_defender = {
										is_at_war_with = scope:vassal
									}
								}
							}
							add_attacker = scope:vassal
						}
					}
				}
				scope:liege = {
					if = {
						limit = {
							NOT = {
								has_opinion_modifier = {
									modifier = miv_oathbreaker
									target = scope:vassal
								}
							}
						}
						if = {
							limit = {
								NOT = {
									exists = var:miv_cw_betrayal_pending
								}
							}
							set_variable = {
								name = miv_cw_betrayal_pending
								value = yes
								days = 15
							}
							set_variable = {
								name = miv_cw_betrayer
								value = scope:vassal
								days = 15
							}
							set_variable = {
								name = miv_cw_betrayal_war
								value = scope:miv_cw_war
								days = 15
							}
							scope:vassal = {
								set_variable = {
									name = miv_fellow_rebels
									value = 0
									days = 15
								}
							}
							trigger_event = {
								id = interactive.0012
								days = 5
							}
						}
						else_if = {
							limit = {
								exists = var:miv_cw_betrayer
								exists = var:miv_cw_betrayal_war
								var:miv_cw_betrayal_war = scope:miv_cw_war
							}
							var:miv_cw_betrayer = {
								change_variable = {
									name = miv_fellow_rebels
									add = 1
								}
							}
						}
					}
					add_opinion = {
						target = scope:vassal
						modifier = miv_oathbreaker
					}
				}
				if = {
					limit = {
						exists = scope:miv_cw_war.primary_attacker.joined_faction
						is_vassal_of = scope:miv_cw_war.primary_defender
					}
					if = {
						limit = {
							exists = joined_faction
							NOT = {
								joined_faction = scope:miv_cw_war.primary_attacker.joined_faction
							}
						}
						leave_faction = joined_faction
					}
					join_faction_skip_check = scope:miv_cw_war.primary_attacker.joined_faction
				}
				if = {
					limit = {
						has_game_rule = civil_war_indirect_vassals_enabled
						exists = var:miv_cw_depth
						var:miv_cw_depth < 2
					}
					save_temporary_scope_as = liege
					every_vassal = {
						limit = {
							interactive_bannermen_civilwar_eligible_trigger = {
								LIEGE = scope:liege
							}
						}
						save_temporary_scope_as = vassal
						interactive_bannermen_civilwar_cascade_effect = yes
					}
				}
			}
		}
	}
}

interactive.0011 = {
	hidden = yes
	immediate = {
		save_temporary_scope_as = vassal
		scope:miv_bannermen_liege ?= {
			if = {
				limit = {
					NOT = {
						this = scope:vassal
					}
					is_alive = yes
					exists = scope:miv_bannermen_war
					scope:vassal = {
						is_alive = yes
					}
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = unfit
					EVENT = 0016
					HOLDER = scope:miv_bannermen_liege
					WAR = scope:miv_bannermen_war
					SIDE_OF = scope:miv_bannermen_liege
					VASSAL = scope:vassal
				}
			}
		}
	}
}

interactive.0020 = {
	hidden = yes
	immediate = {
		save_temporary_scope_as = vassal
		scope:miv_bannermen_liege ?= {
			if = {
				limit = {
					NOT = {
						this = scope:vassal
					}
					is_alive = yes
					exists = scope:miv_bannermen_war
					scope:vassal = {
						is_alive = yes
					}
				}
				interactive_bannermen_batch_toast_effect = {
					KEY = refuse
					EVENT = 0014
					HOLDER = scope:miv_bannermen_liege
					WAR = scope:miv_bannermen_war
					SIDE_OF = scope:miv_bannermen_liege
					VASSAL = scope:vassal
				}
			}
		}
	}
}

interactive.0012 = {
	hidden = yes
	immediate = {
		save_scope_as = liege
		if = {
			limit = {
				exists = var:miv_cw_betrayer
				exists = var:miv_cw_betrayal_war
			}
			var:miv_cw_betrayal_war = {
				save_scope_as = miv_cw_war
			}
			var:miv_cw_betrayer = {
				save_scope_as = VASSAL
			}
			if = {
				limit = {
					scope:VASSAL = {
						exists = var:miv_fellow_rebels
						var:miv_fellow_rebels >= 1
					}
				}
				interactive_bannermen_toast_war_effect = {
					WAR = scope:miv_cw_war
					SIDE_OF = scope:liege
					VASSAL = scope:VASSAL
					TYPE = msg_war_oathbreaker
					TITLE = miv_toast_rebel_many
					TOOLTIP = miv_toast_rebel_many_desc
				}
				scope:VASSAL = {
					remove_variable = miv_fellow_rebels
				}
			}
			else = {
				interactive_bannermen_toast_war_effect = {
					WAR = scope:miv_cw_war
					SIDE_OF = scope:liege
					VASSAL = scope:VASSAL
					TYPE = msg_war_oathbreaker
					TITLE = miv_toast_rebel_one
					TOOLTIP = miv_toast_rebel_one_desc
				}
			}
		}
		remove_variable = miv_cw_betrayal_pending
		remove_variable = miv_cw_betrayer
		remove_variable = miv_cw_betrayal_war
	}
}
