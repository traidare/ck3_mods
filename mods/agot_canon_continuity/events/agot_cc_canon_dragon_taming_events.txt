# Generated by workspace/agot_canon_continuity. Replays the canon rider-dragon
# bonds AGOT's tenth-birthday dispatch cannot reach, on the day each static
# history record gives. Every branch is a no-op unless the rider still has no
# dragon and the recorded dragon still has no rider.

namespace = agot_cc_canon_dragon

agot_cc_canon_dragon.0001 = {
	scope = character
	hidden = yes

	trigger = {
		agot_cc_canon_taming_pending_trigger = yes
	}

	immediate = {
		if = { # Belaerys_3 and dragon_terrax
			limit = {
				exists = character:Belaerys_3
				this = character:Belaerys_3
				current_date >= 7825.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = terrax }
		}
		else_if = { # Targaryen_13_B and dragon_anogaros
			limit = {
				has_character_flag = is_Targaryen_13_B
				current_date >= 7838.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = anogaros }
		}
		else_if = { # Targaryen_13_A and dragon_vermithrax
			limit = {
				has_character_flag = is_Targaryen_13_A
				current_date >= 7841.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vermithrax }
		}
		else_if = { # Targaryen_13_C and dragon_nessarion
			limit = {
				has_character_flag = is_Targaryen_13_C
				current_date >= 7856.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = nessarion }
		}
		else_if = { # Targaryen_13 and dragon_vermithrax
			limit = {
				has_character_flag = is_Targaryen_13
				current_date >= 7873.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vermithrax }
		}
		else_if = { # Sonaryen_1 and dragon_neltharion
			limit = {
				exists = character:Sonaryen_1
				this = character:Sonaryen_1
				current_date >= 7879.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = neltharion }
		}
		else_if = { # Mataeryon_1 and dragon_pythax
			limit = {
				exists = character:Mataeryon_1
				this = character:Mataeryon_1
				current_date >= 7885.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = pythax }
		}
		else_if = { # Varezys_1 and dragon_vassarion
			limit = {
				has_character_flag = is_Varezys_1
				current_date >= 7885.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vassarion }
		}
		else_if = { # Targaryen_15 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_15
				current_date >= 7887.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Urnebar_1 and dragon_rovax
			limit = {
				has_character_flag = is_Urnebar_1
				current_date >= 7887.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = rovax }
		}
		else_if = { # Targaryen_14 and dragon_anogaros
			limit = {
				has_character_flag = is_Targaryen_14
				current_date >= 7889.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = anogaros }
		}
		else_if = { # Targaryen_13_D and dragon_archonei
			limit = {
				has_character_flag = is_Targaryen_13_D
				current_date >= 7890.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = archonei }
		}
		else_if = { # Targaryen_16 and dragon_archonei
			limit = {
				has_character_flag = is_Targaryen_16
				current_date >= 7907.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = archonei }
		}
		else_if = { # Targaryen_18 and dragon_anogaros
			limit = {
				has_character_flag = is_Targaryen_18
				current_date >= 7930.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = anogaros }
		}
		else_if = { # Targaryen_21 and dragon_meraxes
			limit = {
				has_character_flag = is_Targaryen_21
				current_date >= 7930.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = meraxes }
		}
		else_if = { # Targaryen_17 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_17
				current_date >= 7932.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Targaryen_19 and dragon_archonei
			limit = {
				has_character_flag = is_Targaryen_19
				current_date >= 7935.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = archonei }
		}
		else_if = { # Targaryen_20 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_20
				current_date >= 7950.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Targaryen_22 and dragon_archonei
			limit = {
				has_character_flag = is_Targaryen_22
				current_date >= 7950.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = archonei }
		}
		else_if = { # Targaryen_24 and dragon_meraxes
			limit = {
				has_character_flag = is_Targaryen_24
				current_date >= 7953.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = meraxes }
		}
		else_if = { # Targaryen_23 and dragon_anogaros
			limit = {
				has_character_flag = is_Targaryen_23
				current_date >= 7954.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = anogaros }
		}
		else_if = { # Targaryen_26 and dragon_meraxes
			limit = {
				has_character_flag = is_Targaryen_26
				current_date >= 7971.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = meraxes }
		}
		else_if = { # Targaryen_27 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_27
				current_date >= 7986.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Targaryen_28 and dragon_vhagar
			limit = {
				has_character_flag = is_Targaryen_28
				current_date >= 7991.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vhagar }
		}
		else_if = { # Targaryen_29 and dragon_meraxes
			limit = {
				has_character_flag = is_Targaryen_29
				current_date >= 7996.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = meraxes }
		}
		else_if = { # Targaryen_30 and dragon_quicksilver
			limit = {
				has_character_flag = is_Targaryen_30
				current_date >= 8015.9.9
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = quicksilver }
		}
		else_if = { # Targaryen_32 and dragon_dreamfyre
			limit = {
				has_character_flag = is_Targaryen_32
				current_date >= 8037.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = dreamfyre }
		}
		else_if = { # Targaryen_31 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_31
				current_date >= 8037.9.9
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Targaryen_36 and dragon_silverwing
			limit = {
				has_character_flag = is_Targaryen_36
				current_date >= 8040.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = silverwing }
		}
		else_if = { # Targaryen_35 and dragon_vermithor
			limit = {
				has_character_flag = is_Targaryen_35
				current_date >= 8042.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vermithor }
		}
		else_if = { # Targaryen_33 and dragon_quicksilver
			limit = {
				has_character_flag = is_Targaryen_33
				current_date >= 8042.6.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = quicksilver }
		}
		else_if = { # Targaryen_41 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_41
				current_date >= 8054.12.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Targaryen_45 and dragon_caraxes
			limit = {
				has_character_flag = is_Targaryen_45
				current_date >= 8072.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = caraxes }
		}
		else_if = { # Targaryen_46 and dragon_vhagar
			limit = {
				has_character_flag = is_Targaryen_46
				current_date >= 8073.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vhagar }
		}
		else_if = { # Targaryen_47 and dragon_meleys
			limit = {
				has_character_flag = is_Targaryen_47
				current_date >= 8075.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = meleys }
		}
		else_if = { # Targaryen_59 and dragon_meleys
			limit = {
				has_character_flag = is_Targaryen_59
				current_date >= 8087.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = meleys }
		}
		else_if = { # Targaryen_60 and dragon_balerion
			limit = {
				has_character_flag = is_Targaryen_60
				current_date >= 8093.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = balerion }
		}
		else_if = { # Targaryen_61 and dragon_caraxes
			limit = {
				has_character_flag = is_Targaryen_61
				current_date >= 8099.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = caraxes }
		}
		else_if = { # Targaryen_63 and dragon_syrax
			limit = {
				has_character_flag = is_Targaryen_63
				current_date >= 8104.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = syrax }
		}
		else_if = { # Velaryon_34 and dragon_vhagar
			limit = {
				has_character_flag = is_Velaryon_34
				current_date >= 8104.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vhagar }
		}
		else_if = { # Velaryon_35 and dragon_seasmoke
			limit = {
				has_character_flag = is_Velaryon_35
				current_date >= 8112.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = seasmoke }
		}
		else_if = { # Targaryen_65 and dragon_sunfyre
			limit = {
				has_character_flag = is_Targaryen_65
				current_date >= 8118.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = sunfyre }
		}
		else_if = { # Targaryen_66 and dragon_dreamfyre
			limit = {
				has_character_flag = is_Targaryen_66
				current_date >= 8119.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = dreamfyre }
		}
		else_if = { # Targaryen_67 and dragon_vhagar
			limit = {
				has_character_flag = is_Targaryen_67
				current_date >= 8120.2.20
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vhagar }
		}
		else_if = { # Targaryen_68 and dragon_tessarion
			limit = {
				has_character_flag = is_Targaryen_68
				current_date >= 8127.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = tessarion }
		}
		else_if = { # Targaryen_75 and dragon_vermax
			limit = {
				has_character_flag = is_Targaryen_75
				current_date >= 8127.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vermax }
		}
		else_if = { # Targaryen_76 and dragon_arrax
			limit = {
				has_character_flag = is_Targaryen_76
				current_date >= 8128.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = arrax }
		}
		else_if = { # Farseed_1 and dragon_sheepstealer
			limit = {
				has_character_flag = is_Farseed_1
				current_date >= 8129.12.6
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = sheepstealer }
		}
		else_if = { # Hammer_1 and dragon_vermithor
			limit = {
				has_character_flag = is_Hammer_1
				current_date >= 8129.12.6
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = vermithor }
		}
		else_if = { # Velaryon_43 and dragon_seasmoke
			limit = {
				has_character_flag = is_Velaryon_43
				current_date >= 8129.12.6
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = seasmoke }
		}
		else_if = { # White_1 and dragon_silverwing
			limit = {
				has_character_flag = is_White_1
				current_date >= 8129.12.6
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = silverwing }
		}
		else_if = { # Targaryen_69 and dragon_moondancer
			limit = {
				has_character_flag = is_Targaryen_69
				current_date >= 8130.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = moondancer }
		}
		else_if = { # Targaryen_70 and dragon_morning
			limit = {
				has_character_flag = is_Targaryen_70
				current_date >= 8135.3.3
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = morning }
		}
		else_if = { # Targaryen_74 and dragon_nightfyre
			limit = {
				has_character_flag = is_Targaryen_74
				current_date >= 8144.1.1
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = nightfyre }
		}
		else_if = { # Targaryen_9 and dragon_drogon
			limit = {
				has_character_flag = is_Targaryen_9
				current_date >= 8300.7.3
			}
			agot_cc_canon_dragon_bond_effect = { DRAGON = drogon }
		}
	}
}
